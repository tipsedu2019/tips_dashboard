# T10 — 학생 목록 표시

- 초기 학생 열 폭: 선택44 / 이름180 / 학교120 / 학년64 / 학생연락처152 / 학부모연락처152 / 재원상태88 / 작업44. 저장된 사용자 열 너비/순서/표시와 초기화 경로는 유지한다. 학생 열의 최소 허용값만44로 내려 학년64와 작업44가 재개방 시72로 커지지 않게 했다. 고정 이름 열 offset은 실제 선택 열 폭을 따른다.
- 학생명14/20·600, 긴 이름·학교명 전체 줄바꿈. 모바일은 이름 → 학교·학년·상태이며 불필요한 학교/학년/status capsule을 제거했다. 수강/대기 popover는 명시 버튼으로 유지한다.
- `formatStudentContact`는11자리010만 하이픈 표시, null/undefined/빈 문자열은 em dash, 국제번호/예외값은 원문이다. 목록은 기존 plain text이며 tel/clipboard action은 없었다. 저장 모델과 input값은 그대로다.
- 검증: `node --test --experimental-strip-types tests/student-contact-display.test.mjs tests/management-numbered-pagination.test.mjs tests/management-filter-transition.test.mjs tests/data-table-pagination.test.mjs tests/numbered-pagination.test.mjs` —52/52 pass. 수정 TSX/helper/test ESLint pass.
- 실제1440/390 브라우저·20행·열 설정 복구는 통합 QA에서 별도로 기록한다. 이 카드 단독으로 운영 데이터/실저장/배포를 증명하지 않는다.
