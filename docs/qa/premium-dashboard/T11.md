# T11 — 학생 상세 집중도

- 기존 넓은920px sheet, 기본정보 → 수업연결 → 수업·교재 이력 순서, 한 열 전체 너비의 연결/이력, sticky header/footer와 작은 화면1열 필드를 유지했다. 새 wrapper나 중복 header/footer를 만들지 않았다.
- 이름/수업명/교재명14/20·600, label14/20·500, dialog title20/28·600와 자간0. 긴 title은 자연 확장한다. 의미 없는 이력 건수/권수 capsule을 plain text로 정리하고 관계 action은 모바일에서도 뒤쪽 정렬한다.
- field42/control radius6/dialog200ms/control150ms는 기존 공통 토큰을 소비한다. 모바일 입력16px·주 action44px와 reduced-motion은 유지한다.
- 검증: 학생 detail-selects/lifecycle-history, guarded-navigation, management-detail-race —97/97 pass. 범위 ESLint pass. 특히 실패 저장의 draft 보존, 명시 discard 전 보호, 취소 후 input focus 복귀, 재시도의 제출값 수락을 기존 실제 hook harness가 검사했다.
- 운영 저장, 실제 브라우저 error/focus/header/footer 확인은 이 테스트 결과와 구분한다.1440/390 브라우저 결과는 통합 QA 기록을 따른다.
