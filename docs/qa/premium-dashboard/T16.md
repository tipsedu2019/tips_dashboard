# T16 — 교재 5탭 수량과 작업 표현

- 재고: 실제 위치 목록의 본관/별관 등 수량을 모바일에서도 0까지 표시하고 권 단위·우측 정렬을 맞췄다. 합계0은 muted, 음수는 semantic destructive. 판매가/재고금액 기존 mode와 계산 보존.
- 주문·입고: 요청/주문/입고3단계와 학생용/교사용6수량 계약을 보존했다. 각 값에 권을 붙이고, 취소/반품을 제외한 입고 열 아래 `남음 max(0, 주문-입고)권`을 실제 표시 행 값으로 표시한다. 다음 유효 주문/입고만 primary, 기존 수정/반품/삭제 조건 및 메뉴 보존.
- 출고: 수량을 실제 line 상태에 따라 대기/출고/반품/취소와 함께 표시한다. 응답에 없는 별도 과거 출고/누적 반품 총량을 만들지 않았다. 기존 완료/반품 가능 여부와 busy 경계를 보존했다.
- 실사: 수락된 currentQuantity를 장부(권), 현재 draft 입력을 실사(권), 기존 계산 차이를 차이(권)로 분리한다. 입력만으로 저장 표현이나 mutation을 일으키지 않는다.
- 요청: 실제 대상 수업/위치와 학생용·교사용 요청 수량 및 권을 유지한다. 임의 부족 기준이나 주문량 자동 변경 없음.

검증:

- `node --test --test-concurrency=2`로 textbook-quantity-presentation, textbook-workspace, textbook-filter-controls, textbook-mobile-filters, textbook-stock-count, textbook-sale-transition, textbook-sales-busy-state, textbook-draft-navigation, textbook-numbered-renderers 실행:173/173 통과. `/tmp/t16-tests-final.log`.
- 새5개 DOM 검증: 실제0위치 둘·neutral tone, 요청 scope 단위, 부분입고 주문10/입고4/남음6, 반품2권·재출고 action 없음, 장부7에서 draft5 차이-2·write 없음. 기존 테스트는 추가한 단위에 맞는 기대값만 갱신했다.
- 기존 회귀는 부분 성공 뒤 완료 행 보존/실패 재시도, 응답 유실의 동일 request identity, 저장 중 새 draft 보존, 출고 busy/반품 계약을 포함한다. 서비스/DB 계산 코드는 변경하지 않았다.
- 수정 workspace 및 두 테스트 ESLint 오류0, `git diff --check` 오류0.
- 실제 Chromium live dev3216, 생산 DTO 계약을 사용하는 합성 fixture:5탭×390/1440=10 captures. 모두 문서 가로 overflow0, runtime/console error0, 미정의 RPC0. 부분입고 남음6권 확인;390 실사 입력5→차이-2권과 mutation0 확인.390 재고/부분입고/실사 PNG 직접 시각 검수.
- 증거: `/tmp/tips-premium-t16/manifest.json`, 동일 경로10 PNG. `/tmp/t16-browser.mjs`는 QA harness 임시 복사와 strict DTO fixture를 사용하며 QA 소유 파일은 수정하지 않았다. 외부 provider는 차단했다.

운영 DB 수량 변경, 실제 주문/입고/출고/반품/실사 저장, 내보내기 실행 또는 배포 증거가 아니다.
