# T14 — 등록 상세의 위치와 편집 상태

- 최신 detail은 이미 학생/과목/진행상태를 sticky header에 유지하지만 `registration-track-editor.tsx`의 `progress={null}` 때문에 기존 stepper는 사용되지 않았다. 허용받은 범위에서 해당 파일의 import와 progress slot만 연결했다. 진행 표시의 상태는 기존 `getRegistrationApplicationProgress`를 사용하고 현재 진행상태 select/선택지/handlers는 보존한다.
-7개 실제 편집 section의 기존 순서에 대응하는 탐색을 한 줄 가로 스크롤로 렌더링한다. 현재 과목 단계만 가로로 드러내고 form 전체를 자동 스크롤하지 않는다. 버튼 클릭은 기존 section을 스크롤하고 제목으로 focus를 보낸다. `aria-controls`를 연결하고 reduced-motion을 따른다. 목록의9단계 workflow/counts는 그대로다.
- 제목은 긴 학생명도 줄바꿈한다. sticky header의 실제 높이를 ResizeObserver로 측정해 section scroll margin을 맞춘다. 변경 과목/알림 영역으로 header 높이가 달라져도 heading이 가려지는 것을 줄인다.
- 편집 section을 접거나 unmount하지 않는다. 각 section의 기존 save handler와 footer ownership를 유지하고 save button의 최소 너비·모바일44px 높이·`aria-busy`만 보완한다. 여러 section의 독립 저장을 하나의 임의 전체 저장으로 합치지 않았다. 권한, stale/unsaved guard, 고객문구, provider/no-send는 변경하지 않는다.
- 검증: `premium-registration-detail`, `registration-application-model`, `registration-create-form-layout`, `registration-track-workspace`, `registration-observation-draft-navigation`214/214 pass. 변경4개 TSX와2개 test ESLint pass. exact files diff-check pass.
- 새 mounted React test: 입학→문의→상담 이동의 실제 heading focus, reduced-motion scroll, 같은 input DOM과 변경 초안 보존, 단계 rerender 동안 mount1/unmount0, 필드 오류 focus/description 도달, 저장 중 disabled+busy를 검증한다. 기존 workspace runtime mocks에는 새 presentation dependency를 추가하고 기존 `progress=null` source 계약만 새 slot으로 수정했다. T13 shared surface 변경에 해당하는 기존 source assertion1개도 수정했다.
- 실제1440/390 sticky header/scroll/모바일 키보드·행 동작은 root 통합 브라우저 QA에서 별도 기록한다. 실제 운영 저장·provider 호출·배포 증거 없음.
