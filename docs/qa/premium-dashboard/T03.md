# T03 — 날짜와 일정 중심 첫 화면

- 시작 SHA: `e284fbd2` (T02).
- 종료 SHA: 이 기록을 포함한 `feat(dashboard): present dated daily schedule states` 커밋.
- 변경: 오늘의 일정 제목·한국 달력 날짜, 실제 generatedAt 기준 시각 및 새로고침. 세 유형을 하나의 영역에 건 단위로 표시. 일정 원본 제목·과목·장소의 전체 줄바꿈과 공급된 href 유지.
- 변경: 최초 3지표/3행 skeleton, 조회 실패의 미조회 표시와 다시 시도, 성공 0건의 정확한 범위 문구와 등록 일정 링크, 갱신 중/실패 시 이전 행·시각 표시. 세 유형 합계 5 초과 시 등록 일정 링크 제공. 등록·학사·통계 바로가기는 낮은 위계의 텍스트 링크로 유지.
- 변경 파일: `src/features/dashboard/dashboard-daily-brief.tsx`, `tests/dashboard-daily-brief-interaction.test.mjs`, `tests/statistics-workspace-interaction.test.mjs`, 이 문서. 기존 통계 테스트는 daily brief가 재사용하는 실제 Skeleton 로더만 추가.
- 검사: `node --test --experimental-strip-types tests/dashboard-daily-brief.test.mjs tests/dashboard-daily-brief-interaction.test.mjs tests/statistics-workspace-interaction.test.mjs` exit 0, 17/17 통과. 실제 컴포넌트·Button·Skeleton 렌더에서 날짜/단위/기준시각, loading/error/empty/5행/refresh 실패, 다섯 원본 href·지난 scheduled 행, 다시 시도 및 새로고침 callback, 기존 바로가기와 통계 상호작용 확인.
- 검사: `node node_modules/eslint/bin/eslint.js src/features/dashboard/dashboard-daily-brief.tsx tests/dashboard-daily-brief-interaction.test.mjs tests/statistics-workspace-interaction.test.mjs` exit 0.
- 검사: `node node_modules/typescript/bin/tsc --noEmit --incremental false` exit 0. `git diff --check` exit 0.
- 런타임: `/Users/hyunjun/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node`.
- 보존: final ordered migration search에서 `20260813192115_dashboard_daily_brief.sql` 한 정의 확인. 오늘 scheduled 시간순 첫5개와 기존 href를 그대로 사용. openTasks 비노출. 새 API·카운트 deep link·커스텀 단축키·발송·공개 UI 없음.
- 브라우저: controller의 T01 baseline 완료 이후 1440/390에서 loading/error/empty/5행/긴 콘텐츠/키보드 캡처 검수 대기. DOM 검사는 실제 픽셀 배치나 터치 검증이 아님. 공유 baseline 서버 보호를 위해 build/서버 재시작 미실행.
- 남은 사항: max1120, 모바일48px/desktop64px 시간열·64px 최소 행 및 긴 제목 줄바꿈은 소스에 적용되었으나 브라우저 측정 전. T03 구현과 집중 검사는 완료, 브라우저 검수 및 릴리스 판단은 controller에 전달.

- T21 route audit 반영: `등록 일정 보기`는 실제 달력 계약 `/admin/registration?view=calendar`로 연결. 하단 `등록`은 기본 상담 목록 경로 유지. 날짜 필터나 유형별 신규 deep link 없음. 빈 상태/5초과 상태 모두 실제 href 렌더 테스트에 고정. 후속 `fix(dashboard): open registration calendar from schedule` 커밋에 기록.
