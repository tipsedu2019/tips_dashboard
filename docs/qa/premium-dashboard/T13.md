# T13 — 등록 목록의 과목 대응과 반복 표식

- 현재9개 view의 column·counts·진행 선택지·권한·행 열기와 이벤트 전파 차단을 유지했다. 학생 이름600, 학교/학년은 plain 보조 텍스트, shared table surface와 header44/row48 최소 토큰을 사용한다.
- 상담 신청/완료·대기·청강·등록 신청·완료의 desktop 열은 같은 CSS subgrid 행을 공유한다. 긴 책임자·수업·교재·시간 문자열이 한 과목의 높이를 늘리면 다른 열의 같은 과목도 함께 늘어난다. 값이 없는 트랙도 `미정`/`미지정` 공간을 남긴다. 상태 열의 과목을 기준으로1회 보이며 다른 열의 과목은 screen-reader 문맥으로 보존한다.
- 모바일은 열별로 쌓이므로 다과목의 과목 문맥을 각 값에 유지한다. 단일 과목은1회만 표시한다. level-test는 예약이 과목을 합친 appointment group이므로 기존 그룹 과목 표식과 장소 대응을 유지한다. 문의/입학 진행의 공통 facts도 억지로 과목별로 복제하지 않는다.
- 검증: `premium-registration-list`, `registration-case-list-model`, `registration-workflow-status`, `registration-observation-status-independence` 26/26 pass. 변경 TSX/test ESLint pass. 새 렌더링 테스트는3과목·긴 담당명·서로 다른 전화/방문/미정 시간·단일 과목을 실제 model/component로 검증한다.
- CSS의 실제1440/390 geometry와 select/row keyboard는 root의 통합 브라우저 QA에서 별도 검증한다. fixture는 운영 등록 조회/저장, provider 또는 배포 증거가 아니다.

## 실제 컴포넌트 브라우저 확인 — 2026-09-15

실제 RegistrationCaseList와 실제 목록 모델을 별도 QA 앱에서 렌더링했다. 1440px에서 세 과목의 네 대응 열이 각각 동일한 시작점/높이를 갖는다(32/43/32px). 긴 담당자가 두 줄이 되어도 행 대응을 유지한다. Enter와 390px 터치 클릭은 권한에 맞는 편집 콜백을 각각 한 번 호출한다. 오류·가로 넘침 0건. 초기 검수기는 관리자의 onEdit 대신 onOpen만 세었으므로 검수기 콜백 집계만 수정했다.

- 재사용 fixture: `tests/fixtures/premium-ui-gallery.tsx`
- 실행: `/tmp/tips-premium-dashboard-20260915/registration-list-check.mjs`
- 결과/캡처: `/tmp/tips-premium-dashboard-20260915/registration-list/`
