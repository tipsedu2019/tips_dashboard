# T12 — 수업 목록·상세 일관성

- 초기 수업 열은 이름 → 과목·학년 → 선생님 → 요일/시간 흐름으로 정렬했다. 기존 필수 열, 저장된 사용자 열 순서/너비, 필터·정렬·페이지는 유지한다. 수업명14/20·600와 semantic foreground, 모바일 이름 우선·plain 과목/학년/상태를 적용했다.
- 시간 구분자를 표시 단계에서만 en dash로 바꾼다. `formatClassScheduleDisplayLines`의 요일/담당/장소 묶음 계약과 `formatClassScheduleLineForList`의 정확히 같은 공통 담당/장소만 생략하는 비교를 유지했다. 수업 상세 header에도 슬롯별 줄을 표시한다. 원본 문자열/파싱/서비스 값은 바꾸지 않는다.
- 학생 roster의 이름14/20·600, 학교/학년/연락처14/20·400, 연락처 표시 helper 통일. 기존 수강·대기 명단과 학생 상세 이동 confirmation, 초안 guard, 뒤로가기 context는 유지한다. 상세1024px width와 기본정보/시간표/교재/학생 구조, sticky footer는 재설계하지 않았다.
- class field42/radius6/dialog200ms 토큰 사용, label14/20, 긴 title 전체 줄바꿈을 적용했다.
- 검증: `schedule-time-display`, class-student-roster, management-numbered-pagination, class-create-atomic, class-close-atomic, class-schedule-slots, management-continuous-class-schedule, management-students-toolbar —128/128 pass. 변경 TSX/helper/tests ESLint와 diff-check pass. 변경된2개 source-regex는 기존 badge/단일 summary span 대신 plain status·slot renderer를 확인하도록 갱신했다. domain/confirmation/atomic assertions는 그대로다.
- 같은 시간에 월/수 선생님·강의실이 서로 다른 fixture를 기존 slot display helper와 새 시간 display helper에 함께 넣어 각각 보존됨을 검증했다. 실제 브라우저1440/390·filter return·focus는 통합 QA에서 별도 기록한다. 운영 실저장·배포 없음.
