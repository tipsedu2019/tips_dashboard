# T01 — bounded baseline harness

Status: **baseline delivered; full T01 target/state matrix incomplete**.

Implemented reusable `scripts/qa/premium-dashboard-browser.mjs` and `tests/fixtures/premium-dashboard.mjs`. They run against the real immutable Next build, mock only transport/auth data, reject unknown API contracts and block external browser traffic. No UI, backend, auth implementation, dependency, production fixture route or provider changes were made.

The canonical six-route A baseline passed at 1440×900 and 390×844: 12 accepted captures, zero unhandled contracts, runtime/console errors and body overflow. Visible images are loaded; browser version, build ID, source attribution, network inputs, rendered text and diagnostic navigation timings are recorded. The old build belongs to `5e423390`, while the script records the independently changing checkout SHA separately.

See [baseline.md](./baseline.md) for exact invocation, captured data, artifact location and limitations. The A–G master matrix and target review are not complete: registration E track correspondence, F stale/error races, authenticated G changes, student detail and 60-character class fixture are remaining work. B/C/D and partial F/G branches are preparatory data, not test evidence. No release, production-save or real-send claim is made.

Validation: actual production-build browser run (12/12), `node --check` for both new modules and `git diff --check`. No rebuild required for this harness-only work. The earlier probe captures under `/tmp/tips-premium-dashboard-20260915/before` are superseded by `/before/complete` and excluded from the accepted baseline.
