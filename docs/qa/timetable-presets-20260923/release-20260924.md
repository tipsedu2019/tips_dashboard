# 2026-09-24 운영 출시 기록

사용자가 최종 마무리와 운영 배포를 요청했다. 실제 수강 반영, 기존 초안 가져오기, 알림 발송은 출시 작업에 포함하지 않는다.

## 최종 변경

- 네 프리셋 보기의 시간 축을 09:00–24:00으로 고정하고 시간 범위 필터를 제거했다. 범위 밖 수업은 원본과 목록 및 충돌 검사에 보존하며, 경계를 걸치는 블록만 표시 범위로 자른다.
- 미배포 중간 migration `20260923085008`의 10개 수동 40001을 최종 정의와 같은 P0001/23P01로 바로잡았다. 이 파일의 manifest hash만 변경했다. 기존 baseline/catalog 및 main의 final migration hash는 그대로다.

## 출시 전 검증

- 시간표 및 인접 일정 Node 회귀: 219/219 PASS (`evidence/release-20260924-node.log`).
- 별도 network-none DB `tips_timetable_release_20260924`: baseline/prerequisites + ordered migrations 118개 재생 PASS, 시간표 SQL 11개 파일 668/668 PASS.
- 최종 관련 함수 45개의 definition/owner/ACL/settings가 `final-fix-verified` 증거와 완전히 일치한다. 기존 수동 fixture 세 개도 변경되지 않았다.
- domain SQLSTATE contract, migration layout, manifest final/hash 검증 PASS. 기존 baseline와 catalog hash 불변.
- 고정 시간 축 UI 변경 후 release build와 변경 파일 lint PASS. 로컬 브라우저 네 보기 및 모바일 390px에서 필터 제거/범위/가로 넘침 없음 확인.
- 운영 DB 사전 읽기: 신규 planner table 없음, class 73개, normalized slot 5개, legacy planner preference 17개, 개강 준비 class 0개. 내용 복사나 변환 없이 개수만 확인했다.

## 운영 적용

아직 미실행. GitHub CI, 운영 schema migration, Vercel Production 및 운영 읽기 확인 결과를 여기에 기록한다.

## PR CI 후속 수정

첫 PR CI에서 수업관리의 일정 stale 오류가 한국어로 변환된 뒤 기존 오류 코드 문자열 검사에서 누락되어 최신값 복구 버튼이 사라지는 회귀를 발견했다. 원본 오류의 message를 판별하도록 수정했고, 기존 race/continuous/progressive UI 검사 110/110, TypeScript, 변경 파일 lint가 통과했다.

기존 SQL 다섯 파일은 과거 중복·불완전 시간표를 fixture로 만들므로, fixture 준비 직후 owner 권한으로 해당 트랜잭션의 운영 점유 기준을 고정하도록 정리했다. 테스트 대상 RPC를 호출하기 전 모든 constraint를 다시 immediate로 돌리며, 제품의 함수·트리거·권한과 충돌 assertion은 변경하지 않았다. 로컬 재검사에서 numbered 134, statistics 55, observation status 27, booking 47개가 통과했다. archived delivery는 기존 실패 지점을 넘어 41개 통과 뒤 이 PostgreSQL-only 컨테이너의 dblink TCP 접속 제한에서 멈췄다. 전체 결과는 Supabase CLI 환경의 PR CI 재실행으로 확인한다.


## CI 두 번째 보완 및 전체 격리 검증

- 시간표와 수업 저장 RPC에 8초 제한 및 자동 재시도 금지를 적용했다. 호출자 취소는 제한 시간과 결합한다. 프리셋 목록은 30개씩 조회하며 전체 목록은 기존 페이지 순회로 읽는다.
- 조회량 검사기는 JSX를 올바르게 해석하고 최종 SQL로 증명된 정확한 RPC만 단건·목적별 문맥으로 분류한다. 목록 RPC의 누락/동적/초과 페이지 크기는 계속 차단한다. 기존 부채 허용 목록과 CI workflow는 수정하지 않았다.
- 기존 파서의 문자열 모양에 의존하던 두 검사는 실제 교사·강의실 파싱과 표시 동작으로 검증한다. 일정 생성 후 요청 키를 비우는 구현에 맞춰 기존 source assertion 한 개를 갱신했다.
- 청강 보관 동시성 테스트의 별도 DB 접속 준비를 같은 테스트의 로컬 catalog 쓰기보다 먼저 실행한다. 새 운영 일정 advisory lock과 테스트 자신이 교착하던 원인이며, 운영 잠금/권한/동시성 단언은 유지한다.
- CI와 동일한 별도 Supabase 실행: SQL 57개 파일 + 동시성/DTO probe 9개, DB lint 및 postdeploy 계약 모두 PASS. 임시 스택 종료 및 삭제 PASS. 실제 운영 DB는 변경하지 않았다.
- 요청 제어·조회 계약·인접 서비스 229/229, 설치된 Supabase 전송의 페이지 크기/취소/시간 제한/재시도 3/3, 시간표와 인접 화면 299/299 PASS. TypeScript `--noEmit --incremental false` PASS. 목록 검사 worktree PASS.
- 배포 SQL 검사의 트랜잭션·시간 제한 보완은 `/tmp/timetable-migration-release-safety.patch`로 분리했다. 자동 승인 검토가 실행 단위 변경에 대한 별도 확인을 요구해 8개 migration 수정 승인을 요청한 상태다. 이 패치는 아직 적용하지 않았고 운영 배포도 시작하지 않았다.


## 사용자 승인 후 SQL 보완

사용자가 2026-09-24에 SQL 보완 및 운영 배포 진행을 승인했다. 미배포 migration 8개에 트랜잭션 경계·lock timeout 5초·statement timeout 120초와 한정된 린트 주석을 적용했다. 최종 제약, 함수 본문 및 권한은 유지한다. baseline과 기존 main migration 해시는 변경하지 않았다.

- Squawk 2.63.0 / PostgreSQL 17: 신규 migration 13개 모두 PASS.
- 새 network-none DB `tips_timetable_approved_20260924`: 118개 ordered migration 재생 및 SQL 668/668 PASS.
- 관련 최종 함수 45개 definition/owner/ACL/settings가 출시 전 `release-20260924-final-definitions-acl.json`과 동일하다. 기존 수동 fixture 3개도 그대로다.
- manifest 비실행 검사, domain SQLSTATE 검사, rollback preflight/배포 영수증/SQLSTATE 테스트 23/23 PASS.
- 다음 단계: 최신 PR CI, 운영 migration 적용 및 RPC/ACL 확인, main 병합·Vercel 운영 배포, 운영 화면 확인.


## 운영 롤백 사전 검증에서 발견한 legacy 조회 성능 수정

승인 커밋 `68870ce2`의 PR CI와 Preview는 통과했으나, 운영 DB workflow [35999549146](https://github.com/tipsedu2019/tips_dashboard/actions/runs/35999549146)의 rollback pgTAP에서 실제 legacy 일정 6,305개를 읽다가 60초 statement timeout이 발생했다. db-push는 건너뛰었고 운영 ledger는 327개/최종 `20260922140000` 그대로였다. 시간 제한을 높이거나 충돌 검사를 우회하지 않았다.

새 migration `20260924124904`는 legacy JSONB 반복 복사를 expanded array append로, class별 중복 identity 조회를 단일 집계로 바꾼다. 운영 guard는 점유가 그대로면 반환하고 unresolved 차집합을 집합 연산으로 비교한다. 기존 충돌 판정·순서·유실 방지·잠금·권한을 보존한다. 실제 legacy 교재/진도 필드 `textbookEntries`, `progressStatus`는 점유 fingerprint에서 제외하므로 내용 저장이 새로운 불명 점유로 오인되지 않는다.

- 운영 원문 복사 없이 실제 필드 구조와 집계량으로 합성 50개 수업/6,400개 일정 생성. 이전 조회는 2초 제한에서 `57014`, 수정 조회 3회는 184/196/187ms, 실제 signup trigger 및 deferred guard 590ms. 모든 fixture는 rollback했다.
- 정상·이름 참조·scalar/null·중복/빈 identity·잘못된 시각/날짜·누락 자원 15가지의 이전/최종 reference JSON이 정확히 일치한다. 내용 필드 변경 시 reference와 fingerprint 불변.
- 새 network-none DB: baseline과 ordered migration 119개 재생, 시간표 SQL 12파일 **680/680 PASS**. 실제 authenticated 내용 저장, 새 불명 점유의 `23P01`, 변경 거절 시 원본 보존, label/중복/제거의 이전 guard 결과 일치 포함.
- 최종 관련 함수 45개 중 성능 대상 2개 본문만 변경. 나머지 43개 정의와 전체 45개 owner/ACL/settings 불변. 기존 수동 fixture 3개 불변.
- 새 SQL Squawk, domain SQLSTATE, manifest, rollback preflight/배포 영수증 테스트 23/23 PASS.
- 새 커밋으로 PR CI 및 운영 rollback preflight를 다시 통과한 뒤에만 실제 migration과 UI를 배포한다.

증거: `performance-legacy-reference.json`, `evidence/legacy-perf-*`; 재현: `scripts/qa/timetable-legacy-reference-performance.mjs --local --container tips_timetable_legacyperf_20260924`.
