# Task5 fixround1 scoped re-review
Reviewer /root/rereview_timetable_controller, gpt-6-astra high,2046d4e1..2fe4b846.

1. Older receipt resurrects an authoritatively deleted item — ADDRESSED. timetable-plan-model.ts109–124,301–304 rejects superseded receipts and preserves submitted draft. Deletion-order regression tests/timetable-plan-controller.node.ts33. Separate introduced regression below.
2. StrictMode cleanup destroys reused controller — ADDRESSED. use-timetable-plan-session.ts19–39 resumes setup and pauses cleanup; model452–466 invalidates without permanent disposal. ActualReactStrictMode test loads/saves through replay.
3. Own-save signal blocks next queued edit — ADDRESSED. model301–315 clears stale on matching authoritative revision/slots and rejects genuinely superseded receipts. Tests34 drainsA→B,31 collaborator protection.
4. Logout clears only active plan key — ADDRESSED. recovery.ts17–25 synchronously retires active controllers before actor-wide key deletion; auth-provider.ts183–192,264–275,432–433 calls on anonymous/actorchange/logout. Session22–23,32–39 destroys retired but pauses ordinary sameactorunmount. ActualReact tests16–17 cover unmountedplans+cleanuporder.
5. Transfer validation accepts incomplete/inconsistent data as verified — NOT ADDRESSED (Important). Missing source metadata and dated-reference fields now rejected, but src/features/academic/timetable-plan-service.ts102–107 validates appliedItems with shallow planItem predicate15–17. Still accepts applied entry with another planId,state draft,no name,no applied-class/transfermetadata. model424,434–448 replaces source item,removes slots,publishes verified. Focused probe accepted {id:'a',planId:'other-plan',revision:2,state:'draft',pendingSlots:[]} and produced malformed item+slots[]+verified. Complete applied-item shape and source/state consistency must be checked before canonical merge; original element-shape/response-consistency gap.
6. Minor successful undo test — ADDRESSED. test23 callsundo and asserts coordinates,expectedrev2,newkey,result.

New breakage — Important: valid independent creations fail when receipts arrive out of order. model114,123–124,301–304 treats latest merged receipt sequence as evidence absentitem authoritativelydeleted. Startseq1/noitems,dispatchindependentcreatesA/B,deliverBseq3beforeAseq2,NOrefresh. Both writes succeed but A rejects timetable_stale; serveronlyB/controllerstale. Probe {a:'timetable_stale',b:'saved',serverItems:['b'],draftItems:['b','a'],state:'stale'}. Track authoritative-read provenance or per-item deletion evidence separately from receipt-advanced sequence; preserve independent replies and deletionprotection.

Out-of-scope observations:none. Checks:suppliedfixpackage+context,50/50log,emptytsc/lint; no suite reruns,2 focused in-memory probes only; no mutations.
Spec FAIL; quality Needsfixes. Openoriginal5+newindependentcreateregression. Task7backend/browser/liveRealtime/prod separatelatergates.
