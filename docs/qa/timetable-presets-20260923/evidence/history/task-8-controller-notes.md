# Task 8 runtime integration

Isolated test DB is named Docker container tips_timetable_20260923, PostgreSQL17.6.1.159, --network none/socket-only, no hostport, no productiondata. Docker /Users/hyunjun/.local/bin/docker; psql only inside container. Existing successful SQL command: docker exec -i tips_timetable_20260923 psql -U postgres -d postgres -X -v ON_ERROR_STOP=1.

Controller ruling: the concurrency verifier may accept explicit --local --container tips_timetable_20260923 instead of forcing a TCP URL for this stricter isolated transport. Verify named container network-none/non-production before seeding. Use two persistent independent psql connections (e.g. node child_process pipes through docker exec), record backendPIDs and actual lock/wait/barrier evidence; do not replace a race with serial calls or guessed sleeps. Observer queries may use another read-only connection. Never obtain URL/token from production environment files. CLI URL mode is optional only if explicitly supplied/verified local.

Fixture writes are disposable synthetic data in this named DB only. Retain raw results and verify both statement errors and final database values; psql0 alone is not pgTAP success. Root owns Nextfixture/browser, coordinate before needing browser performance measurements. Full no-send/database history checks remain required; no actual providers.

Additional root ruling/Task4 defense: shared lock helper rejects timetable writes outside READ COMMITTED/READ UNCOMMITTED with25001/timetable_isolation_not_supported; a RR snapshot fixed before lock cannot be made fresh merely by waiting for advisory lock. Add actual snapshot-pinned two-connection attempt (one normalRC commit, otherRR direct/public write rejection), exactcode+message, unchanged rejected transaction and finalnooverlap. Reads remain supported. Do not confuse SET TRANSACTION after-query errors with the guard error. Existing genuine serialization40001 must not be mapped to a domain conflict.

Concrete performance risk to measure, not speculative refactor: Task4 operating reference currently serializes datedSessions across class statuses and composite revision fingerprint includes actual dated data; before/after guard snapshots execute under global lock. In addition to mandated pure/drag fixture measurement, record local read/revision RPC and guarded-write timings/payload size with a representative dated-session history in the synthetic fixture, so full historical data does not silently make20second poll/writes impractical. Keep these separate from browserframe p95; only optimize if measurements show a real concern. No production query/data needed.

Local browser QA runtime discovery (read-only, no new test run): existing scripts/qa/premium-{dashboard,class,settings}-browser.mjs use Playwright through createRequire(PLAYWRIGHT_PACKAGE). docs/qa/premium-dashboard/baseline.md:29 points to /Users/hyunjun/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/package.json. Installed headless Chromium binaries found under /Users/hyunjun/Library/Caches/ms-playwright/chromium_headless_shell-{1234,1223}/chrome-headless-shell-mac-arm64/chrome-headless-shell. Verify package/runtime when used; do not install/change dependencies just for QA. A purpose-built isolated local browser test can use separate browser contexts for two actors (same-origin localStorage is isolated per context), while root manual CUA tabs need distinct origins if sharing context. Existing QA scripts enforce loopback-only requests, block unknown API/external routes, and store screenshots/results; preserve those boundaries. Test against actual allowed isolated-DB RPC adapter for save/collaboration proof, not a duplicate in-memory planner backend. Coordinate ports/browser ownership with root and read relevant Playwright skill before applying it.

Final-definition SQLSTATE follow-up found by root: Task4 explicitly retained pre-existing makeup domain40001 raises while converting eight class_schedule_stale producers. Those raises are in definitions re-emitted by this branch, so final repository contract still requires correction before enabling operating transfers. Current Task4migration line325/366/382 transition_makeup_request_v2_unguarded (makeup_request_stale_status/source_changed); line607 notification_revert_makeup_calendar_effects_legacy_v1 (makeup_schedule_plan_stale); line1310/1340 notification_apply_makeup_calendar_effects_legacy_v1 (schedule_plan_stale/calendar_event_conflict); line1360/1364 revert_normalized_makeup_effect_v1 and1435/1438 apply_normalized_makeup_effect_v1 (lesson_session_stale). Five functions, ten manualraises. In Task8 final concurrency/error validation, add a narrow CLI-generated migration correcting these domain states to suitable non40001 codes while preserving message names/publicsignature/ACL/locks/idempotency and real database40001. Verify actual final definitions with exact executed pgTAP and impacted makeup consumer mappings/existing focused assertions; no blanket replacements across unrelated history. Task4original audit remains historically valid, but this follow-up is an open release gate until fixed. No productionrelease before finalgate.

Performance evidence must name the browser/build mode. Existing Next3261 is development webpack. A passing dev frame target is useful but is not a production deployment measurement. If dev overhead alone appears to exceed the target, confirm the same synthetic fixture under a local release build before making speculative rendering changes; coordinate any server stop/restart with root and keep safe loopback configuration. Reuse Task9 build/runtime conventions and report distinct pure projection, actual pointer frame, and DB/reference timings.

Additional Task8 preflight found a sixth final makeup function on the actual transition path: dashboard_private.notification_assert_makeup_room_available_v1(uuid), last defined20260716192000_notification_makeup_adapter.sql:2211–2258, called by Task4 final transition at20260923085008:384, raises40001/makeup_room_collision. Root ruled include this one adjacent domain-code conversion with preserved message/locks/ACL, not broad unrelated notification SQLSTATE cleanup. Actual old pgTAP expectation is supabase/tests/notification_makeup_adapter_test.sql:277. Consumer compatibility concrete risks: src/app/api/makeup-requests/approve/route.ts:285 maps only40001 to HTTP409 (new P0001/23P01 otherwise becomes503); src/features/makeup-requests/makeup-request-workspace.tsx:250 only gives generic retry for40001; src/features/makeup-requests/makeup-create-attempt.js:105 classifies40001 but notP0001 as conclusive. Check which new message pairs each reachable client consumes and preserve immutable unknown outcomes, not a blanket allP0001 discard. Existing replay test tests/notification-makeup-adapter.test.mjs:864 uses40001/makeup_request_stale_status. Update relevant final-definition tests with actual exactSQLSTATE and genuine40001 compatibility. Earlier five functions/tenraises still apply; this helper addsone.

Runtime after Task7: committed fb5f3a600ebaaeebea1cbc434bef79057d233bd3, proxy/API session3609 implementer-owned, Next3261 originalsession94061/previousnodePID11136 (verifybeforestop). Read task-7-report.md for selectedtransferRPC/pending helpers/receipts and synthetic audit-delete exception. Synthetic fixture key changes are isolated to explicit bridge; x-timetable-fixture-db:1 activates actual operating reader. Root CUA tab2 is idle; coordinate any server restart. Safe Nextdev env documented in browser-baseline-notes.md: NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:3262, NEXT_PUBLIC_SUPABASE_ANON_KEY=fixture-only, NEXT_TELEMETRY_DISABLED=1. No productionsecret envread. next.config.ts has no custom distDir; don't build over a live dev .next without coordination.

Post-Task7 root runtime refresh: actual docker inspect confirms tips_timetable_20260923 is running, network=none, ports={}, postgres17.6.1.159. Escalated lsof shows ONLY Next nodePID11136 listening on127.0.0.1:3261;3260/3262 are free after implementer finished, so reported proxy session3609 is no longer a live listener. Task8 may start a fresh scoped fixture proxy when needed; no unrelated process cleanup.
