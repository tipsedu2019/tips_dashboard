# Task 8 implementer report

Status: DONE_WITH_CONCERNS, ready for controller review. Base36989ac56486cc0d5e2920bdb25d2226eb5feeba, branchcodex/timetable-presets-20260923. No push/deploy/production environment or data, provider call, dependency install, original checkout edit, container reset/drop, or subordinate agent. Existing plan/spec remain untracked forTask9. Main review artifact:docs/qa/timetable-presets-20260923/TASK8.md.

## Implementation

1. `scripts/verify-timetable-concurrency.mjs` accepts explicit `--local --container tips_timetable_20260923`; rejects URLs/other containers. Docker inspection asserts networknone, no mappedports, running. Two persistent independent psql processes plus read-only observer establish actual backendPIDs. A executes real firstRPC within transaction; B issues its realRPC and observer must see B's advisory wait, ungranted lock and blocker A before Acommit. No serial imitation or sleep-only evidence. SQLSTATE sentinel plus first-error parsing prevents later success concealing earlier errors. RC/default used for races, RR tested deliberately after snapshot pinning. Synthetic UUIDs/name prefix unique per run.
2. CLI-generated `20260923131454_timetable_makeup_domain_sqlstates.sql` re-emits exactly six final private functions. Five final sources come from20260923085008, room assertion from20260716192000. Eleven domain raises: transition stale status1/sourcechanged2; legacyrevert stale1; legacyapply stale1/calendarcollision1; normalizedrevert stale2; normalizedapply stale2; roomcollision1. Stale→P0001, collisions→23P01; names/messages/signatures/ACL/locks/idempotency intact, genuine40001 unchanged.
3. Actual normalized public approval initially failed42501 because its private apply/revert helpers lacked continuous-class context; previous fixture-injected GUC masked this. Root-approved fix wraps only the two trusted transition effect calls in existing private context helper, passing class ID, actual mutation request key, makeup operation and reason. Captures/restores all5 prior GUCs after success; PostgreSQL statement rollback restores them on failure. No direct-write grant or broad bypass. Auth/source/room/lock order unchanged. Actual public approve and approval_canceled now pass without fixture GUC; subsequent same-transaction direct writes remain42501. Audit rows prove actual request keys/operations/reasons. Mid-effect22023 rolls back the first session update and restores context.
4. `makeup-domain-errors.js` centralizes exact named pairs. Approve route maps them409; workspace gives actionable stale/collision feedback; create-attempt treats only known pairs as conclusive, retaining unknown outcomes. Existing replay expectations updated. Self-review caught actual23P01/timetable_resource_conflict from the public makeup race, which otherwise remained503: exact pair added with actual POST RED→GREEN. UnknownP0001/23P01 still503/unknown; real40001 remains409 and existing recovery behavior.
5. CLI-generated `20260923134651_timetable_reference_aggregation.sql`: observed35s timeout during full-history guard/finalization; original reader18.56s/22.29s per call. Replace only normalized growing JSONB concatenation with ordered jsonb_agg, exact lesson.id ordering/fields/nulls/state/blockers/raw/fingerprints retained. Full7800-row old/new JSONB exactly equal. Actual writes still11s after reader fix, so root approved per-date old/new occupancy-key multiplicity aggregation. Check only increased key candidates against the complete day; duplicate counts, removed skipped overrides, past actuals, future defaults, unknown blockers, existing-conflict repair preserved. No history window, legacy parser, locking, baseline/finalizer or authorization change.
6. Browser profiling observed O(N²) unchanged-pair conflict work and expensive unchanged rendering. `findConflicts` optional changed-ID scope skips only unchanged-vs-unchanged pairs and retains exact canonical ordered results, sibling/shadow/dates/fullscope validation. Pure cap drag24.4ms→0.216ms. Initial preview-only experiment had no useful frame improvement and was reverted. Final isolated preview+pointer error state avoids parent updates (the earlier measured React commit-lag claim is withdrawn in fix round1); measured-height content-visibility skips only offscreen browser layout/paint. All canonical data remains loaded. Every panel first measures its actual content height, preserves native scroll geometry, and forces visible content for export. DESIGN documents the scoped rule.
7. Faster rendering exposed Escape→delayed physical release opening an editor because the old0ms suppression expired. Narrow pointer-ID-scoped up/cancel suppression now persists until release; cleans on new gesture/scope/unmount. Browser RED captured actual dialog overlay, GREEN verifies no form/save/preview and next offscreen drag works. View/filter/unmount use standard keyboard access, no custom shortcuts.

## Exact focused results

Installed Node:`/Users/hyunjun/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node`; Docker:`/Users/hyunjun/.local/bin/docker`; existing Supabase CLIv2.115.0 created both migration filenames (`supabase migration new timetable_makeup_domain_sqlstates`, `supabase migration new timetable_reference_aggregation`). Exact final migration files applied only with approved socket psql. No Supabase TCP port opened for CLI test db.

SQL command pattern:
`docker exec -i tips_timetable_20260923 psql -U postgres -d postgres -X -qAt -v ON_ERROR_STOP=1 < supabase/tests/FILE.sql`.
- timetable_makeup_domain_sqlstate_test.sql25/25. RED filetask-8-sqlstate-red.log:11 wrong40001s and real public approval42501. GREENtask-8-sqlstate-final.log includes11 invoked final branches, public apply/revert,5-GUC restoration, mid-effect failure rollback, same-tx42501, audit mutationkey/operation/reason.
- timetable_guard_aggregation_test.sql12/12 old-v-final exact code/message outcomes: unchangedconflict, teacher/room/sibling introduction, deletionrepair, duplicatekeyincrease/unchanged, pastactual, unknownoccupancy, removedskipped future exposure and pastno-default.
- timetable_operational_conflicts_test.sql135/135, **three plans5+5+125**, not125total. IncludesRR/SERIALIZABLE rejections and RC completeness cases. Allplan/ok lines parsed, no not-ok/ERROR.
- timetable_plan_no_send_test.sql132/132 with nonempty rows; allrollback.
- Sum304/304. `task8-verification.json` records plans individually.

`node scripts/verify-timetable-concurrency.mjs --local --container tips_timetable_20260923 --output docs/qa/timetable-presets-20260923/concurrency-results.json`:10/10. Final run task8- prefix/PIDs/UUIDs in artifact. Each row has actual blocked query, wait_event=advisory, ungranted count and blockerPID. Directions/outcomes:

| race | A/B result |
|---|---|
| same teacher, distinct room |00000/23P01|
| same room, distinct teacher |00000/23P01|
| different editor actors/different items |00000/00000, both retained|
| different actors/same item |00000/P0001, winner retained|
| operating first/old shadow draft |00000/P0001; freshretry23P01|
| draft first/operating second |00000/00000, canonical client sees draft conflict|
| planner promotion first/management |00000/23P01|
| management first/planner |00000/P0001|
| generation first/actual public makeup approve |00000/23P01|
| actual public makeup approve first/generation |00000/23P01|

Separate final SELECT finds weeklyoverlap0 and rejected operating class unchanged. Existing receipt replay exact; current share revocation rejects replay42501. RR snapshot fixed before RCcommit then public and direct timetable writes25001/timetable_isolation_not_supported (not SET TRANSACTION setup error). Independent genuine profiles-row MVCC collision40001/could not serialize access is preserved.

`TIMETABLE_TASK8_LOCAL=1 node --test --experimental-strip-types tests/timetable-release-contract.test.mjs tests/makeup-create-attempt.test.mjs tests/notification-makeup-adapter.test.mjs tests/timetable-plan-interaction.test.mjs tests/timetable-transfer-interaction.test.mjs tests/timetable-image-export.test.mjs tests/timetable-conflicts.node.ts tests/timetable-plan-controller.node.ts`:120/120 before final one-message consumer follow-up. Final relevant consumer command first3files44/44 after shared-guard exact pair; real POST covers7 namedpairs, unknownP0001/23P01, real40001. `task-8-shared-guard-http-post-red.log` records actual503 vs409 failure; `task-8-shared-guard-http-green.log` finalGREEN. Conflict optimization exact ordered equivalence RED/GREEN logged. Final UI pure tests also passed.

`node scripts/qa/verify-timetable-final-sql.mjs --local`:8/8 actual prosrc exactly equals final migration body; all signatures/ownerpostgres/SECURITYDEFINER/search_path empty/privateACL preserved, anon/auth execute false, manual40001 false. `final-sql-provenance.json` contains SHA256 and ACL perfunction. CREATE OR REPLACE retains grants; no new helper grants.

## History and no-send evidence

Rollback-only a824 synthetic fixture has original class student and waitlist IDs/textbook usage/historicJSON,2students/enrollmenthistory, real normalized registration start-claim, completed admission batch/invoice/payment timestamps, attendance observation with normalized provenance, textbook sale and paid12000line, dated lesson, canonical event/delivery and task outbox. Every copy/move (operatingcopy, operatingmove, plancopy, planmove) compares each relation's rowcount and entire rowJSON plus original classJSON, new-class count and empty new history. Thirteen relations checked;12 explicitly nonempty, legacy makeup delivery table also compared even if empty. Inner transfer subtransactions rollback perpath without resetting pgTAP counters. No zero/zero-only history claim.

Actual `createTimetablePlanService` uses socketDB RPC adapter for four previews/commits and immutable replays; actual DB history snapshots identical. Nonempty canonical queue1→1 is separate from observed globalfetch/provider spy0. No sender/provider invocation, external network blocked. This does not claim a live provider integration test.

## Performance and rendering

Representative200/600 and cap500/2000, viewport1440×1200. Initial dev framep95134.3/433.4ms retained separately. Local production baseline33.4/150ms; final17.5/33.6ms. Whole-gesture p9517.6/33.7ms includes down/up, while like-for-like move metric retained. Pure fourprojection p951.55/8.29ms, pure complete-scope changed drag0.080/0.216ms. WITHDRAWN after review: handler0.1/0.1ms and handler-end→Reactcommit4.1/6.1ms were not measured correctly; endpoint registration preceded the product handler. Neither number supports a product handler/commit-duration claim. Pointerdown→secondframe55.5/84.9ms and Escape/up→secondframe73.5/122.2ms reported separately. Chromium LoAF attribution and earlier failed/neutral experiments retained in scratch; no DB performance work overlapped final browser benchmark. **Cap target32ms notmet**; root ruled retain1.6–1.7ms residual, no broad extra refactor or selection of a better repeat.

Browser asserts actual preview during measured gesture, no provider/mutation, fullblock counts in pureprojections. Offscreen documentheight35473/93133 exactly preserved, actual newly visible handle hit, nextdrag preview/cancel clean. Content-visibility paint is awaited by actual hit/rAF before manual coordinate actions, never by suppressing failed assertions.

`node scripts/qa/timetable-reference-performance.mjs --local`:200normalized classes(100closed/100preparing),13weeks×3dated sessions=7800 plus1legacy operating-write class. Allone transaction rolledback. Finalreference140–153ms/2594645B, revision104–109ms/165B. Same actual operating writes underfinalguard479/367/367ms/694B vs11822/11352/11406ms afterreader-only fix. Oldsource cloned only to pg_temp for semantic comparison; exact JSONB equalityTRUE including fingerprints at7800. Originalreader18562ms firstcomparison and22289ms finalcomparison. Legacy parser/unknown/fullhistory preserved, no cutoff.

Safeenv `env -i PATH=NODEBIN:/usr/bin:/bin NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:3262 NEXT_PUBLIC_SUPABASE_ANON_KEY=fixture-only NEXT_TELEMETRY_DISABLED=1 node node_modules/next/dist/bin/next build --webpack` succeeded (last task-8-release-build-complete.log). Root coordinated stopping olddevPID11136. One attempted owned-process stop initially auto-review rejected unverifiedPID; read-only cwd/lsof/ps verification resolved it before an approved stop, no workaround. CurrentUI matches final performance/cleanup code. The final exact shared-guard consumer-message follow-up was Node/POST tested after this UI releasebuild; Task9's next build must include that last message mapping.

`TIMETABLE_QA_PREFIX=task8- node scripts/qa/timetable-plan-pointer-browser.mjs`:19/19 actualRPC checks, full catalog/slot/shadow retained. Initially attempted display catalog slicing was removed perroot before finalrun. Stable resource IDs/native scrolling handle accumulated synthetic catalogs. Fourprojection cross-resource move preserves offset/sibling; Escape100ms laterrelease, pointercancel/lostcapture; keyboard view/filter/unmount whileheld (save0/form0/preview0); five-minute resize; fourpalette drops;60char exact24:00 form; uncertain identical retry; conclusive rejection newkey;390px native touch scroll. Newplanfad77a15-b9f3-4a94-9249-7b9bfc2a51b1/item2b263bd2-e36a-477d-a673-f779abbea2c1, Monday92ec992b-8b6c-4345-87b3-7ec5b4ae3302, Wednesdayf6a8b4cb-902e-4737-920a-3e89a3572dd8. Ownplan archived with4items retained. Failed iterative ownplans likewise archived byfinally. No prior manual item used as writer target.

`node scripts/qa/timetable-render-export-browser.mjs`: actual lastpanel100slots from cap, forcedfullrendering observed duringexport, 23:30–24:00axis, PNG3369×3369/710202bytes, no preview/mutation/provider. Visually inspected complete exported timeaxis. Screenshot+JSON committed as task8-offscreen-export.png/task8-export-results.json.

Manual finalread: ae260000…501 stillrevision8 and Monday550–610/Tuesday540–600/Wednesday1033–1093; e446fbd2-fa5b-4b4d-b244-96a997d896cd stillrevision3 withTuesday/Thursday553–613; main midnight56d614fe-40ed-4861-9332-a1132524e00f Friday/Saturday1410–1440. `manual-fixture-preservation.json` recordsexact rows.

## Historical notification suite limits, not a green claim

Both old suites were run, then original six preTask8 functions/original40001 expectation were restored only inside a rollbacktransaction and failures compared. That alone does not prove branch-origin behavior. Narrow origin evidence: eb23d7d8 contains20260803150000 drop of obsolete public.makeup_notification_settings triggerreconcile_makeup_notification_settings_after_write_v1. Current isolated baseline stillcreatesit; rootlocated baseline42147–42152 and bootstrap-db.py23 baseline/prerequisite/order missing this cleanup. Rollback-only exact origin DROP/legacywriteACL boundary fixes adapter13/writer5 metadata assertions, but NOT other failures. No product workaround or lasting baseline repair.

Adapter24:55000/notification_makeup_legacy_rule_missing, detail91000000-0000-4000-8000-000000000301; producer notification_import_makeup_retained_state_v1 cannot find appropriate workflow/channel/audience rule. prosrc is exactly byte-identical to eb23d7d8's20260716192000 (hash in notification-suite-limits.json), before any timetablepath. Adapter25–28 consequentlyfail; final notification_makeup_legacy_import_incomplete. Writerfixture registry makeup.submitted/subject_team/google_chat +contentcontractjoin returns0rows; assertions8/9/13/15 compareNULL ormissing audit. Later originaltest atline358 directly queries private notification_rules underauthenticated while formatting its SQL;42501 beforeRPCcall. Originaltests unchanged except adapterroom expected40001→23P01 at277. These complete suites cannot be claimed passing in this mixed baseline/prerequisite fixture. Task9 inherits boundary evidence; focused final producer/publicpath tests are independentlyGREEN.

## Cleanup / handoff / concerns

Concurrency deletes only own dated sessions, closes ownclasses, archives ownplans; actors/catalogs/items/receipts retained for audit because deleting receipt-referenced plans hitsFK. Three early failedrun prefixes0a7d5bee/067afe93/a788b607 were separately scoped cleaned toarchived/closed/sessionremoved; no prior manualfixture overwritten. Retained catalogs can expand the fixture UI and are why stableID scrolling matters. Performance/history/pgTAP/origin-comparison fixtures allrollback. Existing audit DELETE FK is unchanged; no product deletion-flow proof claimed.

Live handoff: proxy/API PID38162 session72132 on127.0.0.1:3260/3262; releaseNext PID41198 session26959 on127.0.0.1:3261. Root owns subsequent mode switch. .next currently release; do not startdev/build concurrently. All browser test contexts closed. Next UI is last full performance release; final exact consumerpair requires nextTask9build as stated. No productionsecrets were read/copied. Artifacts underdocs/qa/timetable-presets-20260923; rawRED/iterative/runtime logs underignoredtask scratch. Do not force-add scratch. Task9 stillowns clean imports/two-actorapp/fullregression/finalbuild/manifest/runbook/releaseevidence.

Remaining concerns: cap33.6–33.7ms vs32mstarget; wholegesture down/up cost remains as above; oldnotification suites have fixturemetadata/ACL limits; syntheticlocal measurements are notproduction latency/provider proof. No unresolved functional failure in Task8's focused gates. Self-review found/fixed the real publiccontext gap, Escape-lateclick and reachable sharedguard HTTP503 mapping. Diffcheck clean. Commit message: `test: verify timetable concurrency and preserved operational history`.

## Final validation boundary

Final installed ESLint passed for all changed src/test consumers (0 errors, 0 warnings). Its initial single failure was the test harness variable named `module`; renamed to `routeModule`, then the actual approval POST test passed again (1/1). `node node_modules/typescript/bin/tsc --noEmit --incremental false` passed on the final source. `git diff --check` passed. No SQL/UI/performance rerun after the consumer-only follow-up.

Export review proves the full 09:00–24:00 axis, stamp, all rows and forced rendering; it does not prove every title is fully readable. Some two-lane 30-minute blocks have a partially clipped second title line (including 10:30/12:00/13:30). Root carries this as a Task9/final UI review observation; no additional Task8 styling change.

Final commit: `fffe462c` — `test: verify timetable concurrency and preserved operational history` (40 files). Post-commit status retains only the intentionally untracked Task9 plan/spec; report and raw scratch remain ignored.

## Review fix round1 — fffe462c follow-up

Independent review found a real lifecycle regression: Escape while mouse held, keyboard 새로고침, actual snapshot replacement, physical release on original handle opened editor (dialogs0→1). Reproduced on root-owned release and also reproduced filter variant, mutations0/external0. Raw initial harness JSON: task-8-fix1-cancel-red.json. Initial harness additionally waited for a mouse-handle click to open after a completed drag; that expectation timed out because starting a new handle gesture suppresses its generated click. Final follow-up uses standard Enter activation to verify normal editor access. A synthetic second-pointer then original-handle release can open the editor after new-gesture resets suppression; final scoped second-pointer case checks listener replacement and releases the original mouse outside. It is not evidence for simultaneous multi-pointer click cancellation.

Product diff only removes delayed-release cleanup/reset from snapshot/view/subject/targets/gridCount/canEdit effect; active gesture cleanup remains. Existing start-new-gesture and unmount cleanup remain. Matching pointerup/cancel alone clears the Escape wait across snapshot/filter changes; unrelated pointer999 cannot consume it. New scripts/qa/timetable-cancel-lifecycle-browser.mjs runs seven cases (none/refresh/view/filter/unmount/unrelated-release/new-gesture), allGREEN, with next actual drag and standard Enter activation, no form/save/preview after requested canceled release, pending listener0 after release/unmount. Newgesture second pointer is synthetic and explicitly bounded as above. Browser reads synthetic snapshots, all mutate/commit/external blocked; no DB fixtures touched. Committed task8-cancel-lifecycle.json has RED observations and fullGREEN results.

Timing correction applies to ALL old uses of pointerHandlerP95Ms and handlerEndToReactCommitP95Ms, baseline or final: listener endpoint registered before product listener, so those interpretations are invalid. Removed endpoint/React hook/fields from benchmark; removed both invalid numeric fields from current performance-browser.json/performance-comparison.json recursively and added explicit correction. Independent frame/whole-gesture/input-to-frame/pure-projection/LoAF values unchanged. No new performance measurement; no claim of32ms target satisfaction. Historical raw scratch keeps withdrawn values only as chronology.

Commands/results: installedNode --test --experimental-strip-types tests/timetable-plan-interaction.test.mjs tests/timetable-transfer-interaction.test.mjs tests/timetable-image-export.test.mjs:39/39. ESLint workspace.tsx and both changed/new browser scripts:0errors/warnings. tsc --noEmit --incremental false:exit0. Root stopped release session42459 before safeenv next build --webpack:exit0 (task-8-fix1-build.log). Rebuilt release lifecycle command:exit0,7/7 (task-8-fix1-cancel-green.log). Same explicit loopback fixture env as above; final consumer mapping now included in rebuilt release. No SQL reruns, DB writes, provider sends, installs, push/deploy, or Task9 work.

Runtime handoff supersedes earlier values: new releaseNext PID44652/session89554 on127.0.0.1:3261; root proxy/API PID43514/session14304 on3260/3262 unchanged. All test browser contexts closed. Root owns subsequent mode switch; verify listener lifetime after worker completion.

Fix round1 commit: `ed3e7a31` — `fix: retain canceled timetable pointer suppression across refresh` (8 files). JSON recursive comparison with fffe462c confirms every independent prior performance value unchanged after stripping only two invalid fields; no new benchmark. Post-commit only Task9 plan/spec remain untracked.
