# Task 5/6 integration pointers

Use the exact canonical contract Task3 produces. Plan-only sentence superseded by user 진행; no production operations.

Existing reusable navigation UI is src/hooks/use-draft-navigation.tsx. It wraps useUnsavedNavigationGuard, returns requestNavigation, requestLocalAction, confirmation. requestLocalAction preserves history for same-page preset switching/editor close. Use it for dirty preset switch and route leaving; avoid a second custom dialog/history interception implementation. Preserve focus restoration and standard Escape behavior. AuthProvider/useAuth is src/providers/auth-provider.tsx; academic-read-service.js createAcademicExecutionContext/selectAcademicScopedValue is the existing actor-scope pattern. Verify actual current exports before wiring.

There is a pre-UI browser baseline note at browser-baseline-notes.md with running synthetic Next/fixture sessions and viewport/theme. This is baseline only, not new-feature evidence.

Transfer response integration needs enough minimal operating class labels alongside addedShadowSlots for immediate canonical update; Task3 snapshot adds shadowClasses and Task7 must carry matching display metadata or return an authoritative complete source snapshot atomically. Do not clear/hide slots pending a separate metadata fetch. No student/history fields.

## Sanitized change signals across Tasks3/4/5

No existing reusable realtime/pg_notify change channel was found in affected repository sources. Spec requires access-limited ID/revision-only signals, so subscribing directly to full plan/classes rows would carry unnecessary names or operational metadata. Controller ruling: add one narrow public.timetable_invalidation_signals table in Task3, containing only signal identity, optional plan_id, change_sequence and timestamp (no class names/subjects/students/rosters). A per-plan signal uses the same plan read permission; an operational invalidation singleton is readable only to authorized timetable roles. Authenticated SELECT only with RLS, no direct client writes; private emitter/trigger owns changes. Ordinary plans/items still use the six specified storage/history tables; this extra table is only an invalidation transport.

Task3 owns schema/RLS/minimal typed event contract and per-plan emission when change_sequence changes. Task4 connects the operational signal to all relevant guarded operating/resource changes. Task5 subscribes to INSERT/UPDATE for the selected plan and operating singleton, re-fetches authorized snapshots, and keeps20second visible-only revision/fingerprint polling and focus refresh. No event payload data is trusted as the new schedule itself. Do not publish full public.classes or timetable_plan_items records. Avoid DELETE event reliance; plans are archived, signal rows can persist.

Actual isolated DB has supabase_realtime publication (puballtables=false), wal_level=replica, and no Realtime server/network. Migration can add only this table to existing publication. Verify DB signal writes/RLS/ACL and client subscription behavior locally; do not claim delivered websocket events/provider runtime from this fixture. Poll/focus fallback can be exercised against local DB transport. Production websocket verification stays a separately identified rollout gate.

Primary documentation checked2026-09-23: https://supabase.com/docs/guides/realtime/postgres-changes and https://supabase.com/docs/guides/realtime/authorization (publication setup and RLS-controlled Postgres Changes). This specific minimal-signal table is our implementation choice, not a vendor-prescribed schema.

Read task-3-report.md and canonical timetable-plan-contract.ts for actual SQL signatures. Item mutations use {p_command:{operation:save|delete,...}}; plan mutations use {p_command:PlanCommand}; list/get/revision are typed in TimetablePlanRpcContract. listShareCandidates is management-only. Applied excluded from snapshot.slots, tombstones have isMissing/isVisible. list p_archived=true includes draft+archived (not archived-only). Task4 may extend dated occupancy fields; read its final report too.

Task5 service previewTransfer/commitTransfer precedes Task7 backend: introduce the Task7 canonical transfer contract types now as needed (read task-7-brief.md interfaces only), no parallel wire model or fake success/stub backend. Actual server implementation and UI activation are Task7. Keep response merge extensible for addedShadowClasses or a complete authorized snapshot so labels appear atomically.

Recovery wording ruling: spec requested sessionStorage but calls it browser-exit recovery. MDN https://developer.mozilla.org/en-US/docs/Web/API/Window/sessionStorage confirms same-tab reload/restore survives, tab/window close ends session. Keep actor+plan sessionStorage and existing beforeunload/navigation guard, but accurately claim reload/same-session recovery only; server-saved presets persist across browser restarts. Do not silently add durable localStorage for sensitive drafts or promise fresh-session unsaved recovery. Handle Storage SecurityError/quota failures without marking server save complete; current useDraftNavigation already delegates beforeunload to useUnsavedNavigationGuard. Task9 correct spec/runbook wording with the evidence boundary.

Confirmed range semantics (root/Task4): preset always reserves live WEEKLY shadow positions; optional review period adds actual dated occupancy checks and concrete dates, never frees weekly recurring positions merely because a dated session is skipped/moved. Source-slot/date effective-session authority and dedup/skipped exclusion apply to actual DATED operating writes. Target period is both valid start/end or neither; partial dates are22023 invalid. Reflect in snapshot/service/UI/recommendations/transfer tests and final spec wording. This is consistent with continuing-class shadows and no date-driven operating validity/autoactivation.

Task4 shared-lock guard may return25001/timetable_isolation_not_supported for custom non-RC transactions. Normal PostgREST RC flow is unchanged; preserve error semantics/no false success, leave genuine40001 distinct. Current final Task4 report is authority for this contract.

Task4 handoff clarification: TimetableOperatingReference adds Asia/Seoul asOfDate, datedSessions, datedUnresolvedOccupancies, datedFingerprint and datedComplete. Actual final Task4 report/source remains authoritative. shadowFingerprint is combined from weekly/catalog fingerprint + asOfDate + dated session/blocker/raw occupancy fingerprint, so existing PlanRevision.shadowFingerprint polling catches dated-only changes and KST day changes even when a signal was missed. Do not create a separate parallel revision scheme. findOperatingConflicts and operatingReferenceComplete are canonical dated-aware pure helpers; use them for period-aware placement/recommendations and preserve weekly shadows always.

Task4 is committed e1240a77; read task-4-report.md interfaces and Task5/6 dated contract before implementation. New active unresolved occupancy rejects; presets may still display existing conflicting drafts. Distinguish permission/access failure (clear scoped sensitive state/recovery) from transient reference failure (keep prior display, mark unverifiable, block placement/transfer). A late response from a prior actor/plan epoch must not reopen old data. Undo remains a new revision-checked inverse command; no custom keyboard shortcut. Task7 transfer service must use the server response as one canonical state merge (including labels), never synthesize success before backend exists.

Controller test emphasis from accepted persistence contract: after an ambiguous save result, retry the original requestKey AND immutable submitted body while retaining a newer queued user draft. A refresh/signal response must not overwrite unsaved local intent; server snapshot and local draft remain separately inspectable on stale. Receipt replay can be older than a later snapshot, so do not lower already-known revisions/changeSequence or restore stale shadows from it. Same-tab recovery must keep enough pending command identity for safe retry, not only pretty form values. Malformed RPC response is an error/unverifiable reference, never a synthetic saved/empty result. These are consequences of the existing requestKey/epoch/recovery requirements, not a new offline subsystem.
