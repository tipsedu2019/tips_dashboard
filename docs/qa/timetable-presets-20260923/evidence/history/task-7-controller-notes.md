# Task7 integration rulings

Read Task3/4/5 final reports and canonical contract, not assumed RPC signatures. Shared lock helper is dashboard_private.lock_timetable_operating_resources_v1(), exact hashtextextended(tips:timetable:operational,0) bigint. Do not introduce a second key/helper. Transfer private storage already exists fromTask3.

Whole-plan clone (Task3) preserves drafts/current conflicts with newIDs and makes applied items unplaced drafts. Selected plan-to-plan transfer is distinct: validate destination, and allow keep_pending only for explicit plan-target copy/import. Root ruling: pending/zero-slot source rejection applies to operating promotion and all moves; plan-target copy with keep_pending may preserve unresolved/unplaced source drafts and all pending details without loss. Reject mode retains strict validation.

Task5 may introduce canonical transfer types/service calls early; reuse/extend rather than duplicate. TransferResult must include minimal added shadow-class labels or a complete authoritative source snapshot along with applied/removed/new shadows in one result/update. Do not show unlabeled shadows awaiting a second metadata fetch, or duplicate snapshot slots in grid/conflict input.

Confirmed range semantics (root/Task4): preset always reserves live WEEKLY shadow positions; optional review period adds actual dated occupancy checks and concrete dates, never frees weekly recurring positions merely because a dated session is skipped/moved. Source-slot/date effective-session authority and dedup/skipped exclusion apply to actual DATED operating writes. Target period is both valid start/end or neither; partial dates are22023 invalid. Reflect in snapshot/service/UI/recommendations/transfer tests and final spec wording. This is consistent with continuing-class shadows and no date-driven operating validity/autoactivation.

Transfer receipt ordering check at integration: a replayed full source snapshot can have the same plan changeSequence as a newer already-read snapshot while its operating shadow fingerprint is older (only operating data changed). applyTransfer must not replace newer authoritative shadows/dated reference with that equal-sequence old receipt or falsely mark it verified. Use a coherent response/refresh ordering strategy and add actual transfer service/controller regression alongside immutable receipt replay. Different plan-only copy leaves source data unchanged, so source sequence equality is not proof that a receipt reference is latest. This is a concrete cross-task canonical merge risk to verify when Task7 enables real transfers.

Task6 committed bddfcdc4; read task-6-report.md Task7 handoff. TimetablePlanWorkspace owns selectedItemIds/setSelectedItemIds and class-list controlled props, with toolbar plan-selection-count extension. Use item IDs, not filtered blocks/slot IDs. Controller has explicit narrow failureKind/replaceRejected/discardRejected for definitive item-save rejection; unknown responses remain immutable receipt retries. Transfer commands need their own explicit commit outcome/retry handling. Plan picker owns its own list cache; target-list changes should expose a reload callback/nonce. Source canonical applyTransfer receives a full snapshot or complete delta/reference; don't synthesize labels.

Local runtime at Task6 end: Next3261 session94061; proxy/API3260/3262 session90139. scripts/qa/timetable-plan-rpc-bridge.mjs is allowlisted typedJSON→actual network-none Docker tips_timetable_20260923. Entrypoint http://127.0.0.1:3260/__fixture, synthetic actor00000000-0000-4000-8000-000000000099. Root manual main fixture item ae260000-0000-4000-8000-000000000501 revision8 (Mon09:10–10:10, Tue09–10, Wed17:13–18:13), extra Fri/Sat23:30–24:00 item; keep these for comparison unless separate fixture reset is coordinated. Task7 owns synthetic DB fixture writes after dispatch, root readonly unless coordinated. Create separate transfer QA plans/classes; no production environment files or fallback endpoints. Actual UI browser root uses CUA own tab2, automated isolated scripts use separate contexts. Coordinate proxy replacement with root to release90139 before restarting if needed. Do not rerun/install Playwright CLI; repo QA script pattern already works with bundled runtime.

Concrete mounted-operational refresh integration: timetable-workspace.tsx219–221 keeps OperationalTimetableWorkspace mounted while showing a plan. useAcademicWorkspaceData loadRange depends on request/service and exposes refresh; it has no operating-signal subscription. A promotion must trigger narrow operational refresh/invalidation so switching to 운영 시간표 does not show cachedprepromotionclasses. Preserve filters/preferences, and report committed/save-complete separately from failedfollow-uprefresh. Never issuea newpromotionkey because onlycache/refetch failed. Root sent this namedrisk toTask7implementer.
