# Task 4 focused source inventory (base eb23d7d8)

- management-service.js: createClass 1669 uses create_class_with_group_memberships_v1; updateClass 1690 uses upsertClassRows unless closed; normalized metadata strips teacher/schedule/room, but status still needs gateway. Closed path metadata upsert then close_class_atomic. Preserve metadata-only behavior.
- management-service.js: saveClassScheduleDefaults 1725 and initializeClassSchedule 1747 use existing RPC and public cache invalidation.
- operations/class-schedule-workspace.tsx: handleSaveLessonPlan 3628 directly writes classes.schedule_plan 3660 for legacy storage. This is a real dated schedule write, not just metadata; needs shared conflict gateway and same lock order.
- ops-task-service.ts: checked direct .from(classes).update sites 3375,3393,3850,3896,3922,3951: roster or textbook only; do not add schedule lock to these solely because they write classes. Recheck actual makeup APIs independently.
- 20260716192000_notification_makeup_adapter.sql contains schedule_plan writes around 2042 and 2111. Determine final definition across ordered chain before changing.
- Continuous normalized private require_continuous_class_schedule_mutation_v1 obtains class row; acquire shared advisory lock before it in all schedule entrypoints. Content-only editing can remain separate only if no schedule change.
- Task 3 ruling: private operating lock + shadow/catalog reader must exist before plan mutation is enabled. Task 4 adds the public reference endpoint and connects all real operating writes.

No production data was read or changed by this inventory. Refer to the full approved spec for behavior; these are source pointers, not completed implementation.

Task 1 report lists eight remaining final class_schedule_stale/40001 producers. Convert these domain stale states to P0001 while integrating the entrypoints, including preview/content/backfill/verify/activate siblings so the public schedule family has a consistent domain contract. Do not rewrite actual serialization errors. Add final-definition runtime pgTAP evidence per touched producer.

Task 1 round 3 discovery: free-text legacy schedule details with comma-containing teacher names are intrinsically ambiguous; normalized-mode timetable reads are being corrected to use exact normalized slot teacher_name/classroom_name under existing RLS. Private operational reference must still prefer normalized IDs/rows, and ambiguous legacy text/catalog matches must emit blockers rather than invent a mapping. Preserve the complete room suffix in existing legacy rendering.

Cross-task requirement: when a plan has targetStartDate/targetEndDate, plan placement validation must include authoritative dated active/exception/makeup occupancy in that range, not just weekly shadows. Task3 provides weekly reader/storage foundation; Task4 must extend its private validation path for dated exceptions together with operating guards. Null target range keeps weekly editing and full future-session checks at promotion. No false complete=true on unresolved dated data.

## Sanitized change signals across Tasks3/4/5

No existing reusable realtime/pg_notify change channel was found in affected repository sources. Spec requires access-limited ID/revision-only signals, so subscribing directly to full plan/classes rows would carry unnecessary names or operational metadata. Controller ruling: add one narrow public.timetable_invalidation_signals table in Task3, containing only signal identity, optional plan_id, change_sequence and timestamp (no class names/subjects/students/rosters). A per-plan signal uses the same plan read permission; an operational invalidation singleton is readable only to authorized timetable roles. Authenticated SELECT only with RLS, no direct client writes; private emitter/trigger owns changes. Ordinary plans/items still use the six specified storage/history tables; this extra table is only an invalidation transport.

Task3 owns schema/RLS/minimal typed event contract and per-plan emission when change_sequence changes. Task4 connects the operational signal to all relevant guarded operating/resource changes. Task5 subscribes to INSERT/UPDATE for the selected plan and operating singleton, re-fetches authorized snapshots, and keeps20second visible-only revision/fingerprint polling and focus refresh. No event payload data is trusted as the new schedule itself. Do not publish full public.classes or timetable_plan_items records. Avoid DELETE event reliance; plans are archived, signal rows can persist.

Actual isolated DB has supabase_realtime publication (puballtables=false), wal_level=replica, and no Realtime server/network. Migration can add only this table to existing publication. Verify DB signal writes/RLS/ACL and client subscription behavior locally; do not claim delivered websocket events/provider runtime from this fixture. Poll/focus fallback can be exercised against local DB transport. Production websocket verification stays a separately identified rollout gate.

Primary documentation checked2026-09-23: https://supabase.com/docs/guides/realtime/postgres-changes and https://supabase.com/docs/guides/realtime/authorization (publication setup and RLS-controlled Postgres Changes). This specific minimal-signal table is our implementation choice, not a vendor-prescribed schema.

## Lock-order edge cases from read-only source audit

Final definitions must be verified across ordered migrations. In 20260728233510, save_class_lesson_content_v1 obtains the class lock through require_continuous_class_schedule_mutation_v1 and still writes classes.schedule_plan after a textbook-only patch. A broad BEFORE UPDATE OF schedule_plan statement-level advisory lock could then acquire global lock after class row, causing an inversion. Assess whether this path can safely avoid occupancy validation or must obtain global lock at entry; do not indiscriminately lock unrelated textbook writes. save_class_lesson_session_v1 also locks class then session before projecting schedule_plan.

In 20260729013858, notification_apply_makeup_calendar_effects_v1 routes legacy and normalized helpers; apply_normalized_makeup_effect_v1/revert lock request/session rows. Outer makeup entrypoints may already lock requests before calling these helpers. Inspect final outer callers: shared global lock must precede any relevant row lock, not merely be added inside a helper.

Actual isolated final catalog inventory confirmed public.transition_makeup_request_v2 wraps dashboard_private.transition_makeup_request_v2_unguarded (wrapper migration20260721131903); the wrapper calls assert_assistant_makeup_action_v1 before the unguarded function. Preserve assistant permission behavior while placing shared lock before inner row locks. Also inspect create_makeup_request_v2/delete_makeup_request_v2 for any calendar effect/row-lock path rather than assuming transition is the only entry.

public.deactivate_class_schedule_storage_v1 final definition20260728233510 locks class before switching normalized→shadow. Storage-mode changes alter authoritative occupancy/fingerprint and need same lock-order assessment and operating invalidation; no automatic cutover activation/deactivation is authorized by the planner. Public backfill/activate/verify boundaries likewise remain explicit.

## Final Task3 interfaces

Read task-3-report.md and timetable-plan-contract.ts for final RPC/type signatures. Task3 helper name is dashboard_private.lock_timetable_operating_resources_v1(); use it consistently instead of inventing a second operational-named helper. Root required its body to use the exact hashtextextended(tips:timetable:operational,0) bigint key from Task4 brief; verify final committed definition before work. Private read_timetable_operating_reference_v1()/validate_timetable_item_slots_v1() are the actual foundations; Task4 adds dated occupancy and all operating writers. Task3 source migration is20260923081138_timetable_plan_storage.sql.

Dated occupancy must reach consumers coherently, not exist only as a hidden SQL rejection. Current canonical TimetableOperatingReference/PlanSnapshot and findConflicts are weekly shadow-based. When implementing period-aware actual sessions and per-date source-slot authority/deduplication, define the minimal additive canonical dated reference/fingerprint/blocker fields needed by Task5/6/7. Preserve weekly editing with null period and full future promotion checks; do not falsely reserve a skipped effective session or silently drop exception/makeup occupancy. Report exact semantics and consumer requirements so UI recommendations and conflict messages agree with server. Existing Task2 pure functions may need focused integration extension; keep stable IDs and no names-as-identity.

Incremental conflict baseline risk: compare occupancy coordinate/resource/effective-date values, not merely stable conflict-pair IDs. Same two existing conflicted slots with one moved to another still-overlapping position is NEW occupancy and must reject. Required distinction: unchanged conflict metadata-only accepted; move into another overlap rejected; safe repair accepted; unrelated safe operating mutation accepted. Root sent this concrete test boundary to Task4 implementer.

Confirmed range semantics (root/Task4): preset always reserves live WEEKLY shadow positions; optional review period adds actual dated occupancy checks and concrete dates, never frees weekly recurring positions merely because a dated session is skipped/moved. Source-slot/date effective-session authority and dedup/skipped exclusion apply to actual DATED operating writes. Target period is both valid start/end or neither; partial dates are22023 invalid. Reflect in snapshot/service/UI/recommendations/transfer tests and final spec wording. This is consistent with continuing-class shadows and no date-driven operating validity/autoactivation.
