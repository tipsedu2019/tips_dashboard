# Task 9 integration evidence and release checks

- Base commit eb23d7d8e1e629ffcf58d0f286aafa1d33deec72; branch codex/timetable-presets-20260923; original worktree dirty design docs untouched.
- Node baseline 23/23 recorded in baseline.log. Read task reports for new evidence; do not conflate local DB/fixture browser/production.
- Supabase baseline replay uses supabase/test-baselines/dashboard-free-tier-v1.sql plus documented schema repair, default-period fixture, notification fixture, three legacy content migrations, then 105 ordered manifest migrations. See bootstrap-db.py and db-bootstrap.log for PostgreSQL-only image prerequisites (auth.jwt, auth.users fields, storage tables). A reused full Supabase local stack supplies these components differently.
- Existing repository runner verifies new migrations against supabase/test-baselines/dashboard-free-tier-v1.manifest.json. Append all feature migration entries with actual SHA256 and final status only after testing. Preserve existing final entries and baseline capture/hash metadata. New feature migrations must not silently bypass manifest checks.
- Runtime container tips_timetable_20260923 has --network none and no host ports; image public.ecr.aws/supabase/postgres:17.6.1.159. Check existence/current state before assuming it remains available.
- Existing visual reference docs/qa/timetable-cells-refine-20260922/README.md records scripts/qa/timetable-annual-design-fixture-server.mjs and viewport 1440x960, 390x844. Use the corresponding dataset when comparing appearance; timetable-fixture-server.mjs has different simplified data.
- Shared design CI command is in .github/workflows/free-tier-guardrails.yml, step Verify shared dashboard design contracts. Run its actual list from current branch.
- No production DB migration, push, deploy or real promotion is authorized by this turn. Complete local implementation and evidence, prepare release steps.

- For new browser persistence flows, prefer an explicit local synthetic-auth transport that invokes the real isolated DB RPCs through an allowlisted adapter, instead of reimplementing server validation inside an in-memory fixture. Preserve the existing read-only visual fixture for baseline comparison. If browser fixtures are independent of DB, report that boundary explicitly; UI screenshots alone are not DB save evidence. No production URL/token fallback is permitted in the fixture transport.

## Historical planner payload audit (read-only git history)

Verified source: git show 0d5f25e8^:src/components/NextSemesterPlannerView.jsx. getPlannerKey at269 returns planner:term:<termKey-or-workspace>:<subject-or-all>:<surface>. Surface values teacher-weekly,classroom-weekly,daily-teacher,daily-classroom (lines24–29). Historical payload saved by dataService.setAppPreference at565+: {entries:serializeDraftMap(draftEntries), selectedClassId, period:periodInput}; entries is an object keyed by oldclass/draft key, not an array. serializeDraftMap lines226–252 stores classId,className,subject,teacher,classroom,period,status,studentIds,isNew,scheduleLines. Each scheduleLine carries id,day,start,end,teacher,classroom (lines175–192). Hydration merged base classes and saved entries and swallowed save errors; do not reuse that architecture.

Crucial importer boundary: old payload may carry studentIds and status/source class IDs. Read/import must whitelist class basics and scheduleLines, drop student/history data, create new plan item/slot UUIDs, and record sourceClassId only through server-controlled import when a source exists. Never restore old status onto an actual class or copy studentIds. Preserve original preference value unchanged. Distinct subject/surface keys are independent recovery candidates, no implicit merge. A missing old preference is not evidence of recovered data. Root inspected source only, no production preference rows.

This is an exact old format pointer for parser fixtures; current installed app src has no app_preferences/getAppPreference use found by focused rg. Determine actual DB table/role scope before reading legacy preference rows in implementation; do not infer global visibility from removed client code.

Actual isolated final schema metadata checked (no preference rows read): public.app_preferences has global key text primary key, value jsonb, created_at/updated_at and no actor/plan owner column. RLS enabled but existing authenticated_select=true and authenticated_write=true policies plus staff_write coexist; authenticated has SELECT. Do not infer these legacy values are per-user private, and do not expand planner teacher access to raw preference payloads/studentIds. Use a management-only, planner-prefix-scoped safe recovery read/import boundary with whitelisted fields. Preserve original rows unchanged; broader historic preference ACL remediation is not this feature task. Report source-only/local-schema evidence separately from any actual production recovery.

Task9 acceptance includes implementing missing preparation-class and legacy-preference import flows, not only documenting them. Root ruling permits the necessary CLI-created migration, canonical RPC/service/types, parser/tests and picker/import UI beyond the illustrative docs file list. Reuse Task7 keep_pending import mechanics where available; server controls provenance and whitelists allowed metadata. Existing active shadow→new draft copies basics only; applied→new draft carries original slots as pending. Do not overwrite running classes or old preferences.

Recovery wording ruling: spec requested sessionStorage but calls it browser-exit recovery. MDN https://developer.mozilla.org/en-US/docs/Web/API/Window/sessionStorage confirms same-tab reload/restore survives, tab/window close ends session. Keep actor+plan sessionStorage and existing beforeunload/navigation guard, but accurately claim reload/same-session recovery only; server-saved presets persist across browser restarts. Do not silently add durable localStorage for sensitive drafts or promise fresh-session unsaved recovery. Handle Storage SecurityError/quota failures without marking server save complete; current useDraftNavigation already delegates beforeunload to useUnsavedNavigationGuard. Task9 correct spec/runbook wording with the evidence boundary.

Two-browser-session QA: two same-origin tabs share Supabase localStorage auth, so they do not prove independent actor sessions. A local fixture bridge can expose two explicit loopback proxy origins (e.g. different local ports) sharing the same isolated DB API, with synthetic per-origin auth tokens and a strict known-origin CORS allowlist. This is an option for CUA in-app browser when separate browser contexts are unavailable; verify actual distinct actor IDs and keep them isolated. Reuse installed browsers/tools, no production auth fallback. Test revoked-share/actor-switch/cache cleanup with those independent sessions.

Confirmed range semantics (root/Task4): preset always reserves live WEEKLY shadow positions; optional review period adds actual dated occupancy checks and concrete dates, never frees weekly recurring positions merely because a dated session is skipped/moved. Source-slot/date effective-session authority and dedup/skipped exclusion apply to actual DATED operating writes. Target period is both valid start/end or neither; partial dates are22023 invalid. Reflect in snapshot/service/UI/recommendations/transfer tests and final spec wording. This is consistent with continuing-class shadows and no date-driven operating validity/autoactivation.

Current baseline synthetic transport inspected: scripts/qa/timetable-annual-design-fixture-server.mjs has api port3262 CORS hardcoded to3260, proxy3260→Next3261, fixture actor00000000-0000-4000-8000-000000000099, __fixture writes sb-127-auth-token per origin, data call fallback501 and no forwarding. For two independent authenticated browser actors, support a second explicit loopback proxy origin with distinct fixture tokens and allow only those origins in CORS. Current fixture row IDs a/b/c are display-only; actual isolated DB browser fixture needs valid UUIDs while retaining equivalent visible names/time/theme for baseline comparisons. Keep annual board route support or use a separate scoped DB adapter; never forward unsupported data paths to production.

QA artifact durability: final SDD cleanup deletes this plan-specific ignored scratch directory. Before that, preserve essential execution evidence in docs/qa/timetable-presets-20260923 (or committed reproducible scripts): final focused SQL/Node/type/build summaries with actual commands and relevant rawlogs, clean-chain summary/hashes/runtime constraints, final function/ACL evidence, browser proof and race outcomes. Do not leave final REPORT hyperlinks pointing only at .superpowers or /tmp files that will be removed. Tests/spec source are the primary reproducible contract; retain honest disclosures for missing initial Task1 rawRED, later retained RED/GREEN, local-only Realtime and separate production gates. Root will collect every Ruling line for user-visible final disclosure; preserve a dated decision appendix if useful, without silently dropping superseded decisions from the audit history. Never force-add scratch itself.

Local browser QA runtime discovery (read-only, no new test run): existing scripts/qa/premium-{dashboard,class,settings}-browser.mjs use Playwright through createRequire(PLAYWRIGHT_PACKAGE). docs/qa/premium-dashboard/baseline.md:29 points to /Users/hyunjun/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/package.json. Installed headless Chromium binaries found under /Users/hyunjun/Library/Caches/ms-playwright/chromium_headless_shell-{1234,1223}/chrome-headless-shell-mac-arm64/chrome-headless-shell. Verify package/runtime when used; do not install/change dependencies just for QA. A purpose-built isolated local browser test can use separate browser contexts for two actors (same-origin localStorage is isolated per context), while root manual CUA tabs need distinct origins if sharing context. Existing QA scripts enforce loopback-only requests, block unknown API/external routes, and store screenshots/results; preserve those boundaries. Test against actual allowed isolated-DB RPC adapter for save/collaboration proof, not a duplicate in-memory planner backend. Coordinate ports/browser ownership with root and read relevant Playwright skill before applying it.

Build evidence boundary: worktree uses synthetic loopback Supabase configuration for local runtime. A release-mode build may use explicit safe fixture/public placeholders only if required by existing build contract; record the exact nonsecret environment mode and distinguish compilation from production environment/deployment verification. Do not read/copy production secret env files merely to make a local build pass. Do not claim main CI ran because the same local command passed.

Task6 scoped rereview observation to resolve/report in final recovery acceptance: picker metadata/create/clone/share unknown commands currently persist only in the mounted picker memory, unlike item-controller actor/plan sessionStorage. Same-session dialog close/reopen and input changes have actual persisted-receipt proof; full-page reload/remount metadata recovery is not implemented/proven. Task6 brief did not explicitly require metadata reload recovery, so scoped reviewer left this nonblocking; final review must relate it honestly to specsection7 and avoid claiming every unsaved form/metadata request survives reload. Evaluate actual task9 draft recovery scope and either implement a narrowly required missing recovery boundary or document the exact limit, without weakening item-draft recovery or silently adding durable localStorage.

Documentation alignment after Task7: initial spec RPC table uses preview_timetable_transfer_v1 / commit_timetable_transfer_v1, while the canonical service/types and Task7 implementation use preview_timetable_plan_transfer_v1 / commit_timetable_plan_transfer_v1. Update docs to actual reviewed final API names rather than introducing duplicate aliases. Also carry Task6 final reviewed wholeitem/slot/add editing labels, exact storage-recovery boundary, and Task7 actual post-commit invalidation behavior into the runbook/QA map.

Task7 integration additionally supplies readable pending-placement labels and an explicit pending-row action into the existing add-placement form, with atomic selected-pending removal plus new confirmed slot and preserved siblings. Reuse the reviewed formatter/resolution contracts for import recovery; do not add a second pending editor. Read Task7 final report for concrete function/field names and actual browser cancel/conflict/unknown receipt evidence.

Additional current source pointers (root readonly preflight at36989ac5): active shadow→new draft already exists in timetable-plan-workspace.tsx:243, label '이 수업으로 새 초안 만들기'. It fills basics and empty slots via existing editor. Verify its actual zero-placement/source/history/focus behavior in Task9 rather than building a duplicate action. Preparation/legacy import remains absent and required. Capability testing must exercise missing RPC and failed shadow/reference after a previously successful load, preserving operating view and refusing new planner saves/promotions; don't infer safe-disabled from a displayed error alone. Existing picker reads every numbered page; keep its full list and actor guards.

Manifest runner exact source is scripts/run-isolated-supabase-db-tests.mjs:293(validateManifestMigrations),315(exact ordered manifest/pending list),318(hashes),533(entry). It has a non-executing plan/validation mode; --execute would start its own full Supabase runtime, so don't invoke that implicitly for the existing network-none fixture. Preserve captured baseline metadata/hashes and append all feature migration hashes. Current CI shared design command at .github/workflows/free-tier-guardrails.yml:50–61 includes admin-shell, sidebar-brand, premium-semantic-contrast, common-controls-ui, data-table-surface, data-table-selection-actions, data-table-pagination, data-table-search-field, workspace-tabs tests. Run current actual source command at your HEAD.

Final recovery acceptance ruling (root): the previously recorded picker metadata-reload limitation should be implemented narrowly, not merely described away. Persist only submitted uncertain metadata commands (create/clone/rename/share/archive/restore) with original immutable body/key and separately retained follow-up fields in actor-scoped sessionStorage. Reload/remount must first offer/perform receipt recovery, never create a second mutation intent. Reuse common actor retirement/logout prefix cleanup and handle storage errors; preserve future field edits after recovering original command. Unsubmitted ordinary form text continues using the existing dirty/navigation guard and need not be broadly autosaved. Keep same-tab/session boundary; no durable localStorage. Add behavioral recovery/actor-switch tests and one actual DB browser lost-create-response→reload→identical-retry→single-preset case. This closes the Task6 final-review observation without changing its prior scoped verdict.

Legacy import trust details for implementation: app_preferences payloads are untrusted globally keyed historical JSON. Whitelist applies to nested pending/raw recovery payloads as well as visible item metadata; never tuck an entire old entry (studentIds/status/other unknown personal fields) inside pending_slots. Preserve only supported schedule-line fields and diagnostic reasons while retaining the untouched original preference row. If a recovery preview/read and import are separate operations, detect a changed source version/fingerprint instead of silently importing a different payload from the one the manager reviewed. Use existing idempotency/current-authority boundaries and server-derived provenance.

Concrete two-actor revocation check: test the picker list as well as the open controller grid/sessionStorage. Current picker reload depends on archived/service/reloadNonce, while an open plan becoming forbidden clears controller-sensitive state separately. Verify a revoked teacher does not retain the revoked plan name/metadata in stale picker list or pending metadata recovery UI, and can still return to permitted operating reads/other shared plans. Fix the narrow invalidation path if the actual test exposes retained revoked data; do not clear another actor's valid active session or overwrite drafts on ordinary stale revisions.

Task8 isolated-bootstrap drift investigation (final proof/result in Task8 report): origin eb23d7d8 already has20260803150000_notification_makeup_content_single_writer.sql:31-39 revoking legacy DML/policy and dropping reconcile_makeup_notification_settings_after_write_v1 on makeup_notification_settings. Baseline SQL42147-42152 still CREATEs that obsolete trigger; notification-settings-prerequisites does not DROP it, and bootstrap-db.py:23 includes only three old content migrations142000/144000/145000 before the105manifest chain, omitting150000. Actual old makeup test55000/notification_makeup_legacy_rule_missing may stem from this mixed fixture metadata; Task8 is proving it with rollback-only exact-boundary comparison. Preserve final newer mirror function definitions; blindly reapplying an older whole migration may downgrade them. Incorporate the proven narrow fixture schema/ACL reconciliation in the clean-chain setup and final metadata inventory, not a product workaround. Do not label failure preexisting solely by comparing preTask8, sinceTask4 is also feature-branch code.

Important correction to the preceding bootstrap observation: Task8 rollback-only exact origin DROP/legacy-write ACL boundary fixed only obsolete-trigger metadata assertions (adapter13/writer5). Remaining55000 legacy_rule_missing/import_incomplete and oldwriter permission failures are not yet explained by that trigger. Do not present one trigger repair as making both historical whole suites green. Task8 report must retain exact stack/unchanged-origin source and missing fixture conditions, with complete-suite verification limit separate from25/25 final public/domain focused assertions. Do not expand this feature into speculative notification-controlplane migration/seed repair. Final REPORT must preserve whichever boundaries the completed investigation proves.

Final UI observation from root actual artifact inspection: task8-offscreen-export.png captures the entire09–24axis/100slots and restores offscreen rendering, but some two-lane30minute blocks partially clip their second title line (10:30/12:00/13:30rows, e.g. 성능수업180). Include this in the planned long-name/export UI check: use the existing design/grid typography and ensure deliberate readable truncation or sufficient line height, rather than half-visible text. This is a Task8 deferred Minor/final UI observation, not evidence that the full export is missing rows.

Task8 post-agent runtime check by root: escalated lsof found no listeners on3260–3262 after agent completion; reported38162/41198 are not live handoff processes. Docker existingtips_timetable_20260923 is running/networknone/ports{} and socketSELECT works PostgreSQL17.6 (image health status unhealthy reflects incomplete full stack, not a failed SQL check). Task9 may start fresh scoped localfixture/runtime, no unrelatedprocess stop. IMPORTANT: old scratch bootstrap-db.py begins dockerstop of existingmanualfixturecontainer and --rm reuse. DO NOT execute it as-is. For finalcleanreplay create a separate uniquely named networknone/no-hostports synthetic container (e.g. tips_timetable_20260923_replay after verifying absent), preserve originalmanualDB and record exact prerequisites plus proven metadata reconciliation. Do not reset/remove an existing container or claim oldwhole notification suites pass.

Task8fixround1 ed3e7a31 rebuilt safeenvrelease INCLUDING finalexactconsumerpair, soearlierlastbuildpredatesconsumerwarningissuperseded. ActualEscape-heldrefresh/filter/viewcleanup regressionfixed, sevenlifecyclechecks (sixrealmouse+oneboundedsyntheticlistenerreplacement),39Node,tsc/lint/build. AllhistoricalpointerHandlerP95Ms/handlerEndToReactCommitP95Msinterpretationswithdrawn because endpointprecededproducthandler; currentartifactsremoveinvalidfields, validframe/wholegesture/puremodelmetricsunchanged. Preserveclearmeasurementcorrection infinalQA, do notresurrectinvalid0.1ms/commitlagclaimsfromoldrawlogs. Newagentrelease89554mayendwhenagentcompletes; rootproxy14304 remainsownedbyroot. Verifyportsbeforemodechange.

Ruling: Task9 preset-creating import commands must receive the same actor-scoped immutable submitted-request recovery as ordinary create/clone, including source fingerprint and original request key, instead of opening a fresh creation intent after an unknown response and reload. The new import RPC must not recreate the duplicate-preset gap being closed for metadata. Reuse the narrow existing recovery boundary, not broad unsubmitted-form autosave. Cost if wrong: one additive recovery command shape and focused import receipt tests; operating classes and legacy preference originals remain untouched.

Ruling: Legacy recovery must not silently substitute English for a missing subject. Reject malformed missing-subject candidates with an explicit, user-readable metadata error, preserving the source preference; valid historical entries remain importable and unresolved time/resources still use pending. This avoids creating a silently promotable class with invented business metadata. Cost if wrong: a malformed old candidate requires manual reconstruction or a later explicit metadata-repair flow instead of one-click bulk recovery.
