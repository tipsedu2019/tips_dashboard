# Consolidated final fix wave — I1–I6 and M2

Date: 2026-09-24. Base `75585a5f4bbb266af7c9ac38147b3eb4c0a39371`; branch `codex/timetable-presets-20260923`; worktree `/Users/hyunjun/.codex/worktrees/timetable-presets/tips_dashboard`. Sole implementer `/root/review_timetable_ui`, explicitly reassigned from the historical reviewer role. No subagents or parallel implementers. Local implementation and self-review complete; independent scoped re-review remains with root. Final commit SHA is returned in the handoff (this document is included in that commit).

## Changes against the six findings

| Finding | Implemented behavior and proof |
|---|---|
| I1 | `timetable-plan-model.ts` applies local expected-item-revision preflight only before first submission. Submitted body/key reaches the authoritative RPC receipt after the item's revision changes. Existing/new × refresh/reload cases retain newer queued input and settle to one result. Actual release editor existing revision1 and new null revision each commit, lose response, reload/reopen the same preset, retry an identical body/key and settle with exactly one item and expected revision. Existing superseded receipt/current authorization/independent-item checks remain passing. |
| I2 | Controller state exposes pending operation IDs/kinds/names/failure independently of the optimistic draft. `TimetablePlanRecoveryActions` is rendered in workspace and failed delete dialog, so removal cannot remove recovery. Confirmed stale offers latest/reapply-delete; definite rejection offers discard; unknown offers original replay only. `resolveStale` rejects unknown submitted outcomes even after a newer snapshot. Server stale is the exact P0001/message pair; local preflight and superseded receipts mark their known stale explicitly. Actual release stale delete accept and reapply both pass, and an actually committed/lost delete retries after reload despite absent canonical row. Definite rejection UI uses a clearly labeled synthetic exact SQLSTATE transport response; its button restores the canonical item. New rendered tests assert allowed button labels/callbacks for all three kinds. Other queues remain governed by existing preservation tests. |
| I3 | Only exact22023/timetable_capacity joins exact22023/timetable_invalid and23P01/timetable_resource_conflict as definite rollback. Korean message states500items/2000placements and next action. Editor already provides correction/discard for rejected; controller recovery tests exercise both actions. pgTAP asserts the501st item and a2001-slot body both produce exact22023/timetable_capacity; ambiguous code/message pairs, including code-less stale text, remain original-request-only. |
| I4 | Teacher-only detail uses day-specific/class-level room fallback. Classroom-only, full comma pair, explicit empty teacher, explicit empty room, slash-separated historical forms retain intended values. Tests assert parsed teacher/classroom/start/end, not only formatted text. Focused source check confirms management open/edit passes these returned objects directly to `setClassScheduleSlots` at management-page.tsx:2630 and2875. No full management-browser lifecycle claim; the actual parsed-data boundary is covered. |
| I5 | CLI-generated additive migration20260923164328 replaces only private import-source function. Supports en dash, comma/full-width comma/slash and teacher-only/classroom-only details. Safe `originalSchedule` is bounded to2000characters and only preparation schedule text is retained; no arbitrary entry/row/student/status properties. Malformed time keeps an available leading weekday and safe original for preview, committed pending sourceText, and existing repair formatter. A focused self-review regression ensures weekday characters in a teacher name do not invent a weekday. Existing privacy/source/auth/receipt/no-send checks stay intact. |
| I6 | Existing19 lifecycle test cases and every original assertion remain. Exactly the request collector and two payload paths now recognize update_class_operational_v1 and args.p_patch.schedule_plan. No test removed/skipped/weakened. All19 pass within the202test gate; original branch4failures are retained RED and not called preexisting. |
| M2 | Shared candidate text uses normal break-words and a nowrap date span. Actual390×844 dark screenshot with long subject label shows2026-09-23 unbroken and insideviewport. No global CSS/shared token change. |

## Meaningful RED and latest GREEN

All logs below are under `docs/qa/timetable-presets-20260923/evidence/` unless stated otherwise. Counts overlap and are not summed across gates.

- `final-fix-red-node.log`:74tests,60pass/14fail. Includes4original lesson-plan lifecycle failures,2teacher-room fallback regressions,4explicit revision receipt recovery cases,2capacity recovery cases,2removed-delete recovery descriptor cases.
- `final-fix-red-import.tap`:8new placement preservation assertions fail against pre-fix actual isolated DB;38planned. Existing privacy assertions remained green. `final-fix-red-pending.log`:safe original repair-label assertion fails before formatter fix.
- `final-fix-red-ambiguous-stale.log`:a code-less stale Error could expose stale discard before exact-pair fix. `final-fix-red-day-inference.tap`:one actual SQL failure proves why malformed text must not derive weekday from teacher-name characters.
- Latest `final-fix-regression-node.log`:**202/202**,0failed/0skipped/0cancelled. Command: `$NODE --experimental-strip-types --test tests/timetable-*.node.ts tests/timetable-*.test.mjs tests/class-schedule-slots.test.mjs tests/class-schedule-draft-navigation.test.mjs`. This includes all19original lifecycle tests, controller receipt/authorization/queued input/supersession tests, parser matrix, rendered delete actions, picker/hook/transfer/recovery and pending presentation tests.
- Final clean database `tips_timetable_finalfix_verified_20260924`: baseline+explicit prerequisites+**118 ordered migrations**, then **11SQLfiles /668assertions /0failures /all exits0**. Import39/39, storage74/74, no-send132/132 included. `final-fix-verified-replay-summary.log`, `final-fix-verified-clean-replay.log`, `final-fix-verified-clean-sql-results.json`, per-suite `final-fix-verified-clean-*.tap` retain final results. Command: `python3 scripts/qa/timetable-clean-replay.py --container tips_timetable_finalfix_verified_20260924 --prefix final-fix-verified`, followed by same args to `timetable-clean-sql.py` and `timetable-final-audit.py`.
- `final-fix-manifest-validation.json`:baseline/catalog hashes unchanged;13feature migrations checked in118ordered list. Command: `$NODE scripts/qa/timetable-manifest-check.mjs --output docs/qa/timetable-presets-20260923/evidence/final-fix-manifest-validation.json`.
- `final-fix-sql-provenance.json`:**9critical private functions** exact prosrc equality with their final defining migration, ownerpostgres/search_path/ACL/security-definer assertions pass; no manual40001. Command: `$NODE scripts/qa/verify-timetable-final-sql.mjs --local --output docs/qa/timetable-presets-20260923/evidence/final-fix-sql-provenance.json`. `final-fix-verified-final-definitions-acl.json` separately captures actual final definitions in clean DB. Import-source final definition is164328; public commit-import remains150523. Old085008/storage definitions are not treated as final when later layers replace them.
- `final-fix-browser.json/log`:**7/7 actual release browser acceptance groups**. Existing/new lost item save, stale delete accept/reapply, rejected delete discard, lost committed delete replay, narrow dark candidate date. Command: `$NODE scripts/qa/timetable-final-fix-browser.mjs`. Save/delete response loss is route.fetch actual DB commit followed by synthetic503; originalbody/requestkey/item IDs match replay. Definite rejection transport and candidate enumeration are explicitly synthetic fixtures. Browser creates/archives only uniquely named new QA presets. Prior failed-harness QA presets were archived; originals untouched.
- Screenshots at QA root: `final-fix-delete-accept.png`, `final-fix-delete-reapply.png`, `final-fix-import-mobile-dark.png`. Mobile PNG directly inspected; date nowrap measurement fits390width, long subject wraps normally. Browser contexts closed.
- Full `$NODE node_modules/typescript/bin/tsc --noEmit`:exit0, `final-fix-tsc.log` empty. Touched source/tests/JS QA eslint (exact argv in `final-fix-lint-command.json`):exit0,0warnings/errors, `final-fix-lint.log` empty. An initial2unused mock-prop warnings were fixed before final lint.
- Latest release buildexit0, `final-fix-build.log`. Exact command: `env -i PATH=/Users/hyunjun/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/usr/bin:/bin NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:3262 NEXT_PUBLIC_SUPABASE_ANON_KEY=fixture-only NEXT_TELEMETRY_DISABLED=1 $NODE node_modules/next/dist/bin/next build --webpack`. No environment files/production credentials in this worktree. Build was repeated after exact stale classification changed; prior owned Next was stopped before rebuilding. No concurrent .next writer.

`$NODE` means `/Users/hyunjun/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node`; Docker `/Users/hyunjun/.local/bin/docker`. CLI2.115.0 help was read and `supabase migration new timetable_import_original_placement` created the additive file. Official migration workflow reference: https://supabase.com/docs/guides/deployment/database-migrations . No install/CLI upgrade/reset/link/push was performed.

## Self-review, evidence corrections, and unchanged boundaries

- Re-read changed model/recovery/workspace/parser/import SQL/harness diff and inspected actual parsed-management boundary. Exact-stale ambiguity and malformed-day inference discovered during self-review were fixed and received their own RED/GREEN before final verification.
- An early SQL editing syntax error was transactionally rejected before replacement; corrected before GREEN. Original final-fix clean replay then exercised118migrations; after anchored-weekday refinement a completely fresh verified DB replay used final hashes. Earlier scratch/replay evidence is retained, newest `final-fix-verified-*` is authoritative.
- Browser harness attempt1 incorrectly nested a parent-scoped locator; attempt2 reloaded into fixture's default operating view before reopening the same preset. Both failure logs retained with `harness-attempt1/2` names. Final script explicitly reopens the same preset after reload; stored original request is then recovered through production UI. This fixture selection-reset behavior is not presented as a production selection-persistence test.
- No shared component/token contract changed, so prior63shared-design checks were not redundantly rerun. No operational lock/guard/transfer producer was changed, so prior10actual two-connection races were not rerun. Latest no-send SQL still verifies protected relation content/counts; no provider sends or real operating promotions occurred.
- **M1 retained:** four public_classes_read_failed403 fallback blocks in successful safe build. This is not clean public-read evidence. No public-controlplane/credential change.
- **M3 retained:**500items/2000slots valid framep9533.6–33.7ms still exceeds32ms target under root deferral. No new performance claim; withdrawn handler/commit timing remains withdrawn.
- Root aside17automatic same-revision reference recovery and18expanded applied snapshot detail remain deferred. Unknown requests use same-tab session recovery only; no new session/unsubmitted form autosave promise.
- Production migration/mainCI/deployment/real historical preference presence/real operating promotion/providerRealtime/cache delivery/customer sends remain separate unperformed gates. Historical notification whole suites are not claimed green, and existing audit-delete FK/tombstone proof boundaries remain unchanged.

## Durable records, local data, runtime ownership

`REPORT.md` and operations runbook now describe latest118/13migration counts and recovery actions. `evidence/history` preserves full final-review-1, latestTask9rereview, current progress ledger, regenerated verbatim RULINGS, final-ruling-disclosure-draft, final-fix-brief and this report. Prior raw failure/performance evidence remains.

Original isolated DB `tips_timetable_20260923` received only the additive schema definition and approved rollback SQL tests/newQA presets. `tips_timetable_20260923_replay` was untouched. Three original manual items' revisions/slots were reverified unchanged in `final-fix-verified-manual-preservation.json`. New network-none/no-host-port containers `tips_timetable_finalfix_20260924` and `tips_timetable_finalfix_verified_20260924` are retained; no existing container stopped/reset/removed.

Owned live processes at handoff check: proxy/API PID54689/session31747 on3260/3262; final release Next PID55642/session22273 on3261. Earlier owned NextPID54752 was terminated for rebuild. Root owns no process. Processes may end with agent lifetime; verify lsof before reuse. No production/push/deploy/PR/real operating write/provider action.

No known remaining blocker within the consolidated fix list after self-review. Independent final scoped re-review is still required; this is an implementation handoff, not that independent verdict.
