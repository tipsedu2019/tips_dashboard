# Task 8 approved extensions and evidence boundaries

Read with task-8-brief.md, task-8-controller-notes.md and the final implementer report. This file records scope changes, not a verdict.

- Six final makeup producers must stop manually raising40001 for11 domain conflicts. Stale cases use a domain code and room/calendar collisions a conflict code, preserving messages/signatures/ACL/locks. Verify exact final producers and actual HTTP409/retry/unknown classification consumers; genuine engine40001 remains distinct.
- Actual public normalized approval failed42501 because its effect path lacked continuous-schedule mutation/audit context. Only trusted approve/cancel effect calls may use the existing private context helper, after existing auth/source/room validation, with actual request key/class/operation/reason and allfive caller GUCs restored. Direct writes afterward must still be denied. Public call-path tests must not pre-set the guard GUC to fake success.
- A real7800-session fixture exposed growing-JSONB reader cost and repeated occupancy-key count guard cost. Ordered set-based normalized-session aggregation and per-date key multiplicity aggregation are authorized, preserving every row/status/null/raw/blocker/fingerprint, source/date authority and removed skipped overrides, existing-conflict repair, duplicate-key increases, auth/lock/baseline/finalizer order. Compare old/new fullJSONB and exact guard outcomes/SQLSTATE, finalsource/ACL and actual races. No history truncation or collision pagination.
- Pointer performance may skip only unchanged-versus-unchanged pair computation, with ordered conflict equivalence and all changed-versus-complete-scope comparisons preserved. Rendering changes must follow measured evidence; keep only useful final changes. Visible-panel rendering may defer offscreen browser work but must preserve layout/scroll, live resource data, export completeness, pointer cleanup and allfour projections.
- A real Escape-while-pointer-held regression exposed late physicalrelease clicks opening an editor. The narrow suppression-lifecycle fix belongs in this task and requires no accidental save/form, subsequent gesture success and cleanup on lifecycle changes.
- Measurements distinguish development/release mode, pure projection/validation, actual pointer input/handler/frame, full operating reference/revision payloads and real guardedwrite latency. The frame target remains the brief target; do not accept a changed benchmark that merely removes slow phases.
- Old whole notification makeup suites have isolated baseline/prerequisite failures. Require exact evidence and source/fixture boundaries. Comparing only preTask8 cannot prove pre-existing relative to this feature branch because Task4 also added newguards. An origin obsolete-trigger cleanup explains only some assertions; remaining notification rule/import/ACL failures need their own explanation. Do not count an incomplete/failed whole suite asPASS or assume focused public-path coverage proves unrelated settings flows.
- Task9 still owns preparation/legacy import UI/server boundary, capability fallback, twoactor browser/reload recovery, full finalbuild/design checks, manifest/runbook/durableevidence. Production migration/deployment, mainCI and actualproviderRealtime are separate unexecuted gates.

Root decisions and raw chronology remain in progress.md; this task reviewer should use the concrete constraints above and investigate any named risk rather than redoing the full implementation or futureTask9 scope.

Root performance disposition after measured fixes: final600-slot framep9517.5ms;500/2000 cap33.6ms (wholegesture33.7) versus150ms releasebaseline. The32ms cap target remains unmet. Root chose to preserve this explicit residual observation and proceed to task review after required functional evidence rather than expand another rendering refactor or repeat runs seekingPASS. Cost: occasional two-frame response/startup hitch at maxcapacity. See progress.md ruling and report for exactmeasurements; assess the implementation and evidence independently.
