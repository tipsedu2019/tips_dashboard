**I1 — Committed explicit-revision editor save receipt recovery — ADDRESSED.** `src/features/academic/timetable-plan-model.ts:298-300` now applies the local revision preflight only when `entry.submitted` is absent. The existing immutable command therefore reaches `service.saveItem` at line316 after refresh/reload. Lines378-385 distinguish an unknown submitted outcome from a confirmed stale result, and line438 prevents snapshot changes alone from authorizing abandonment. `tests/timetable-plan-controller.node.ts:67-82` covers existing/new × refresh/reload, exact body/key replay, a later queued edit, its updated revision, and a settled clean queue. The retained release-browser script additionally verifies actual DB commit/response loss/reload/replay for both existing revision1 and new null-revision editor saves, with a single resulting item and expected revision. Current authorization and superseded-receipt behavior remain in the unchanged server path and covering controller tests.

**I2 — Deleted optimistic item loses its recovery controls — ADDRESSED.** `src/features/academic/timetable-plan-model.ts:202-203` derives `pendingOperations` from operation queues rather than `draft.items`; the deleted item's name is preserved at lines187/230/364-365. `src/features/academic/timetable-plan-recovery-actions.tsx:12-18` renders latest/reapply-delete for confirmed stale, discard for confirmed rejection, and only original replay for uncertain results. `src/features/academic/timetable-plan-workspace.tsx:291-296,312-313` uses those controls both outside and inside the deletion dialog and disables duplicate deletion while an operation is pending. The original row's optimistic absence no longer removes its controls. Exact `P0001/timetable_stale` classification and explicit local/superseded markers avoid treating an arbitrary stale-looking transport error as permission to discard. Rendered action tests, controller absent-row/reload tests, and recorded actual-browser stale-delete accept/reapply and committed-lost-delete replay substantiate the fix. The browser definite-rejection case uses a disclosed synthetic exact error pair and verifies restored canonical UI; it is not misrepresented as a real DB rejection.

**I3 — Capacity rollback classified as unknown — ADDRESSED.** `src/features/academic/timetable-plan-model.ts:77-80` recognizes exactly `22023/timetable_capacity` as a definite rejection; `src/features/academic/timetable-plan-interaction.ts:162` provides the 500-item/2000-placement message and corrective next action. Existing editor correction/discard and the new operation recovery controls can now settle the rejected request. Controller tests at `tests/timetable-plan-controller.node.ts:84-88` exercise correction with a new key and discard; the ambiguous-error matrix retains original-only replay for mismatched code/message and code-less stale errors. `supabase/tests/timetable_plan_storage_test.sql:107-108` asserts the exact producer pair for 2001 supplied slots and the 501st item against the active mutation. Final retained storage TAP is 74/74. The two controller cases deliberately mock the same producer pair; actual numerical-limit behavior is established by SQL rather than inferred from their labels.

**I4 — Teacher-only parenthetical becomes a room — ADDRESSED.** `src/features/management/class-schedule-slots.ts:152-154` restores the no-comma teacher-only classroom fallback while keeping explicit comma-empty sides intact. `tests/class-schedule-slots.test.mjs:127-134` checks returned teacher/classroom/exact-time fields for teacher-only, classroom-only, full pair, both explicit empty sides, slash pair, and day-specific fallback. The existing management editor call sites consume that parser output directly. This resolves the demonstrated `(홍길동)`→room `홍길동` regression. Evidence covers the actual parsed-data boundary; it does not claim a newly run complete management browser workflow.

**I5 — Import discards the original placement needed for repair — ADDRESSED.** The additive final migration `supabase/migrations/20260923164328_timetable_import_original_placement.sql:41-56` accepts en dash and supported resource details, retains only the bounded preparation schedule string as `originalSchedule`, and preserves a leading weekday on malformed input. The anchored extraction at line45 avoids inventing a weekday from a teacher-name character. The existing public commit still serializes the sanitized line into pending input; `src/features/academic/timetable-plan-interaction.ts:16-17` now displays that safe original when time is invalid. Final SQL tests at `supabase/tests/timetable_plan_import_test.sql:67-81` cover preview, exact 1033/1123 minute commit, malformed original persistence, weekday 3, and diagnostic; the original privacy/auth/source/receipt checks remain. Final import TAP is 39/39. I independently compared the captured clean-DB final import function body with the164328 migration: exact match, owner postgres, empty search_path, owner-only ACL. Earlier 150523 helper text is not treated as active. Public import authorization/receipt/locking definitions were not replaced by this migration.

**I6 — Existing19 lesson-plan lifecycle tests regress to 15/19 — ADDRESSED.** `tests/class-schedule-draft-navigation.test.mjs:139,227,306` changes only the request collector and two payload paths to `update_class_operational_v1` / `p_patch.schedule_plan`. The supplied fix diff removes no test, skip condition, or lifecycle assertion. All 16 literal case names and the three route-expanded cases are present among the retained 202 passing Node results, including the four formerly failing save/retry/late-actor paths. The previous true-base 19/19 versus branch 15/19 regression remains documented historically; it is not relabeled preexisting.

**M2 — Narrow import dates split inside digits — ADDRESSED.** `src/features/academic/timetable-plan-import-dialog.tsx:94` replaces `break-all` with normal `break-words` and an explicit `whitespace-nowrap` date span. I inspected `docs/qa/timetable-presets-20260923/final-fix-import-mobile-dark.png`: at 390px the long subject wraps and 2026-09-23 remains intact. The recorded browser check asserts nowrap and date bounds x 217.08 / width 78.81 within 390px. Candidate enumeration is explicitly synthetic, which is sufficient for this display-specific finding; no global token or component style was forked.

## New breakage in the fix diff

**None identified at Critical or Important severity.** The scoped source/test/additive-SQL/QA-support diff preserves the original request identity, current-auth boundary, independent queues, accepted stale resolution, source privacy, and explicit empty-resource semantics. A pending operation's name/descriptor is presentation state; it does not become mutation authority. The added migration replaces only the private import-source reader and revokes non-owner execution, without data rewriting or replacement of transfer/operational conflict producers.

## Checks and evidence limits

- **Scope checked:** supplied `review-75585a5f..5a2fe56f.diff`, base `75585a5f4bbb266af7c9ac38147b3eb4c0a39371` → HEAD `5a2fe56fe1b0ac1ce8195bc4b471b2db79ea1fb4`, against `final-review-1.md`, `final-fix-brief.md`, `final-fix-report.md`, scoped rereview rubric and latest root dispositions. Product/test/SQL/QA changes were read in bounded passes; repeated raw replay logs were sampled through their final evidence rather than reread line by line. This is the requested one scoped rereview, not a second whole-branch review.
- **Node evidence checked:** `evidence/final-fix-regression-node.log` reports 202 tests / 202 pass / 0 fail / 0 skip / 0 cancelled. Checked added tests against changed code, the retained RED descriptions, and all 19 original lifecycle names (16 literal + 3 route-expanded). No suite was rerun.
- **SQL evidence checked:** recomputed the final `final-fix-verified-clean-sql-results.json` totals: 11 files, 668 passes / 668 planned, all recorded exits 0 and no failures. Independently counted final import TAP 39/39 and storage TAP 74/74 with their plan lines. The final verified prefix supersedes earlier667-assertion evidence before anchored-weekday refinement. No fresh database access or SQL execution was needed.
- **Final-definition/hash checks:** rehashed 13/13 feature migration files against `final-fix-manifest-validation.json`; the artifact records 118 ordered migrations with unchanged baseline/catalog hashes. Compared the captured clean final import body directly with 164328 and checked owner/ACL/search_path. Read the recorded nine-function provenance result, which retains exact source matches and no manual domain40001. I did not independently rerun the 118-migration replay or nine-function DB checker.
- **Browser evidence checked:** read `scripts/qa/timetable-final-fix-browser.mjs`, `evidence/final-fix-browser.json` and the 7/7 named outcomes. Existing/new saves and delete response-loss cases call `route.fetch()` to commit to the isolated DB before returning synthetic 503; replay body/key and stored item/revision are asserted. Stale deletion uses an actual intervening DB mutation. Definite deletion rejection is synthetic 22023/timetable_invalid, and the long import candidate list is synthetic enumeration. The harness reloads and explicitly reselects the same preset; it does not prove selection persistence. I inspected both the mobile date and stale-delete-reapply PNGs directly. No browser was launched or server changed during rereview.
- **Build/type/lint evidence checked:** the report supplies exact commands and recorded exit 0 for full tsc, touched lint and safe-environment webpack build. Final tsc/lint logs are empty and the final build log retains four public-read403 blocks. Empty logs alone are not treated as independent proof of a new execution; these are recorded implementer checks, not checks I reran.
- **No new focused probe required:** the scoped code paths and retained named regressions answered the prior concrete risks. I did not repeat passed suites, introduce a broad test gate, or expand into unchanged operating features.
- **Mutation boundary checked:** no source/index/HEAD/branch/DB/server/production action in this rereview; the requested `final-rereview-1.md` is the sole write. Local implementation acceptance does not imply push, deployment, main CI, operational migration or real promotion.

## Out-of-scope observations and retained dispositions

**No newly discovered out-of-scope issue.** Original Declined-to-judge items 1–23 were explicitly dispositioned by root; this rereview does not silently reopen them or convert them into completed evidence.

**M1 remains a documented limitation.** Four synthetic `public_classes_read_failed`403 fallback blocks remain in `evidence/final-fix-build.log`; build exit 0 is not verified public API health. Root's explicit Ruling permits retaining the safe fixture boundary without production credentials or a public-control-plane change.

**M3 remains an unmet target under explicit deferral.** The previously measured cap 500 items / 2000 slots frame p95 33.6–33.7 ms still exceeds 32 ms. No new performance measurement was made; withdrawn handler/commit-lag interpretations remain withdrawn. This is not promoted to PASS.

**Aside 17/18 remain explicitly deferred:** same-revision automatic reference recovery and expanded applied-snapshot details. Existing explicit refresh/focus and the stored summary remain the accepted scope. These behaviors were not modified in this fix diff.

**Earlier safety/verification limits remain:** production/mainCI/migration/deployment/real legacy-data presence/real promotion and provider Realtime/cache/send delivery are unperformed; historical notification whole suites are not PASS; the audit-delete tombstone test does not prove real deletion API success; prior bounded pointer/race evidence is not expanded. No operational lock/transfer producer or shared token contract changed, so their previously passed broad gates were not repeated or claimed rerun.

## Verdict

**Fix round: All I1–I6 and M2 findings addressed; no new Critical/Important breakage identified in the fix diff.** No Important finding from the consolidated list remains open. Accept this one scoped fix round at `5a2fe56fe1b0ac1ce8195bc4b471b2db79ea1fb4`, retaining M1/M3 and the root's explicit deferred/release boundaries above.
