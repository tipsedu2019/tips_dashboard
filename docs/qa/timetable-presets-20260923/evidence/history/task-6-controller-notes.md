# Task6 adapter/browser integration

Task2 agreed extra adapter exports: projectTimetableSlot(slot,view) -> {id,panelKey,columnKey,startMinute,endMinute}; resolveMovedGridTarget(slot,origin:GridTarget,target:GridTarget) -> DropTarget. Derive absolute pointer delta from both grids visibleStartMinute/rowPosition/slotMinutes, snap delta5min, then add originalslot.startMinute. Targetview determines resource/day. Confirm actual contract/report after Task2 commits. Freeze time axis through drag lifetime; same coordinates across different resource panels still create a change.

Read browser-baseline-notes.md for already running synthetic Next/fixture servers and existing desktop/mobile comparisons. Existing operational UI remains the design reference. Root uses cua_repl for browser verification. Avoid conflicting with root browser session; coordinate when needed. Existing useDraftNavigation hook supports same-page preset switching via requestLocalAction; details in task-5-controller-notes.md.

Current grid integration risk (read-only checked): legacy-timetable-grid.jsx shell handlePointerScrollStart captures every non-mouse pointer around714 without target filtering, alongside touch handlers. New drag handles must prevent shell pan capture from stealing their pointer while ordinary block/shell touches still scroll. Block editing currently starts via onMouseDown for the whole block, and window mouseup calls commit; pointer cancel/Escape/capture-loss must explicitly cancel the new editor flow rather than accidentally trigger this legacy commit. Existing shared ghost moved check compares only column/start around425 and must include source/target grid identity. Current blockMap starts/active stores one block per position around365 and loses overlaps. Only active source consumer found is timetable-workspace.tsx; preserve its operational readonly path/layout/export and standard tooltip keyboard access.

Operational workspace currently stores selectedTeachers/selectedClassrooms as display-name strings and selectedDays as Korean day labels (timetable-workspace.tsx205+), then filters workspace.rows by those names. Plan targets are stable catalog IDs / numeric weekdays. Keep these namespaces distinct when reusing the filter UI and preserve operational filter state across preset switches. All conflict/recommendation inputs remain complete canonical plan/shadow data; subject/target filters affect display only. Ensure view/panel switching never remounts or resets the canonical edit queue/draft controller.

Confirmed range semantics (root/Task4): preset always reserves live WEEKLY shadow positions; optional review period adds actual dated occupancy checks and concrete dates, never frees weekly recurring positions merely because a dated session is skipped/moved. Source-slot/date effective-session authority and dedup/skipped exclusion apply to actual DATED operating writes. Target period is both valid start/end or neither; partial dates are22023 invalid. Reflect in snapshot/service/UI/recommendations/transfer tests and final spec wording. This is consistent with continuing-class shadows and no date-driven operating validity/autoactivation.

Exact-time form preflight: current src/components/ui/date-time-picker.tsx normalizeTimeInput permits hours0..23 only; TimePickerControl is an option-list picker (10min defaults, preserves an already-selected odd minute but does not provide arbitrary typed input), so passing24:00 as an option silently drops it. Existing operations native Input type=time also cannot represent24:00. Ensure the new placement form supports typed17:13 and end24:00 with shared Input styling and explicit validation; do not silently round/end at23:59, and do not broaden the common datetimepicker semantics without sibling tests/DESIGN justification. This is a concrete existing-component limitation, not authorization to fork common styling.

Task5 interim API (verify final report before use): controller.resolveStale(itemId, accept_server|reapply_draft) explicitly resolves latestserver/localdraft; retry keeps originalkey/body for unknownresponse, reapply creates newkey/currentrevision. Undo is bound to lastlocalsave revision and refuses rebase over collaborator changes. Item-scoped stale must not disable independent safe item saves. Combine controller.dirty with editor form dirty that has not yet been dispatched when wiring useDraftNavigation; otherwise switching away from incomplete form silently loses input.

Fixture integration preference: Tasks3/4 storage/operating APIs now exist in clean network-none tips_timetable_20260923 and pass292 SQL assertions. When adding planner routes to the Task6 fixture server, prefer an explicit allowlisted RPC adapter to that isolated DB over reimplementing save/conflict semantics in mock JS. Only loopback listeners/allowedfixture origins, allowlisted function names + safely quoted typedJSON arguments, SET LOCAL ROLE authenticated + fixtureactor claims per transaction, no unknown-data forwarding/productionURL fallback. Seed synthetic UUID classes/catalogs/actors with equivalent visible baseline labels; preserve annual-board fixture support. Backend transfer RPCs stay absent untilTask7. Coordinate DB seeding/reset and rootbrowser use; immutable original baseline screenshot comparison is separate from realDBsave proof. Task9 still owns final imports/twoactor/recovery/release evidence.

UI contract cross-check for Task6: spec sections3–5 (lines54–164) cover plan selector/create/rename/clone/archive/restore/share, classlist search/all/unplaced/applied, item-ID selection surviving view/filter changes, readonly shadow detail, basic-info unplaced draft from an operating shadow, per-slot unplace versus wholeitem delete, multipleweekday atomic form, resize, unavailablecatalog labels, emptycatalog settings link. Implement Task6 UI/access boundary for those features, leaving actualtransferdialog/commands toTask7 and legacyimports toTask9; do not ship enabled placeholderbuttons. Read these specific spec sections along with brief. Auth service finalTask5 fix adds useTimetablePlanSession ownership; consume useTimetablePlan without creating another controller perview. Root runtimepreflight afterTask5 firstfix: Next3261 andproxy3260 HTTP200; API3262alive(root501 isexpected no route). Read browsernotes for processsessionIDs before replacingfixture.
