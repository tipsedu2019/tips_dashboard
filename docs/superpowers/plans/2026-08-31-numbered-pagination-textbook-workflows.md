# Textbook Workflow Numbered Pagination Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Page every textbook operational record list without changing inventory, settlement, grouping, duplicate checks, draft or export meaning.

**Architecture:** Separate page DTOs from full-filter summaries and explicit detail/mutation/export contexts. Preserve existing mutation functions by supplying complete purpose-specific inputs; never pass a page slice where an existing function expects a complete ledger. Share the reviewed pager/preferences/controller, with domain-specific invoker SQL and strict adapters.

**Tech Stack:** React19, Next16, existing shadcn/ui, Supabase/PostgreSQL, TypeScript, node:test, pgTAP.

**Spec:** `docs/superpowers/specs/2026-08-31-app-wide-numbered-pagination-design.md`

## Global Constraints

- For each new bounded RPC, extend the foundation's explicit numbered-RPC query-budget registry only after strict10/15/20 final-SQL validation and local pgTAP proof; retain timeout/retry/authorization checks. The owning read-model task may update `src/lib/query-surface-budget.js` and its focused tests for that exact contract, never exempt arbitrary list RPCs or weaken unrelated guards. Complete aggregate/export/write-context APIs have separate contracts and must not be misclassified as bounded list reads.

- Page-number blocks contain ten existing numbers: 1–10, 11–20, 21–30. No ellipses or direct page input. Single arrows move one page; double arrows select global first/last.
- Page size10/15/20 minimum10, shared DataTablePagination/useDataTablePageSize/createNumberedPageController, full-filter authorized total, direct target-page reads only. No general full-load fallback.
- Preserve each display-parent unit: purchase student/teacher pairing and sales month/class/textbook aggregation are not raw-row pages.
- Compute balances/quality/facets/summary and count over their complete authorized source scope before page slicing. Restrict raw relation/history DTO enrichment to returned page IDs or explicit detail.
- Preserve current-page selection independently from per-user/per-record drafts. Never delete a draft merely because its record is absent from this page.
- Bind retained rows, summaries, catalogs, details and drafts to resolved actor ID plus role. Disable reads until auth is ready; clear old presentation and invalidate late in-flight/cache writes on logout, user or same-user role change. No client actor value becomes a database authority override.
- Preserve full-filter export and monthly settlement scope, existing write authority and all domain mutation semantics. Do not change formulas, negative-stock policy, notification/send behavior, RLS or authorization boundaries.
- No remote migration, push, deployment or sends. CLI-generated migrations remain candidate until actual isolated SQL proof; no speculative indexes.
- Supplier/publisher settings and other global-order settings are separate from operational pages; on 2026-08-31 the user approved recommended ordering/save API improvements needed for paging. Preserve existing reorder/add and complete ordered publisher-link semantics; do not introduce an arbitrary catalog cap.
- Distinguish unit/parity/SQL/build/browser evidence; do not bypass Browser access policy or deploy a UI without its required RPC capability.

## Required implementation input

Read `docs/superpowers/plans/2026-08-31-textbook-numbered-read-audit.md` completely before coding this domain. It defines exact filter unions, page units, source anchors, existing quantity/cost formulas, lookup precedence, and required detail/export/draft boundaries. Its proposed API signatures are the contract for this plan, not evidence of an implementation.

Implementation clarifications superseding the audit's delivery sketches: use the task-specific split CLI migration names below, not the audit's single illustrative migration; extract `textbook-read-model.ts` as Task1 specifies, not the audit's alternate `.js/.d.ts` sketch. Numbered SQL returns the requested page with matching total and empty rows when out of range; the reviewed common controller alone performs its bounded final-page clamp/reload. Do not silently relabel requested pages inside SQL.

The variable-length editable `textbook_sub_subject_settings` table/mobile cards are in scope, unlike the excluded finite subject configuration matrix. Task7 covers their own draft/global-order/default-merge semantics. The root's source-only preflight is `.superpowers/sdd/2026-08-31-numbered-pagination-textbook-workflows/subsubject-preflight.md`; verify current source before implementation.

## Task 1: Extract executable projection and scope contracts

**Files:**
- Create `src/features/textbooks/textbook-read-types.ts`.
- Create `src/features/textbooks/textbook-read-model.ts`.
- Modify `src/features/textbooks/textbook-operations-workspace.tsx` only to import extracted pure models/types.
- Create `tests/textbook-numbered-model-parity.test.mjs`.
- Modify `tests/textbook-workspace.test.mjs` only to move affected definition-location assertions to the extracted module and verify the real workspace imports/wiring; retain unrelated UI/service safeguards.

**Interfaces:** Export audit-named `MasterFilters`, `PurchaseFilters`, `SaleFilters`, `SaleHistoryFilters`, `InventoryFilters`, `InventoryHistoryFilters`, `ClosingFilters`, `SettingFilters`, `PageRequest<F,S>`. Extract existing `InventoryCountRow`, `InventoryHistoryRow`, `SaleHistorySummaryRow` and purchase/master DTO types without inventing alternate copies. Export pure grouping/filter projections matching current `buildPurchaseDisplayRows`, `buildSaleHistorySummaryRows`, `buildInventoryCountRows`, master quality and inventory filters. Keep UI rendering outside this module.

- [x] Write RED fixture tests using literal expectations: paired student+teacher lines count as one display row; repeated same-copy-scope lines remain separate; sales from two source rows in one month/class/textbook yield one history group; opening/purchase/sale/return quantities preserve existing balance and closing formulas.
- [x] Include cross-page-boundary fixtures (at least101 display parents), inactive/missing-reference textbook resolution, global duplicate title beyond the page, zero/negative/teacher stock, and all-time balances. Freeze purchase pairing using stable `(created_at ASC NULLS LAST,id ASC)` source order before offset selection. This explicitly stabilizes previously unspecified input order; preserve native timestamp precision for that ordering and test equal/missing timestamps and shuffled inputs.
- [x] Extract existing pure functions/types and wire old workspace consumers to them without paging yet. Give `PurchaseCaseRow` stable anchorLineId/memberLineIds plus complete member DTOs. Preserve title/ISBN/barcode lookup exact→normalized→compact precedence and edition separation.
- [x] Run new parity tests and existing textbook ledger/workspace/service regressions; typecheck/lint and commit as `refactor: isolate textbook list projection contracts`. This task must not change data loading or formulas.

## Task 2: Master, inventory and history read models

**Files:**
- Create CLI migration `textbook_inventory_numbered_reads`.
- Create `supabase/tests/textbook_inventory_numbered_reads_test.sql`.
- Create `src/features/textbooks/textbook-read-service.ts`.
- Extend `src/features/textbooks/textbook-read-types.ts` with the actual summary/detail/balance/duplicate transport contracts; reuse Task1's projected row types.
- Create `tests/textbook-numbered-read-service.test.mjs`.
- Append only this task's exact candidate/final entry to `supabase/test-baselines/dashboard-free-tier-v1.manifest.json`; prior final entries remain immutable.
- Modify `src/lib/query-surface-budget.js` and `tests/query-surface-budget.test.mjs` only for the exact new bounded read contracts after final SQL proof, as the global constraint requires.

**Interfaces:** Implement the audit contracts for `listTextbookMasterPage`, `listTextbookInventoryPage`, `listTextbookInventoryHistoryPage`, `getTextbookMasterSummary`, `getTextbookInventorySummary`, `getTextbookMasterDetail`, `getTextbookInventoryBalance`, `checkTextbookMasterDuplicate`. RPC names are corresponding snake_case with `_v1`, page requests use `(p_filters jsonb,p_sort text,p_page integer,p_page_size integer)`. The three page methods return NumberedPage; summary/detail/balance/duplicate methods return their separately typed purpose-specific DTOs from the audit, not artificial numbered-page envelopes. Page projection retains all inventory quantity/value fields and global quality flags specified by the audit.

- [x] Write RED strict transport tests for page11 one RPC, exact filter/sort/page/size, caller AbortSignal, retryfalse, timeout, malformed/missing API failure and no fallback. Write pgTAP fixtures that reproduce Task1 parity expectations and validate matching count/source authorization.
- [x] Generate migration with invoker functions and existing role guards. Derive minimum balance/quality/latest-count aggregates needed for filtering/order before key paging; raw movement history is never part of master page payload. Count the full filtered key set, retain exact total on empty/out-of-range pages, and use stable id/composite tie-breakers.
- [x] Inventory history uses `stock_moves UNION ALL stock_counts` with `(event_at DESC,kind,id)` and retains adjustment_move_id. Do not deduplicate count and linked move implicitly. Inventory shortage means negative or1..3; zero is unused and surplus>=20. Master duplicate quality uses all active titles before list filters.
- [x] Implement strict service response parsers and existing auth/timeout conventions. Preserve existing APIs and no-send/RLS boundaries. Add exact candidate migration hash, run local SQL if available plus service tests/lint/TS, then commit as `feat: add numbered textbook inventory reads`.

## Task 3: Purchase, sale, closing and explicit work contexts

This umbrella is implemented through sequential Tasks3a/3b/3c below, with independent review after each. Do not dispatch all18RPCs as one task. Task3 remains incomplete until all three gates close; its requirements below continue to bind the owning subtask. Tasks4–8 retain their numbering.

**Files:**
- Create CLI migration `textbook_workflow_numbered_reads`.
- Create `supabase/tests/textbook_workflow_numbered_reads_test.sql`.
- Create the separate Task3c CLI migration `textbook_closing_work_context_reads` and `supabase/tests/textbook_closing_work_context_reads_test.sql` for its own final SQL gate.
- Extend `src/features/textbooks/textbook-read-service.ts` and read types.
- Create `src/features/textbooks/textbook-work-context-service.ts`.
- Create `src/features/textbooks/textbook-handoff-model.ts` by extracting the existing pure order/return/MakeEdu group builders and their minimal pure dependency closure. Modify `textbook-operations-workspace.tsx` only for those extraction imports/deletions in this task; operational paging integration remains Task5. Reuse ledger/taxonomy/read-model helpers, with no workspace import cycle or duplicate alternative formula.
- Extend `tests/textbook-numbered-read-service.test.mjs`; create `tests/textbook-work-context.test.mjs`.
- Narrowly update `tests/textbook-workspace.test.mjs` only if existing helper-location guards move with the extraction, preserving executable parity and workspace wiring coverage rather than removing safeguards.
- Append only this task's candidate/final migration entry to the baseline manifest, and extend the exact query-budget contracts/tests only after final SQL proof; prior final hashes and generic guard behavior remain immutable.

**Interfaces:** Implement audit-named purchase/sale/sale-history/closing page methods; authoritative purchase/sale/operations summaries; direct purchase/sale/closing detail; `getClassTextbookSaleContext`; `getTextbookPurchaseHandoff`; `getTextbookBillingHandoff`. Page RPC naming/params match Task2 pattern. Explicit export outputs contain `{groups,sourceLineCount,complete:true}`; failure never returns a partial success.

Purchase detail explicitly separates scopes: `getTextbookPurchaseDetail({anchorLineId,mode},options)` calls `get_textbook_purchase_detail_v1(p_anchor_line_id uuid,p_mode text)`, with `mode: "request" | "order"`. Request mode admits admin/staff/teacher under caller RLS and returns only requested-source detail with complete request-safe members/references, never management aggregates. Order mode admits admin/staff and its management projection. A missing eligible source returns explicit `row:null`; forbidden role remains42501. The real supplied member ID resolves its unfiltered display case even when not the first anchor after pairing; no originating list/page/filter snapshot is required. This supersedes the audit's provisional id-only signature without changing existing mutation authority.

Independent reference-picker/search/selected-reference resolution is the dedicated Task4 prerequisite, not additional unnamed work inside this task. This includes replacing the later lifecycle resolver's full catalog scan and preparing whole-inactive cleanup targets; Task5 must consume these explicit boundaries before removing startup catalogs. Task3 preserves the complete selected work/export contexts named above but does not add ordinary-page/full-bundle fallbacks.

Explicit export/save inputs use one purpose-scoped atomic JSON RPC response without an arbitrary row cap, with eligible rows, their authoritative count and only required referenced projections derived under the same statement snapshot. Reuse the extracted handoff builders and existing closing ledger on that explicitly complete context. Do not introduce a multi-call chunk/snapshot-token protocol in this task. Ordinary numbered pages remain bounded; failure, cancellation, mismatched count/identities or incomplete context must prevent document construction/writes. An explicit large export may fail the existing timeout rather than return a partial success; do not broaden it into a startup full-table bundle.

The existing closing-detail record table is also a page boundary: add `ClosingMovementFilters` with `{closingMonth,subject,search}` and `listTextbookClosingMovementPage(PageRequest<ClosingMovementFilters,"event-desc">,options)` using `list_textbook_closing_movement_page_v1` and the same four page arguments. Its row is the existing `{id,at,typeLabel,textbookTitle,locationName,quantity,amount,marginAmount}` projection, sorted by event descending then stable real movement ID. Add `getTextbookClosingPreview({closingMonth,subject,openingQuantity,openingAmount},options)` for the existing complete preview calculation plus unfiltered movement count, without raw movement arrays. Direct saved closing detail similarly returns the selected stored row and complete current aggregate/count (explicit missing row), never its full movement table. Add explicit `getTextbookClosingSaveContext({closingMonth,subject},options)` for the legacy complete save inputs, and `getTextbookClosingMovementExport(filters,options)` returning `{rows,sourceLineCount,complete:true}` for full-filter clipboard output. These name already-approved separate preview/detail/save/export boundaries, not new calculation semantics or ordinary-page fallbacks.

Sales history also requires its own `getTextbookSaleHistorySummary(filters,options)` / `get_textbook_sale_history_summary_v1(p_filters jsonb)` contract: `{totalCount,totalWaitingQuantity,totalIssuedQuantity,sourceTotalCount,yearOptions,monthOptions,classOptions,effectiveMonth}`. Count and quantities apply the complete year/effective-month/class-filtered grouped source; sourceTotalCount counts all eligible unfiltered history groups so the existing hide-only-when-no-history behavior remains possible. Year options and class options come from all eligible history groups, while month options depend only on selected year, never the displayed page or class. Preserve numeric Korean class-label ordering with an explicit deterministic identity tie. A selected month absent from those month options resolves to `all` in both page and summary; other filters retain their existing meaning. The existing history component does not receive the operations search query and exposes no search input. Accordingly narrow the audit's provisional `SaleHistoryFilters.search` to the empty-string literal, reject nonempty values as invalid input, and pass `""` from its actual consumer; do not invent search semantics or leak an adjacent list's filter into history. These source-backed clarifications supersede that provisional audit field and add no new UI controls.

- [ ] Write RED fixtures proving pairing never splits at page boundaries, same-scope duplicates remain separate, parent totals differ correctly from raw-line badges, grouped sales history page11 is based on group count, and summaries remain identical regardless of requested page.
- [ ] Implement SQL candidate/group sources with existing complete filters and role boundaries. Purchase pairing key excludes order_id and includes status/resolved textbook/class/location/requester/supplier/order_date/statement_number. Preserve current status order and member mutation IDs. Keep request-role reads separate from management aggregates.
- [ ] Preserve the exact existing purchase facet loops: request and board-scope raw-line badges iterate all mode groups with their respective replacement row filter; order-filter badges additionally apply their candidate group restriction. This source-specific clarification supersedes the audit's generic facet sketch. Pager parent count and displayed full-filter totals still use the final visible-group scope; add a literal case proving these distinct counts rather than changing existing badge semantics.
- [ ] Preserve sale-history complete-source options, waiting/issued totals and stale-month fallback through both page and dedicated summary. Include more than100 groups, off-page classes/months, teacher-copy inclusion, cancelled/returned/excluded exclusion, unknown year/class, stale month, empty source and empty filtered result. Its raw-status/latestAt semantics come from the extracted model, not sale-process display normalization; no operations-search dependency.
- [ ] Closing page reads saved closing rows only; explicit preview/detail returns complete month+subject aggregates and separately paged movement rows. Preserve closing formulas from ledger: incoming/outgoing type sets, sale_issue-only team profit, zero teacher/TIPS-store cost, rounded90% external cost. No formula repair is part of paging.
- [ ] Exercise the actual closing-movement projection/search against the legacy source, including more than100 rows, direct page11, empty/out-of-range count, month/timezone boundary, same-time ID tie and both quantity and money search. Keep full aggregate/mismatch metadata independent of detail search/page. Detail row amount, ledger aggregate amount fallback and stored-vs-current rounded mismatch rules are distinct existing formulas; do not silently unify them. Clipboard export retains the full active search scope and fails on incomplete reads.
- [ ] Implement purpose-specific complete contexts for existing writes. `getClassTextbookSaleContext` includes selected class's complete enrolled roster, referenced students/textbook, current all-time balance, and complete billable duplicate IDs for class/textbook/month. Revalidate inventory expected quantity at save. For legacy closing mutation, fetch its complete month/subject movement+reference context only on explicit save, with a completeness assertion; never use a displayed page as its input.
- [ ] Exports use full active filter scope, not current page/selection. Use the explicit atomic complete-context contract above and retain the final grouped service outputs; ordinary page reads may not traverse pages to assemble the export. Detail IDs resolve independently of page rows, with general reference lookup handled by Task4. Tests assert incomplete/error context cannot call writes or produce documents.
- [ ] Execute the extracted handoff builders with literal existing order/return/billing outputs before changing their source path, including cross-order grouping, student/teacher/location splits, raw eligible source-line counts, price/zero formatting and real student line IDs. They may consume an explicitly complete export context; importing the large React workspace into a service or using page arrays as a complete context is forbidden. Preserve PNG/PDF/clipboard rendering and preview-before-send.
- [ ] Run service/context/parity/SQL tests, lint/TS and commit as `feat: add bounded textbook workflow read contexts`.

## Task 3a: Extract the existing handoff models

**Files:**
- Create `src/features/textbooks/textbook-handoff-model.ts`.
- Modify `src/features/textbooks/textbook-operations-workspace.tsx` only for extraction imports/deletions.
- Create `tests/textbook-work-context.test.mjs` for literal executable handoff parity.
- Modify `tests/textbook-workspace.test.mjs` only for affected helper-location guards and real import/invocation wiring.

**Interfaces:** Preserve the existing TextbookHandoffLine/LocationQuantity/Group types and order/return/MakeEdu builder signatures in a pure module. Reuse ledger/taxonomy/read-model dependencies; no React import, alternate formulas, data-loading, SQL, async API, UI rendering or mutation change. This is the first independently reviewable portion of Task3.

- [x] Preserve the original executed-oracle RED and write literal extracted-model expectations for cross-order grouping, repeated student/teacher/location quantities, order/return eligibility, configured cost/rounding/zero formatting and real billing student-line identities. No conditional no-op oracle test remains after extraction.
- [x] Extract only the necessary pure dependency closure. Confirm declaration bodies match the original source apart from export modifiers, and retain existing callers/capture/rendering/preview-before-send behavior. A large necessary closure is reported honestly, not split into new unplanned abstractions.
- [x] Preserve preliminary sale-history adapter/type/test work in the ignored task artifact before restoring only those not-yet-owned edits to the exact Task3 BASE. Archive the unexecuted existence-gate source and retire only the worker's own empty unexecuted candidate/manifest append. No final SQL, prior Task2 implementation or user changes may be reverted. Root's dirty plan is not this task's staging scope.
- [x] Run affected actual handoff/workspace/model/ledger tests, nonincremental TypeScript and focused lint, self-review and commit only the four extraction files as `refactor: isolate textbook handoff projections`. Report exact evidence and WIP preservation hashes in task-3a-report.md; no Task3 SQL/API completion claim.

## Task 3b: Purchase and sale numbered read models

**Files:**
- Create a fresh CLI `textbook_workflow_numbered_reads` migration and `supabase/tests/textbook_workflow_numbered_reads_test.sql`.
- Extend `src/features/textbooks/textbook-read-service.ts` and `textbook-read-types.ts`.
- Extend `tests/textbook-numbered-read-service.test.mjs` and, when required for literal source parity, `tests/textbook-numbered-model-parity.test.mjs`.
- Append only this task's candidate/final manifest entry and post-proof exact query-budget contracts/tests.

**Interfaces:** Implement exactly nine public RPCs and their strict named service methods: list_textbook_purchase_page_v1, list_textbook_sale_page_v1, list_textbook_sale_history_page_v1; get_textbook_purchase_summary_v1, get_textbook_sale_summary_v1, get_textbook_sale_history_summary_v1; get_textbook_operations_summary_v1; get_textbook_purchase_detail_v1 and get_textbook_sale_detail_v1. The three pages take `(p_filters jsonb,p_sort text,p_page integer,p_page_size integer)`, echo the requested page and accept only10/15/20 sizes; sorts are respectively status-event, status-event, month-class-title. Empty/out-of-range requests retain the full matching count and empty rows, leaving the shared controller to clamp. Preserve caller/deadline cancellation, eight-second timeout, retry(false), strict transport and no fallback. No closing RPC, complete export/save context, reference-picker API or UI integration in this subtask.

Purchase detail is `getTextbookPurchaseDetail({anchorLineId,mode},options)` via `get_textbook_purchase_detail_v1(p_anchor_line_id uuid,p_mode text)`, mode request|order. Any real current case-member ID resolves the complete unfiltered display case independently of the originating page; missing eligible source is row:null. Request page/summary/detail admit admin/staff/teacher under existing caller RLS and expose only requested-source/request-safe members and references, not management inventory/supplier aggregates. Order and other management methods admit admin/staff. Forbidden roles remain42501 and invalid inputs22023; no invented created_by read restriction or synthetic mutation ID.

Sale-history summary is `{totalCount,totalWaitingQuantity,totalIssuedQuantity,sourceTotalCount,yearOptions,monthOptions,classOptions,effectiveMonth}`. The first count/quantities cover all matching grouped parents; sourceTotalCount/yearOptions/classOptions cover the complete eligible history source, monthOptions only its selected year. A month absent from those options becomes all for BOTH page and summary; unknown year/class retains an empty result. SaleHistoryFilters.search is the empty-string literal and nonempty input is rejected, because the existing UI has no history search. Preserve raw status, teacher-copy inclusion, lexicographic latestAt, UTC current-month fallback and existing numeric Korean ordering with a stable group-key tie. Other summary/detail DTO fields must cover the actual existing tables, group totals, quantities and operational metrics identified in the required audit and source preflight, not only a current-page count.

The status-event sorts explicitly stabilize previously unspecified within-group display order: existing purchase UI group order requested/ordered/partially_received/received/returned/cancelled and sale group order charged/issued/cancelled/returned, followed by the existing displayed eventAt DESC NULLS LAST and real anchor/line ID ASC. Purchase parent formation remains created_at ASC NULLS LAST/id ASC before this parent sort; only the first student+teacher pair combines, later repeated-scope lines remain singletons. Derive parent event from the existing student-primary line and its own order through current getPurchaseEventAt fallbacks; sale uses current getSaleEventAt with process display status. Preserve native temporal precision, date-only UTC interpretation, raw display strings and absence/unparseable-last ordering. Do not reinterpret board-recent eligibility or history's lexicographic latestAt. Extend existing PurchaseCaseRow with typed mode/status/eventAt/selected-only references/quantities rather than replacing its complete member contract. The sale DTO retains its real line and selected sale/textbook/class/student/location references, status/groupStatus/eventAt/quantity/amount/recipientName; no unused catalog or management stock data in request mode.

Shared textbook reference resolution preserves exact textual record ID, then existing NFKC/lower/whitespace-normalized aliases, then nonempty compact Unicode letter/number aliases. Equal-rank ambiguity uses real book ID ASC to stabilize the formerly unordered source; no active-first preference or changed caller eligibility. Preserve all existing aliases/edition separation and reject empty compact matches. Canonical ID-ordered source may be supplied to the unchanged JS oracle. Task4 inherits this same rule rather than inventing another resolver.

- [x] Start from the reviewed extraction checkpoint. Preserve the archived preliminary adapter work as provenance; restore its tests first and observe genuine missing-method RED before reusing or improving its implementation. Task2 final-wire fixtures and signed-decimal repair remain unchanged.
- [x] Implement complete source predicates, deterministic purchase pairing and sale-history grouping before key pagination. Preserve exact raw-line facet loops, display-parent totals, server order, full-filter sale/operations totals, raw-status/teacher history semantics, full-source history options and stale-month fallback. Retain all real member mutation IDs and only required selected-parent enrichment.
- [x] Keep teacher request page/summary/detail separate from admin/staff management reads under caller RLS. Test another actor's RLS-visible request, nonfirst-member detail, missing eligible source, role denial42501 and strict invalid inputs22023. Do not modify policies, writer functions or auth access.
- [x] Add more than100 paired/grouped parents, repeated same-scope/timestamp ties, off-page quantity/facet/options, requested empty/out-of-range page counts, full scope invariance, missing/malformed API and cancellation/no-fallback fixtures. Run actual candidate then immutable final SQL with unchanged baseline parity and exact verbatim final-wire replay through the service.
- [x] Only after final proof register these three exact numbered and six exact nonpageable contracts. Run affected service/model/ledger/workspace/budget tests, nonincremental TS and lint; commit as `feat: add numbered textbook purchase and sale reads`, report task-3b-report.md, then stop for independent review.

## Task 3c: Closing reads and atomic complete work contexts

**Files:**
- Create CLI `textbook_closing_work_context_reads` migration and `supabase/tests/textbook_closing_work_context_reads_test.sql`.
- Extend `src/features/textbooks/textbook-read-service.ts` and `textbook-read-types.ts`.
- Create `src/features/textbooks/textbook-work-context-service.ts`.
- Create `src/features/textbooks/textbook-closing-model.ts` only if needed for the existing closing detail row/metric/search pure closure; modify the workspace only for that extraction/import and equivalent inline-projection delegation. Extend the real model-parity/workspace tests for moved guards. No asynchronous UI integration or unrelated rendering changes.
- Extend `tests/textbook-numbered-read-service.test.mjs` and `tests/textbook-work-context.test.mjs`.
- Append only this task's candidate/final manifest entry and post-proof exact query-budget contracts/tests.

**Interfaces:** Implement exactly the other nine Task3 public RPCs: list_textbook_closing_page_v1, list_textbook_closing_movement_page_v1; get_textbook_closing_detail_v1, get_textbook_closing_preview_v1; get_class_textbook_sale_context_v1; get_textbook_purchase_handoff_context_v1, get_textbook_billing_handoff_context_v1, get_textbook_closing_save_context_v1, get_textbook_closing_movement_export_v1. Keep named service outputs, including final `{groups,sourceLineCount,complete:true}` exports, and reuse the reviewed Task3a pure builders plus Task3b source/key helpers without changing prior final SQL. No ordinary-list fallback or new chunk/token protocol.

The two pages keep the four standard `(p_filters jsonb,p_sort text,p_page integer,p_page_size integer)` arguments: closing filters `{month,subject,status}`, sort month-desc; movement filters `{closingMonth,subject,search}`, sort event-desc. Detail uses `(p_id uuid)`. Preview uses `(p_input jsonb)` with exact `{closingMonth,subject,openingQuantity,openingAmount}`. Class sale context uses `(p_input jsonb)` with exact `{classId,textbookId,chargeMonth,locationId}`. Purchase handoff uses `(p_filters jsonb,p_kind text)` where kind=order|return; billing handoff `(p_filters jsonb)` with SaleFilters. Closing save context uses `(p_closing_month text,p_subject text)`; movement export `(p_filters jsonb)` with ClosingMovementFilters. All are management-only admin/staff invoker reads under caller RLS, not new writer authority. Caller/deadline abort, eight-second deadline, retry(false), strict transport and no fallback continue to bind every method.

The wire work contexts carry their explicit selected/eligible source identities, complete raw source arrays/count and only referenced projections under one statement snapshot. The service invokes the reviewed real handoff builders and returns final grouped outputs, not a divergent new formatter. Complete closing-save context feeds the unchanged legacy filter/ledger on explicit Save. Class context preserves the entire normalized enrolled ID sequence and corresponding visible student records/missing-record ID fallback, selected textbook and all-time selected-location inventory plus full same-class/book/month billable duplicate identities/count. Do not confuse unique student records with a possibly repeated legacy roster sequence or the count fallback used in a class picker. Missing required authorized class/textbook/location context fails explicitly22023; a missing saved closing detail is legitimate row:null. Teacher-issue and sale-transition callers later compose their own explicit selected reference/detail/balance inputs from the already named services rather than inventing extra Task3c RPCs or passing a page bundle.

Closing row/metric/search parity must execute the actual existing projection. If extraction is needed, preserve existing named pure helpers and the inline movement map body in a pure closing-model module, with original-source/literal RED evidence before wiring equivalent delegation; leave clipboard rendering/React behavior unchanged. `Math.round` stored/current comparisons, raw serialized moved_at month prefixes, quantity versus amount fallbacks, sale_issue-only margin and cash-default differences remain distinct. Do not port those calculations from prose alone and call that parity.

- [ ] Write RED strict actual service/context fixtures and literal local SQL cases for saved closing rows, independently paged closing movements, full preview/detail totals, raw serialized-month/timezone boundary, event-ID tie, distinct row-versus-ledger amount fallback, rounded stored mismatch and full-search clipboard scope.
- [ ] Preserve all closing formulas, current referenced publisher/supplier preference, teacher/Tips zero cost, external rounded90percent cost, sale_issue-only margin, cash-input defaults and explicit save memo/review semantics. No formula repair or actual mutation in this read task.
- [ ] Return full selected class roster/student identities and billable duplicate IDs/count with complete all-time selected balance. Return only explicit active export-filter or closing month/subject rows with needed references, count and identities under one atomic statement snapshot. No arbitrary cap, all17-table hydration or partial success. Strict missing/inconsistent context prevents downstream document/write invocation.
- [ ] Exercise all actual extracted handoff outputs over complete contexts, including sourceLineCount eligible-raw-line semantics and cross-page/off-page data. Keep preview-before-send and failure/cancellation handling; service output grouping is not a notification send.
- [ ] Run actual candidate/final SQL with role/invalid-input/no-send assertions and untouched final SQL DTO service replay, then register only the two exact numbered and seven purpose-complete contracts. Run affected context/service/model/ledger/workspace/budget tests, TS and lint; commit as `feat: add textbook closing and complete work contexts`, report task-3c-report.md and obtain independent review. Only then mark umbrellaTask3 complete.

## Task 4: Independent reference reads and complete cleanup targets

**Files:**
- Create CLI migration `textbook_reference_numbered_reads` and `supabase/tests/textbook_reference_numbered_reads_test.sql`.
- Create `src/features/textbooks/textbook-reference-service.ts`; extend the shared read types with its exact independent contracts.
- Create `src/features/textbooks/textbook-reference-model.ts` only for the existing pure picker projection/filter helper closure that needs executable parity, with minimal workspace imports/deletions and affected helper-location test updates. Do not wire async UI loading yet.
- Create `tests/textbook-reference-read-service.test.mjs` and `tests/textbook-reference-model.test.mjs`.
- Append only this task's exact candidate/final manifest entry and post-proof exact query-budget contracts/tests; all prior final SQL is immutable.

**Interfaces:** Source-backed reference contracts; also read `.superpowers/sdd/2026-08-31-numbered-pagination-textbook-workflows/task-4-reference-preflight.md` for the actual helper evidence.

Root closes the source-design gate with nine named purpose-specific public RPCs below. This is a design ruling, not implementation or DB proof. Task4 still waits for reviewed Task3b/3c; regenerate its brief and verify the final predecessor helpers before dispatch. All new functions are STABLE invoker/fixed empty search_path with explicit role checks and authenticated-only execute. No arbitrary table selector, new definer or policy/table grant. The only inherited definer dependency is the existing five-key `public.list_active_science_subject_areas_v1()` function documented in the reference preflight, called inside the management-only metadata boundary.

### Four independent option pages

Each takes `(p_filters jsonb,p_sort text,p_page integer,p_page_size integer)` and returns the requested NumberedPage plus its authoritative option/facet metadata. Sizes10/15/20; nonpositive/null page or other size/sort invalid22023. Empty/out-of-range rows retain matching count/requested page. No client traverse-to-page, no current-record-page dependency, no general catalog fallback.

| Service / RPC | Filters and sort | Complete source and base order |
| --- | --- | --- |
| listTextbookReferencePage / list_textbook_reference_page_v1 | `{search,selectedFilters}`; allowed selected keys subject/grade/subSubject; sort match-title | Caller-visible active books; Korean numeric title then real ID ASC. |
| listTextbookClassReferencePage / list_textbook_class_reference_page_v1 | `{search,selectedFilters}`; keys subject/grade/teacher; sort match-name | All caller-visible classes, no status restriction; Korean numeric class label then real ID ASC. |
| listTextbookTeacherReferencePage / list_textbook_teacher_reference_page_v1 | `{search}`; sort match-name | Distinct nonblank exact teacher names, not teacher UUIDs; existing Korean NONnumeric name order then exact name tie. |
| listTextbookLocationReferencePage / list_textbook_location_reference_page_v1 | `{search}`; sort match-order | Caller-visible locations; configured sort_order ASC NULLS LAST then real ID ASC. |

All four admit admin/staff/teacher under existing caller RLS. Search is an untrimmed string, not null; selectedFilters is an object with optional allowed keys containing distinct string arrays. Unknown keys/types or repeated selected values are invalid22023; a valid but unknown selected value retains the existing empty-result behavior when its base group exists. No arbitrary search/catalog cap. The actual option fields are the existing SearchSelectOption shape (value,label,description/searchText,metaRows,filterValues), extracted rather than replaced with an unchecked Row. Current book/class metadata/facet semantics above remain exact. Teacher option value/label is the name; location value is real UUID, label is text(raw name||code), search metadata includes its code. Custom teacher names remain client-owned selection values, never auto-created records.

Book/class page envelopes additionally return `baseFilterGroups`, `visibleFilterGroups`, `activeFilterCount` using the existing SearchSelectFilterGroup shape including option counts. Both group collections are full-source metadata, not current-page data; search does not scope peer facets. Canonical base order supplies deterministic first-label/tie behavior. Preserve OR-within/AND-across, absent-base-group ignored, raw unknown selected value versus valid active-count distinction. Teacher/location have no invented facets. Location pages additionally return `defaultLocation:{id,code,name}|null`, resolved over the FULL authorized location source independently of query/page: first trimmed code exactly main in canonical location order, otherwise first canonical location, otherwise null. Class inference retains its separate case-insensitive main/annex logic.

For nonempty search, use installed cmdk1.1.1 defaultFilter over the exact existing buildSearchSelectCommandValue(option).trim() bytes, including metadata/facet labels, rather than substring or page-local filtering. Positive scores rank DESC then the canonical base order; empty search bypasses scoring and uses base order. A private memoized/iterative SQL scoring helper may port the existing algorithm; it must be tested against the installed actual pure function for Korean/noncontiguous/transposition/duplicate-letter/case/punctuation/whitespace and non-BMP input. Avoid repeated relation hydration during scoring; build the minimum option/search projection, select keys, then enrich returned options. Retain the upstream MIT copyright/permission notice with any substantial derived scoring implementation. Measure actual SQL execution; do not speculate an index or silently cap candidates to conceal a slow query.

### Independent selected reference/default context

- `resolveTextbookReference({reference,activeOnly,scope,fallbackSupplier},options)` calls `resolve_textbook_reference_v1(p_reference text,p_active_only boolean,p_scope text,p_fallback_supplier text)`. Scope=request|management. Request admits admin/staff/teacher; management admin/staff. reference/fallbackSupplier are strings, activeOnly a real boolean, unknown scope/types invalid22023. Empty or unmatched reference returns `{row:null}`. Resolve over the whole eligible source using Task3b's exact textual ID→normalized→nonempty compact alias rule and same-rank real ID ASC. Apply activeOnly before resolution; no active-first preference in all-reference mode.
- A resolved row is `{textbook,option,configuredSupplierId,supplier}`. textbook is an explicit typed selected catalog projection containing the title/name/status, price/sale/list price, publisher/default-supplier IDs, publisher/category/subsubject/school/grade/science/ISBN/barcode and any existing raw aliases actually consumed by the preserved projection/pricing helpers; no inventory/stock aggregates or whole catalogs. option is the exact active-book option or null for an inactive selected entity. Management resolves the existing configured supplier preference, then the caller's legacy fallback, and returns only that selected `{id,name}` supplier if RLS-visible. Request uses only the old request-safe direct book supplier field/fallback, supplier:null and no publisher-link/supplier-table read. The unchanged pricing helper takes this selected context plus the local unit-cost fallback/copy scope; teacher-zero/positive-price/external rounding rules do not change. Already-valid lifecycle UUID fast paths remain in the existing writer until Task5 rewires only its non-UUID resolver; neither a successful reference lookup nor UUID syntax grants mutation authority.
- `getTextbookClassReference(classId,options)` calls `get_textbook_class_reference_v1(p_class_id uuid)`, admin/staff/teacher. Missing visible class `{row:null}`; null input invalid22023. Resolved `{row:{id,name,option,enrolledStudentCount,defaultTeacherName,inferredLocation}}`. enrolledStudentCount is the existing normalized listIds roster length, not the picker's raw-count fallback. defaultTeacherName follows original teacher-ID alias order and first named visible catalog teacher, then first denormalized name. inferredLocation is the selected minimal `{id,code,name}` from the existing classroom/main/annex inference or null. No full student roster or inventory here; Task3c owns that explicit sale context.
- `getTextbookLocationReference(locationId,options)` calls `get_textbook_location_reference_v1(p_location_id uuid)`, admin/staff/teacher; `{row:{id,code,name,option}|null}`, null input invalid22023. This preserves an independently selected off-page location. Missing/failed reads never fabricate main/annex IDs. Current teacher selection needs no selected-ID RPC because its value IS the custom-compatible name; the client preserves and deduplicates that name independently of the returned page.

### Lightweight master metadata and explicit cleanup

- `getTextbookMasterOptions({subject,listSubject,bulkSubject},options)` calls `get_textbook_master_options_v1(p_filters jsonb)`, admin/staff only. Exact keys: subject is one canonical english/math/science/other; listSubject adds all; bulkSubject adds keep. Returns `{publisherOptions,subSubjectOptions,categoryOptions,bulkCategoryOptions,scienceSubjectAreas,counts,complete:true}`. publisherOptions contains only `{value,label,description}` labels (configured-first union with all active legacy publishers); no publisher/supplier records or links. It excludes the client draft and none sentinel, which the client merges with the unchanged label helper. subSubjectOptions follows the master subject's visible default-overlaid settings. categoryOptions follows full active list-subject books plus settings with the NONnumeric label order. bulkCategoryOptions follows the existing keep/selected-subject union with numeric order. scienceSubjectAreas contains the exact five-column active existing science RPC rows, at most five by the final physical constraint. counts names each array and equals its full distinct length. No arbitrary option cap, no stock/ledger rows or17-table bundle. This is purpose-complete lightweight metadata loaded only when its owning management surface/form needs it; it is not a bounded-row classification or a way to hydrate ordinary book/class references. Keep existing local SearchCombobox scoring on these complete label-only arrays. Persisted hidden/default merge semantics are shared with later Task7, not reinterpreted here.
- `getTextbookInactiveCleanupContext(options)` calls `get_textbook_inactive_cleanup_context_v1()`, admin/staff only. Returns `{targetIds,totalCount,previewRows,complete:true}` under one statement snapshot, independent of master query/page/selection. IDs are all distinct real inactive IDs in canonical Korean numeric title/ID order, totalCount equals targetIds.length, previewRows contains exactly the first min(5,totalCount) matching `{id,title,detail}` items from the existing helper. Empty complete context is legitimate; missing/aborted/inconsistent context fails. Task5 uses totalCount for remaining-preview text and retains explicit irreversible-action confirmation plus unchanged purge writer rechecks. This read task never invokes purge.

Every named service uses the shared actual client, caller signal plus eight-second deadline, retry(false), strict nested parsing and no empty/full-load fallback. Tests must distinguish null (legitimate absence), missing/malformed response (failure), actor/role presentation lifecycle (Task5), and real RLS/ACL42501 (SQL). Register exactly four numbered and five separate selected/metadata/complete-context budget contracts only after final immutable SQL proof. Task4 owns minimal pure helper extraction and executable literal/original-oracle tests; Task5 owns all async UI/actions. No existing writer, provider, policy or live browser access changes.

- [x] Freeze source-backed APIs from actual TextbookSelect/ClassSelect/TeacherSelect/LocationSelect, their search/facet/metadata helpers and lifecycle alias resolver. Preserve exact→normalized→compact reference precedence and edition separation, missing/custom selected values, original location/default behavior and actual request-role source scope. Preserve active-only versus all-reference lookup meaning explicitly. No catalog cap proves completeness.
- [ ] Write RED actual model/transport fixtures for more than100 references, bounded direct search, independent off-page selection, peer facets/options, duplicate labels/aliases, unknown references, teacher request versus management authority, aborted/late failures and missing API without fallback. Keep record-list page and picker state independent.
- [ ] Implement invoker reads with role/RLS parity, allow-listed filters/sorts, selected-page-ID enrichment and strict transport fields. Bounded lists return only requested rows with matching full count; selected-reference detail resolves its complete required references without all-catalog hydration. Full option/facet metadata must follow its real source scope rather than current-page rows.
- [ ] Preserve complete whole-inactive cleanup targeting separately from current master filters/page/selection. Return authoritative complete real target IDs, count and bounded confirmation preview under existing read authority; incomplete/error context cannot authorize the existing destructive action. The UI still requires its original explicit confirmation, and the existing purge writer rechecks its own permissions/state. No deletion, writer rewrite, permission broadening or send is part of this task.
- [ ] Execute candidate/final local SQL with exact permissions/invalid-input/lookup/completeness assertions, untouched actual SQL DTO service replay, focused parity/service/budget tests, nonincremental TS and lint. Commit as `feat: add independent textbook reference reads` after review-ready evidence. Task5 owns async picker/UI/action integration.

## Task 5: Operational tabs adopt prepared pages

**Files:**
- Create `src/features/textbooks/use-textbook-numbered-data.ts`.
- Modify `src/features/textbooks/textbook-operations-workspace.tsx`.
- Modify `src/features/textbooks/textbook-service.ts` only for explicit work-context integration, not domain formula changes.
- Create `tests/textbook-numbered-pagination.test.mjs`.

**Interfaces:** The new hook keeps separate page snapshots per visible record-list tab, full-filter summary metadata and explicit work contexts. It exposes prepared row DTOs rather than substituting page arrays into TextbookOperationsData. Preference IDs use `textbooks:<tab>`. Existing row components consume prepared page rows plus callbacks and matching summaries.

- [ ] Write RED controller/component tests for direct page11, previous-page retention/error/retry, atomic scope reset, refresh clamp, URL/detail restoration and independent tab scopes. Add draft/selection fixtures verifying page changes preserve inventory drafts but clear current-page selections.
- [ ] Exercise resolved same-ID role change, logout/relogin and late page/summary/catalog/detail completions through the real consumer. Clear old authorization-scoped data immediately and reject old cache writes; preserve same-actor drafts across ordinary paging. Carry server-selected order through actual raw DTO/service/model/hook/table paths rather than re-sorting display titles locally.
- [ ] Replace TextbookTable master slice60, PurchaseProcessTable list processing, SalesProcessTable, SalesHistoryLedger, InventoryCountWorkspace, InventoryHistoryPanel slice30 and MonthlyClosingTable slice12 with prepared paged results. Remove list `더 보기` controls. Each shows shared pager and whole-filter count; mobile mirrors use the same page.
- [ ] Remove inventory draft pruning based on `data.inventory`; key drafts by user/textbook/location, clean only after confirmed deletion/save. Preserve current-page member IDs for bulk actions; never select all filtered results implicitly.
- [ ] Wire editor/mutation/export entrypoints to Task3's complete purpose-specific contexts and Task4's independent reference/cleanup contracts. Forms must not infer missing reference records or duplicate absence from page contents. Dirty form guards remain; page changes cannot discard unsaved edits. Tab facets/options come from authoritative summaries, not page values.
- [ ] Remove startup use of the17-table `listTextbookOperationsData` bundle only after every list/summary/context dependency is replaced. Keep legacy helper callable solely where a remaining explicitly requested complete work context requires it, and name that boundary in tests; no silent general fallback.
- [ ] Run all affected textbook parity/service/controller/workspace/export regressions, focused lint/TS and commit as `feat: paginate textbook operational tabs`.

## Task 6: Supplier and publisher editable pages

**Files:**
- Create CLI migration `textbook_supplier_numbered_reads` and `supabase/tests/textbook_supplier_numbered_reads_test.sql`.
- Extend read service/types.
- Create `src/features/textbooks/textbook-settings-draft-service.ts` and focused transport/draft tests for the authorized owner-scoped transactional save boundary; the following taxonomy task extends this same boundary rather than creating an unrelated Save button.
- Modify `src/features/textbooks/textbook-supplier-settings-workspace.tsx`.
- Create `tests/textbook-supplier-numbered-pagination.test.mjs`.

**Interfaces:** Implement audit `listTextbookPublisherPage`, `listTextbookSupplierPage`; each row includes complete ordered links for that owner, authoritative related counts/names and independent picker support. Draft maps/tombstones are keyed by owner ID, not page.

- [ ] Apply the user's 2026-08-31 approval of recommended settings save-structure improvements. Restrict each save to dirty supplier/publisher owners, preserve each dirty publisher's complete ordered links, and leave off-page owners untouched. Retain existing controls and write authority. Operational tasks1–5 remain independent.
- [ ] With that scope resolved, write RED tests proving off-page publishers/links are unchanged when saving one owner, draft roundtrip retains all ordered supplier IDs and primary priorities, counts retain id-first/name-fallback semantics, and page changes never use a partial link set as replacement input.
- [ ] Add invoker page/detail/picker reads with RLS/role parity, exact count and stable id order. Separate full owner link detail from arbitrary current-page links; catalog limits are not proof of completeness.
- [ ] Refactor draft/save targets to dirty owners only while preserving each dirty owner's complete link list and existing primary/priority meaning. Apply any approved authoritative ordering contract separately; never globally renumber only a page. Keep subsubject lazy loading and its distinct global-order boundary.
- [ ] Use one domain-specific transactional invoker save for dirty publisher/supplier owners and complete links, with optimistic base revision and actor/request replay identity for uncertain retries. Validate and apply the entire submitted owner set or none, retain untouched owner rows/links, and preserve existing DML authority. Preserve a single workspace Save coordinator and keep unrelated subsubject drafts until their explicit successful save; Task7 extends the same atomic endpoint to taxonomy. No generic table writer or partial-success masquerade. Read errors never imply missing/empty links.
- [ ] Shared pager/preferences and preserved unsaved guards, then service/controller/SQL tests, lint/TS and commit as `feat: paginate textbook supplier settings`.

## Task 7: Editable subsubject taxonomy pages and shared Save

**Files:**
- Create CLI migration `textbook_taxonomy_numbered_drafts` and `supabase/tests/textbook_taxonomy_numbered_drafts_test.sql`.
- Extend textbook read types/service and `textbook-settings-draft-service.ts`.
- Modify `textbook-supplier-settings-workspace.tsx` for its subsubject panel and shared Save coordinator only; preserve reviewed publisher/supplier paging.
- Extend `textbook-taxonomy.ts` only with pure default identity/projection helpers if required; preserve existing picker and legacy helper behavior.
- Create `tests/textbook-subsubject-numbered-pagination.test.mjs` and update affected lazy-taxonomy tests with actual behavior coverage.

**Interfaces:** `listTextbookSubSubjectPage({page,pageSize,filters:{subject,search},draft},options)` uses `list_textbook_sub_subject_numbered_page_v1(p_filters jsonb,p_draft jsonb,p_page integer,p_page_size integer)` and returns the projected `NumberedPage<TextbookSubSubjectSettingRecord>` plus matching revision, whole-taxonomy visible count, and per-returned-row move directions from the entire subject order. Extend Task6's atomic settings save body with the chronological subsubject journal; reuse its actor/request identity and all-or-none transaction for the shared workspace Save. Add/patch/delete/move operations carry stable IDs; the server resolves off-page neighbors and duplicate checks without full-list client reads.

- [ ] Verify final taxonomy table/RLS/constraints and executable `mergeTextbookSubSubjectSettings` parity. The effective source is persisted normalized named records plus every missing built-in `(subject,name)` default, not defaults only when a subject is empty. Preserve English/Math/Science/Other defaults, visible flags, subject order, numeric name ties and explicit duplicate-name validation after trim. Built-in defaults are a finite overlay; persisted custom rows are not bounded.
- [ ] Keep reads lazy until the subsubject tab is opened. Ordinary page reads must not persist defaults or convert an unavailable/missing new RPC into a successful built-in-only catalog. Represent virtual defaults with stable non-DB identity; explicit save materializes only necessary rows with valid stable UUIDs/replay identity, never writes legacy `english-단어` strings to a UUID column. Preserve missing-default reappearance after a fresh canonical reload and suppress it while an explicit draft tombstone is active. Test rename/delete of a built-in separately from a custom row; do not change general textbook picker fallback behavior.
- [ ] Project the full chronological journal over that effective source before filter/count/page. Add uses the active subject's global final rank; move swaps adjacent rows in the complete subject order regardless of search/page, preserving other subjects. Return global move availability instead of disabling the first/last row of each page. Use the existing stable Korean numeric name semantics with a deterministic identity tie; do not renumber only a page. Preserve blank editable new rows before Save and the legacy trimmed-empty omission rule without silently deleting persisted rows.
- [ ] Test more than100 custom rows plus missing-default overlays, direct page11 in one request, full total/visible badge, page10→11, off-page duplicate, move across page/search boundaries, add-at-global-end and retained edits across page/subject/tab changes. Actual desktop/mobile consumers must use the same page and common pager preference `textbooks:subsubjects`; no current-page sorting/filtering that changes server order.
- [ ] Extend the single atomic Save to pending publisher/supplier/link/subsubject changes together. Untouched taxonomy remains unread/unwritten on a publisher-only save. Validate all dirty edits before any DML; constraint/RLS/conflict failure rolls back all included changes and preserves all drafts. No-op leaves timestamps unchanged; uncertain retry reuses the identical request body/ID, with no automatic write retry. Subsubject duplicates are checked against off-page persisted/projected rows. Preserve native privilege/constraint errors and never manually raise40001.
- [ ] Preserve resolved actor/role clearing, auth readiness, stale-result guards, existing unsaved navigation behavior and successful-save-only draft clearing. Run real model/service/consumer tests, final local pgTAP with permissions/default/order/atomic rollback/replay coverage, focused lint/TS and commit as `feat: paginate editable textbook taxonomy settings`.

## Task 8: Textbook verification gate

**Files:** Update `docs/qa/2026-08-31-numbered-pagination.md`.

**Interfaces:** Evidence per master, request, order/receipt, sale/history, inventory/history, closing, supplier/publisher and editable subsubject list; exact formula/parity/export/draft/permission test results and remaining deployment gates.

- [ ] Run the complete affected textbook regressions and production build. Verify no in-scope operational list still starts the17-table full bundle or accumulates displayed pages, and no old full-scope calculation receives only page rows.
- [ ] Execute isolated final SQL and first/middle/final explain probes when available; record offset cost honestly, and add an index only from measured plan evidence. Record browser tests only when permitted and RPC capability exists.
- [ ] Independent whole-slice review and one fix/re-review wave. The user has approved the required settings save improvements; unimplemented publisher/supplier/subsubject work is pending implementation, not an unresolved approval. App-wide completion requires the remaining general settings/audit plan, not just textbook tabs.
