# UX/UI 개선 구현·검증 기록

2026-09-22 · `codex/ux-ui-refinement-20260922` · 기준 `bc64ea648afeb38cce706795f0eda25a5d8696ff`

[원본 리뷰](2026-09-22-full-ux-ui-review.md) · [승인된 실행 계획](2026-09-22-full-ux-ui-improvement-plan.md)

## 반영 범위

| 계획 | 반영한 동작 | 주요 파일 |
|---|---|---|
| T9 / T1 / F14 | 캘린더 중립 일정 면·종류 표식·제목 줄바꿈, 날짜/검색/보기/생성 위치, 최초 조회 상태 구분, 한 주 복구에서 같은 상세·권한 사용 | `calendar-main.tsx`, `calendar.tsx`, `academic-calendar-workspace.tsx` |
| T1 / T4 | 전반·퇴원 검색 기본 노출, 검색 범위와 정렬 분리, 학생/수업/현재 행동 중심 기본 열, 기존 열 폭 보존, 조회 실패와 정상 0건 분리 | `ops-task-workspace.tsx` |
| T1 / T2 / T4 | 휴보강 상태별 추천 열, 공용 필터, 모바일 중첩 카드 제거, 제출 오류를 처리 창 안에 표시하고 메모 보존 | `makeup-request-workspace.tsx` |
| T4 / F08 | 재시험 표 관계, 학생/점수/결과/다음 행동 중심, 부가 정보 열 설정, 키보드 너비 조절, 계보/계정 연결 경고 보존 | `ops-task-workspace.tsx` |
| T8 | 알림 채널의 장애 격리·채널 재시도, 탭/저장바 정리, 홈페이지 수동 탭과 연결 패널·공용 목록 면, 채용 중복 제목 제거·삭제 후 초점 복귀, 교재 ‘정리’ 명칭 통일 | notifications / public-content / recruiting / textbooks |
| T2 / T3 | 프로필 고정 header/footer·스크롤 body·필드 검증·재로그인 안내·반응형 전환 중 초안 유지, 모바일 실제 경로 이동 시 닫기·현재 메뉴 재선택, 가장 구체적인 경로만 활성화 | `nav-user.tsx`, `nav-main.tsx`, `app-sidebar.tsx`, `sidebar.tsx` |
| T5 | 수업 상세 총원을 30개 관계 페이지와 분리해 반환·표시 | `use-management-records.ts`, `records.js`, `management-page.tsx`, 새 migration |
| T6 | 정규화 일정 preview의 날짜별 신규/기존 표시, 전부 기존일 때 확정 비활성, 미리보기는 초안을 변경하거나 저장하지 않음 | `class-schedule-workspace.tsx` |
| T7 | 학교 표시 정렬과 저장 순서 분리, 필터 뒤의 오류 행으로 복귀, 학교/선생님/강의실 반복 제어 이름 | master workspaces |

공용 `useDataTableColumns`에 `defaultVisible`을 추가했다. 저장된 boolean을 우선하고 기존 소비자는 모두 표시 기본값을 유지한다. 공통 토큰을 덮어쓰지 않았으며 메뉴별 표현을 `DESIGN.md`에 기록했다. 전자결재와 수업그룹 기능은 재도입하지 않았다.

## 검증 환경과 증거

원본 checkout의 기존 수정은 보존했다. 합성 데이터만 사용하는 `scripts/qa/ux-refinement-fixture-server.mjs`를 통해 실제 Next 화면을 확인했다. 브라우저 `127.0.0.1:3270` → Next `3271`, 합성 backend `3272` 구성이다. 미등록 데이터 경로는 501, Next에 대한 쓰기 요청은 405로 차단하며 운영 환경변수·세션·개인정보를 사용하지 않는다.

재현:

```sh
node scripts/qa/ux-refinement-fixture-server.mjs
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:3272 NEXT_PUBLIC_SUPABASE_ANON_KEY=fixture-only NEXT_PUBLIC_FALLBACK_ADMIN_EMAILS=fixture@example.invalid node node_modules/next/dist/bin/next dev --webpack --hostname 127.0.0.1 --port 3271
```

`http://127.0.0.1:3270/__fixture`에서 시작한다. `POST http://127.0.0.1:3272/__control`의 `mode`로 normal/empty/error/loading/dense를 전환한다. `/__evidence`는 합성 요청 기록만 반환한다.

화면 증거는 로컬 `output/ux-refinement/`에 있다. 캘린더만 동일 데이터·1440×900/390×844의 전후 캡처를 확보했다. 다른 메뉴는 개선 후 캡처와 기존 코드/공용 계약을 대조했으며 동일 조건의 전후 픽셀 비교로 주장하지 않는다.

- 캘린더: 첫 조회 503 → 실패 표시 → 같은 범위 수동 재시도 → 45개 합성 일정 복구. 원래 운영의 일시 실패 원인은 이 재현만으로 확정할 수 없다.
- 전반: 긴 수업명·3행, 기본 검색/열/현재 행동을 1440px에서 확인. 모바일 페이지 가로 넘침 없음.
- 휴보강: 긴 수업명·3행, 6개 기본 열과 모바일의 사유/일정/장소 확인.
- 재시험: 점수 입력과 현재 행동을 같은 행에서 확인. 선택 전후 검색 top=148px, 첫 표 행 top=291px로 유지.

- 퇴원: 7개 기본 열과 긴 수업명, 모바일 3행·검색·처리 행동·번호 페이지를 확인. 모바일 실제 메뉴 이동 후 탐색 메뉴가 닫히고 `#admin-workspace`로 초점 이동.
- 홈페이지/알림/채용: 1440px·390px 목록/채널/저장 영역 확인. 알림 채널 실패 중 고객 안내 접근과 채용 단일 제목·빈 상태/삭제 후 초점은 실제 컴포넌트 테스트도 통과.
- 프로필: 390×667에서 입력·고정 저장 영역·가로 넘침 없음. 1440→720×450→390×667 전환에서도 같은 창과 선택 아바타 유지. 실제 비밀번호 변경/계정 저장은 하지 않았다.
- 캘린더 추가: 최초 과밀 응답 후 무한 로딩 없이 ‘한 주 보기’ 안내, 주간→월간 복귀, 다크 테마. 주간 상세의 정확한 ID/권한과 늦은 응답 차단은 실제 컴포넌트 테스트로 확인.

## 최종 자동 검증

| 검증 | 결과 | 범위/제약 |
|---|---|---|
| 선택 회귀 테스트 | **736 / 736 통과** | 셸·공용 제어·캘린더·업무·휴보강·알림·홈페이지·채용·설정·수업 상세/일정. 전체 저장소 전수 테스트라는 뜻은 아니다. |
| TypeScript / production build | 통과 | `next build --webpack`, 합성 환경변수. 80개 정적 페이지 생성. |
| 변경 소스 ESLint / diff 공백 검사 | 통과 | 대형 기존 업무 파일에 대한 Babel 정보 메시지만 출력. |
| migration layout | 통과 | migration manifest 정합성 포함. |
| 격리 DB / pgTAP | 통과 | 최종 migration chain, 총원 0/30/31/65, 관계 페이지 30, ACL/invoker, 기존 읽기 계약. |

테스트 명령은 `node --test --test-concurrency=3 --experimental-strip-types`로 영향 범위 43개 파일을 실행했다. 기존 실패로 확인된 `class textbook picker follows...` 한 건만 `--test-skip-pattern`으로 제외했다. `lesson-design-page.test.mjs`의 기존 실패 12건은 별도 baseline 로그에 남겼으며 위 통과 수에 포함하지 않았다. 이를 수정된 테스트나 새 회귀로 섞어 보고하지 않는다.

최종 로그는 로컬 `output/ux-refinement/validation/`에 보존했다. 테스트 중 JSDOM의 `attachEvent/detachEvent` polyfill 로그가 있었으나 테스트 실패는 없었고 관련 검색 초점은 실제 Chrome에서도 확인했다. 빌드 중 public class 조회가 fixture의 미등록 경로(501)를 만났으며 정적 페이지의 기존 실패 처리가 적용되어 빌드는 정상 종료했다. 운영 조회 성공 증거로 해석하지 않는다.

## 최종 리뷰와 수정

새 컨텍스트 리뷰어가 전체 변경을 검토한 뒤, 다음 항목을 재현 테스트로 고쳤다. 추가 리뷰를 반복하지 않고 각 실패→성공 및 최종 회귀 실행으로 검증했다.

1. 전반·퇴원·휴보강 검색 범위에 잘못된 `all` 옵션이 들어가지 않게 했다.
2. 휴보강 검색 실패 후 정렬해도 요청한 검색어·범위를 유지한다.
3. 주간 복구에서 월간으로 떠난 뒤 도착한 일정 상세 응답을 무시한다.
4. 첫 달력 조회의 과밀 응답을 로딩 상태로 남기지 않는다.
5. 알림 준비 상태 재시도와 정상 패널 교체 중 채널 탭 초점을 유지한다.
6. 계정의 재인증/세션 만료를 다시 로그인할 수 있는 안내로 바꾸고 초안을 보존한다.
7. 이전 행을 유지하는 동안 실제 승인된 검색/기간/페이지를 표시한다.
8. DB 총원이 정규화 단계에서 30행으로 덮이지 않게 했다. 총원이 없으면 확인 중으로 표시하며 관계 페이지는 유지한다.
9. 반응형 사이드바가 바뀌어도 프로필 창·초안을 유지하고, 열기 버튼이 사라진 뒤 닫으면 본문으로 초점을 돌린다.

리뷰어의 두 경미 후보(계정 오류 안내, 이전 결과 기준)는 사용자가 복구하거나 올바른 대상을 처리하는 데 영향을 주므로 중요 문제로 판단해 수정했다. 최종 리뷰에서 보류한 경미 수정은 없다.

## DB와 배포 순서

`20260922140000_class_detail_roster_counts.sql`은 ordered migration의 최종 `get_management_detail_v1(text,uuid)` 정의에 등록/대기 총원만 추가한다. 기존 관계 조회와 같은 RLS-visible 학생 및 `student_ids`/`waitlist_ids` 기준을 사용하고 owner/ACL/security invoker를 보존한다. 업무 충돌 SQLSTATE를 추가하지 않았다.

격리된 Supabase에서 `class_detail_roster_counts_test.sql`과 `management_page_reads_test.sql`을 최종 migration chain에 실행해 통과했다. 등록 0/30/31/65, 대기 31, 첫 페이지 30, ACL/invoker와 기존 SQLSTATE를 검증했다. 임시 DB/container는 정리했다.

운영 적용 시 **migration → 클라이언트 배포 → 목록/상세 인원 읽기 비교** 순서가 필요하다. 이전 DB 응답에는 총원이 없어 UI가 ‘인원 확인 중’으로 표시된다. 운영 migration/배포/실제 저장/발송은 이번 로컬 검증과 별개이며 실행하지 않았다.

## 알려진 검증 한계

- `lesson-design-page.test.mjs`의 옛 편집 UI 구조를 기대하는 12건은 시작 커밋의 소스로도 동일하게 실패한다. 이 변경의 회귀와 구분했다. 새 preview 동작은 실제 컴포넌트의 미리보기/생성/초안 보존 테스트로 검증한다.
- 수업 명단 테스트의 `연결 교재` 문구 기대 1건도 시작 커밋에서 동일하게 실패한다. 기존 실패 총 13건의 정비는 별도 과제다.
- 브라우저 확대 단축키로 실제 200% 적용을 확인하지 못했다. 720×450의 좁은 유효 공간은 검증했지만 실제 배율 검증으로 표기하지 않는다.
- 실기기 터치, VoiceOver 전체 읽기 순서, 운영의 실제 쓰기/발송과 첫 조회 간헐 오류 원인은 검증 범위 밖이다.
- 운영 배포가 되기 전에는 사용 중인 화면에 이 변경이 적용됐다고 볼 수 없다.

## 범위 판단 기록

실행 중 판단은 다음 순서로 남겼다. 검증된 결과와 남은 비용을 함께 표시한다.

| 판단 | 이유와 남는 한계 |
|---|---|
| T0–T9의 한글 번호를 유지하고 실행 도구용 brief를 추출 | 문서 번호를 바꿀 필요가 없으며 위험은 작업 기록상의 차이뿐이다. |
| 시각 변경은 기존 디자인 계약과 브라우저로 검증 | CSS 문자열을 따라 쓰는 새 테스트를 늘리지 않았다. 모든 픽셀 회귀를 자동 감지하지는 않는다. |
| 개편된 JSX/열 구성에 맞게 옛 소스 문자열 기대 수정 | 런타임 행동 검사는 보존했다. 소스 검사만으로 시각 일치를 주장하지 않는다. |
| 기존 수업계획 소스 검사 12건 별도 유지 | 시작 커밋에서도 실패했다. 옛 테스트 정비가 남는다. |
| 동일 조건 전후 캡처는 캘린더만 주장 | 나머지는 개선 후 화면과 공용 계약 비교다. 전 메뉴 전후 픽셀 비교는 아니다. |
| 운영 캘린더 간헐 실패 원인을 단정하지 않음 | 합성 503은 복구 UI만 증명한다. 운영 원인 조사와 성능 측정이 남는다. |
| 모바일/키보드 관찰을 실기기·VoiceOver 인증으로 확대하지 않음 | 측정한 뷰포트에서만 확인했다. 실제 보조기술 검증은 별도다. |
| 운영 migration/배포/쓰기/발송은 실행하지 않음 | 이번 작업은 검토 가능한 로컬 구현이다. 운영 화면은 아직 이전 버전이다. |
| RLS는 최종 격리 DB와 기존 visibility 조건으로 확인 | 운영 실제 역할·행 분포의 읽기 비교는 배포 단계에 남는다. |
| 관계 배열의 페이지 처리를 유지하고 총원만 별도 반환 | 성능을 위해 전체 명단을 한 번에 읽지 않는다. 소비자는 총원 필드를 사용해야 한다. |
| 채용 서버 검색/API 확장은 추가하지 않음 | 기존 목록/제목/빈 상태/초점 개선 범위다. 새로운 검색 기능은 별도다. |
| 수업 명단의 옛 문구 검사 1건도 baseline으로 분리 | 정확한 시작 소스로 재현했다. 기존 실패 정비는 총 13건이다. |
| 720×450 검증을 실제 200% 확대라고 표시하지 않음 | 확대 적용을 확인할 수 없었다. 실제 배율 검증이 남는다. |
| 로컬 브랜치와 worktree를 유지 | 구현 완료 후 승인되지 않은 통합·공개를 진행하지 않았다. 푸시·PR·운영 반영은 후속 배포 작업이다. |
