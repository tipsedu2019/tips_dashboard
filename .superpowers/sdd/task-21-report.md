# 작업 21 구현 결과 보고서

상태: 운영 상태를 바꾸지 않는 알림 전환 검증기와 결정적 그림자·부분/전체 롤백 fixture를 구현했습니다. 작업 22의 전체 회귀·빌드·브라우저 게이트가 끝나기 전까지 Release F 최종 완료로 표시하지 않습니다.

구현 커밋: `1a694d1`, `40f5bf4`, `a337251`

## 읽기 전용 전환 검증

- 승인된 런타임 플래그 12개와 소유자 전환 순서 10개를 정확한 상수로 고정했습니다.
- 공통·업무 어댑터·등록 런타임 표시, worker 중단 래치, worker/watchdog 3분 heartbeat, 대기 지연과 즉시 중단 지표를 실패 폐쇄 방식으로 판정합니다.
- 기존·정규 의도를 업무, 이벤트, 발생 ID, 대상 그룹, 채널, 대상 키, 수신자 세대, 템플릿 체크섬, 정규화 렌더 해시로 비교합니다.
- `targetGeneration`은 10진 문자열인 업무 수신자 세대로만 인정하며 별도 `ownerGeneration`으로 대체하면 실패합니다.
- 검증 스크립트는 네트워크, RPC, 플래그·스케줄·DB 변경 코드를 포함하지 않습니다.

## 그림자와 소유권 fixture

- 10개 소유자 각각에 결정적 fixture 주기를 두고 기존 경로는 주입 recorder로 의도만 기록합니다.
- 정규 그림자 행은 모두 `skipped/shadow_mode`, 재실행 불가로 끝나며 외부 요청, 알림함 투영, 중복 요청은 0건이어야 합니다.
- 전환 전 단계와 부분 롤백 뒤 모든 업무에서 기존 또는 정규 소유자가 정확히 한 명인지 검증합니다.
- 그림자 중 공급자 또는 알림함 부작용이 감지된 fixture는 12개 플래그를 모두 끄고 worker 중단 래치를 올리며 그림자를 자동 복구하지 않습니다.

## 원자 롤백 fixture

- 부분 롤백은 대상 소유자만 끄고 다른 정규 소유자와 전역 그림자 꺼짐 상태를 보존합니다.
- 전체 소유자 롤백만 그림자 복구를 허용합니다.
- `pending`·`retry_wait`는 취소하고 전송 전 `claimed`에는 취소 요청을 남기며, `sending`·`sent`·`failed`·`delivery_unknown`과 공급자 참조는 보존합니다.
- 소유권 이전은 claim 종료 뒤 예약·미전송·공급자 참조 없음 상태에서만 허용합니다. 시작됨, 전송 중, 전송 완료, 공급자 참조 있음, 전달 결과 불명은 거절합니다.
- 알 수 없는/혼합 소유자, 오래된 예상 revision, 부분 롤백의 그림자 복구 요청은 실패하며 입력 플래그·전달·취소 요청·소유권이 그대로 남습니다.

## 현재 검증 결과

- 집중 Node: `11/11` 통과
- 대상 ESLint: 통과
- `git diff --check`: 통과
- 읽기 전용 건식 실행: 통과, 플래그·스케줄·공급자·DB 변경 `0건`

## 남은 최종 게이트

- 작업 22에서 전체 Node, TypeScript, 전체 ESLint, 프로덕션 빌드, 정확한 경로별 브라우저/provider-zero 검증을 다시 실행합니다.
- 승인된 local/preview DB가 없어 실제 원자 롤백 RPC와 pgTAP은 아직 실행하지 않았습니다.
- 7일 운영 그림자, 실제 소유자 전환, 운영 플래그 변경, 스케줄 설치는 별도 승인 전에는 실행하지 않습니다.
