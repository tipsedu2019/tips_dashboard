# SDD ledger — plan: docs/superpowers/plans/2026-08-13-dashboard-query-surface-optimization.md

Execution branch: codex/dashboard-free-tier-optimization
Base: fad56ae59f6b5ec6999e3232bbe68e4c1d26b101
Prior plan commits: 7e364c1b..c820e853

Task 1: complete (commits a7813801..ff97d7e8; adversarial review fix rounds 1-11 complete; focused 59/59, CI/worktree verifier, ESLint, and diff check green)

Task 1: `a7813801` established the query-surface budget verifier and cursor contracts.
Task 1 fix round 1: enforce manifest-listed symbols, resolve local query constants, and validate decoded cursor scopes.
Task 1 fix round 2: fail closed for opaque list projection, limit, RPC, and computed query-entry expressions while preserving statically resolved bounded queries.
Task 1 fix round 3: compare exact debt occurrences, inspect no-limit and optional computed query entrypoints, and reject ambiguous canonical cursor-scope inputs.
Task 1 fix round 4: enforce contracts per direct query chain, bind each baseline-debt exception to an exact recorded-SHA fingerprint, retain only explicit single/range list exemptions, and reject mixed/whitespace wildcard projections.
Task 1 fix round 5: replace regex extraction with TypeScript AST receiver/alias analysis, compare changed legacy files against exact caller-baseline deltas, refresh baseline fingerprints, and preserve nested PostgREST projections.
Task 1 fix round 6: make the exact debt manifest the sole allowance for touched query chains, add structural timeout/order/exact-detail contracts, and reject recursive nested projection wildcards without broadening the legacy ledger.
Task 1 fix round 7: recheck all chains in deletion- or dependency-affected functions, bind manifest debt to both historical and caller baselines, enforce root-only deterministic list controls, and use effective final abort/retry settings.
Task 1 fix round 8: fail closed for uncertain relation options and local values, model shared Supabase builders as immutable request branches, reject RPC spreads and arbitrary task-ID fan-out, and tighten detail-key validity.
Task 1 fix round 9: propagate conditional builder provenance, resolve constants/options at their lexical use site with object-mutation tracking, inspect bound Supabase methods, and prohibit all task-ID `.in` fan-out.
Task 1 fix round 10: connect direct builder aliases and transitive bound query aliases to the same request graph, inspect bound builder operations, and fail closed for resolved or unknown tasks `.in` columns.
Task 1 fix round 11: propagate immutable aliases of bound builder operations, so task-ID `.in` remains visible through `filter -> run` assignments.

Task 2: source-complete / DB-runtime-blocked. Added 30+1 keyset task pages, separate same-filter stats, compact subtype rows, exact selected-detail reads, append/deep-link workspace behavior, and the tasks query-guard regression. Focused source suites are 418/418 GREEN; TypeScript, tasks query guard, local schema lint, candidate hash, and diff check are GREEN. The isolated harness stopped before allocation with `isolated_supabase_db_baseline_review_required`, so the migration remains candidate and no pgTAP/EXPLAIN runtime GREEN is claimed. See `task-2-report.md`.

Task 2 adversarial fix round 1: reviewer findings are source-complete. Non-registration catalogs are lazy on editor/filter open; registration membership/search/owner/order use matching subject-track summaries; paged UI uses authoritative sibling stats/facets; membership/order-changing mutations refetch active-filter page one with stale append protection; task-only off-page registration links open canonical case detail. RED was 135 pass / 6 fail; final focused suites are 424/424 GREEN, exact service/workspace is 141/141 GREEN, TypeScript/query guard/diff checks pass, and candidate hash is `f4efe37189ad885a10d54dc4a979db6f92c62545f6969ab4d98be2b637b54a87`. DB runtime remains reviewed-baseline blocked and unclaimed.

Task 2 adversarial fix round 2: page-one replacement now preserves loaded non-registration catalogs; create and management withdrawal handoff await catalogs before defaults/prefill; continuation pages skip stats RPC and preserve first-page stats; registration subject search is constrained to the selected workflow/owner; general assignee facets include secondary assignments and word-retest manual names have exact filter values. RED was 141 pass / 6 fail; exact service/workspace is 147/147 and five-suite focused is 430/430 GREEN. TypeScript/query guard/ESLint/local schema lint/diff checks pass. Candidate hash is `afa89fe970b5c367f05e67e5b76e38dafa96a7f233001fce019db940cb7082a1`; isolated DB remains reviewed-baseline blocked and runtime pgTAP/EXPLAIN is unclaimed.

Task 3 initial implementation: source-complete / DB-runtime-blocked. Added 30+1 Korean-numeric keyset management pages, same-filter authoritative stats and faceted options, exact selected details, bounded opaque-cursor relations, selection-driven catalog/picker reads, canonical URL filters, append/dedupe/deep-link UI, and management query-guard coverage. The initial focused suites were 118/118 and the initial full management glob was 126/126 GREEN. The isolated harness stopped before allocation with `isolated_supabase_db_baseline_review_required`, so no pgTAP/EXPLAIN runtime GREEN was claimed. See `task-3-report.md`; the current candidate is recorded in the latest fix-round entry below.

Task 3 adversarial fix round 1: assigned textbook IDs are save-safe; invoker RPCs use inline exact validation with an authenticated-role pgTAP source contract; enrollment relations merge canonical forward/reverse sources; visible list fields and roster counts are complete; textbook list/stat/options avoid `lessons`; search/default-period state is canonical and debounced; mutations reconcile page one/stats/options; relation continuation is appended in detail UI; and roster fallbacks use exact-ID projections plus array-only writes. RED was 0/11; final review is 11/11, full management is 137/137, and combined focused management/query/textbook verification is 185/185 GREEN. TypeScript, management query guard, ESLint, candidate hash, and diff checks pass. Candidate SHA-256 is `cf7b8c6ca321bcb0104315e8a8f949c961e0288907ccb99c0863e7adef476e12`. Isolated DB remains reviewed-baseline blocked and runtime pgTAP/EXPLAIN is unclaimed.

Task 3 adversarial fix round 2: authenticated class detail uses the approved science-area RPC boundary; assigned textbook IDs remain independent from joined rows; a dedicated 30+1 query-and-filter-bound textbook candidate picker supports first/additional selection and continuation; and the server default class period is resolved before the first list/stats/options bundle. RED was 8/2; the progressive suite is 10/10, full management is 139/139, and combined management/query-budget/textbook-picker/schema verification is 205/205 GREEN. TypeScript, management query guard, ESLint, candidate hash, and diff checks pass. Candidate SHA-256 is `e5d5f408be3de36e11be6616bfcb66049e264247d959131d87651fe26de4aacf`. No round-2 DB runtime command was run; migration replay, pgTAP runtime, and EXPLAIN remain unclaimed.

Task 3 adversarial fix round 3: bounded class/textbook list, stats, option, and candidate sources now use explicit scalar projections with no whole-row conversion, `schedule_plan`, or `lessons`; equivalent default-period URL normalization consumes one short-lived canonical result rather than issuing a second heavy bundle; and the textbook picker query is parent-controlled across input, results, and cursor scope. RED was 19/3; focused is 22/22, full management is 140/140, and combined management/query-budget/textbook-picker/schema verification is 206/206 GREEN. TypeScript, management query guard, ESLint, candidate hash, and diff checks pass. Candidate SHA-256 is `f3e3ff36b6d0ae0de232910f91fe07b7441eff86d3af4f5e4189358286265c81`. DB runtime, pgTAP runtime, and EXPLAIN remain unexecuted and unclaimed.

Task 3 adversarial fix round 4: bounded student/enrollment branches now use direct scalars with no whole-row conversions; default-period canonicalization uses an exact one-use replay token plus concurrent in-flight coalescing rather than a time cache, while explicit refresh always re-queries; and picker scope changes synchronously hide old rows and continuation until a matching committed result arrives. RED was 19/3; focused is 22/22, full management is 140/140, and combined management/query-budget/textbook-picker/schema verification is 206/206 GREEN. TypeScript, management query guard, ESLint, candidate hash, and diff checks pass. Candidate SHA-256 is `1508a71f54472893935e0921a656ec80d6fac5429cae5800b8ce2396dbd04fac`. DB runtime, pgTAP runtime, and EXPLAIN remain unexecuted and unclaimed.

Task 6: implemented the public-class 600-second last-good summary cache, persistent Next Data Cache fixture proof across a clean process restart, explicit compatibility projections, cache-control header contract, targeted authenticated invalidation, and post-commit class/textbook/schedule refresh handoffs. RED was missing cache modules plus the old cache header; focused verification is 20/20 GREEN and TypeScript/diff checks pass. The public query-surface verifier remains RED because it applies bounded list order/limit requirements to the deliberately unbounded legacy full compatibility API and the invalidation-route role RPC; that concern is recorded in `task-6-report.md` and remains unclaimed. No DB runtime, migration, deployment, provider, or production mutation ran.

Task 3 adversarial fix round 5: bounded detail-relation branches now use direct scalar/needed JSON fields with no whole-row conversion or heavyweight fields; explicit refresh and mutation revoke overlapping passive initial bundles and execute fresh list/stats/options, while late passive responses cannot install a replay token and passive concurrent identical calls still coalesce. RED was 21/2; focused is 23/23, full management is 141/141, and combined management/query-budget/textbook-picker/schema verification is 207/207 GREEN. TypeScript, management query guard, ESLint, candidate hash, and diff checks pass. Candidate SHA-256 is `44dff0d605127f5cd4efcd954f9bb324e846b60dc5d956755e6aa63b67f1dcc5`. DB runtime, pgTAP runtime, and EXPLAIN remain unexecuted and unclaimed.

Task 3 adversarial fix round 6: student reverse lookup and class waitlist paging explicitly combine current and legacy waitlist arrays; relation SQL and the direct picker project class teacher/classroom aliases as scalars and normalize them to the same renderer contract. The new review suite first failed at module load because the canonical normalizer did not exist; focused is 24/24 and combined management/query-budget/textbook-picker/schema verification is 208/208 GREEN. TypeScript, management query guard, ESLint, candidate hash, and diff checks pass. Candidate SHA-256 is `39ec7be093a24ee143d57ee4a4f7da5bc1b38630c0899669ad66900526c5cb50`. DB runtime, pgTAP runtime, and EXPLAIN remain unexecuted and unclaimed.

Task 3 adversarial fix round 7: relation-record aliases are trimmed before selection, so whitespace-only `teacher_name`/`classroom` values fall back to populated `teacher`/`room` scalars. RED was 12/1; focused is 24/24 and combined management/query-budget/textbook-picker/schema verification is 208/208 GREEN. TypeScript, management query guard, ESLint, unchanged candidate hash, and diff checks pass. Candidate SHA-256 remains `39ec7be093a24ee143d57ee4a4f7da5bc1b38630c0899669ad66900526c5cb50`. DB runtime, pgTAP runtime, and EXPLAIN remain unexecuted and unclaimed.

Task 4 initial implementation: source-complete / DB-runtime-blocked. Replaced the operations 17-table initial fan-out with calendar visible-range, selected-year annual board, and server-filtered 30+1 class-schedule reads; added all-or-nothing density guards, exact selected event/class detail boundaries, bounded actor-scoped catalogs, stable eight-second/no-retry reads, previous-success preservation, and mode-specific continuation. RED was 0/8 plus 0/1 visible-range; focused is 38/38 and query-budget regression plus service is 64/64 GREEN. TypeScript, operations query guard, relevant ESLint, Task 4 migration lexical normalization, candidate hash, and diff checks pass. Candidate SHA-256 is `266603c7b020c4e2b2681287f0f7bd63bd9ae411451b26778dd22a80a4abd87d`. The isolated harness stopped before allocation with `isolated_supabase_db_baseline_review_required`, so pgTAP/runtime GREEN is unclaimed and the manifest remains candidate. Repository-wide migration layout is separately blocked by the pre-existing Task 3 candidate normalization finding. See `task-4-report.md`.

Task 4 adversarial fix round 1: exact event metadata is lossless through calendar and annual read-save; derived annual entries use synthetic IDs plus exact parent edit IDs, renderer-ready exam terms, distinct renderer rows for comma-grade expansion; off-page lesson-design links hydrate exact legacy plan/textbooks/catalogs before opening; stale continuation append/error is rejected by revision and fingerprint; and dense recovery renders a real seven-day agenda while preserving the last valid month. RED was 6/8, then exact-wire metadata 0/1 and annual read-save 0/1; focused is 112/112 and exact operations/query-budget is 70/70 GREEN. TypeScript, targeted ESLint, operations worktree query guard, and diff checks pass. Candidate SHA-256 is `b45e5a2d49ebe42780f08ced9d8a5094581c7f8cbef30538040607d5cde21940`. No fix-round DB runtime command ran; pgTAP/EXPLAIN/runtime remain unclaimed. Whole-repo failures remain isolated to unrelated notification fixture/golden drift and the pre-existing Task 3 migration normalization gate.

Task 4 adversarial fix round 2: legacy scalar and structured textbook scopes now roundtrip together through exact event detail and form save; calendar/annual exact-detail failures stay closed with explicit retry; annual derived subject terms use the original parent title; and lesson-design searches a separate bounded 30+1 subject textbook candidate page while preserving exact connected legacy books and keeping `schedule_plan` out of the initial list. RED was 0/5; the new regressions are 5/5 and the Task 4 focused suite is 50/50 GREEN. TypeScript, targeted ESLint, operations query guard, manifest hash, and diff checks pass. Candidate SHA-256 is `47d6af4718be42c95d402fe183a229d9709bc50e3523466d34a7b19b4a2cf67c`. No DB runtime command ran; pgTAP/EXPLAIN/runtime remain unclaimed. The repository-wide glob remains non-GREEN only in unrelated shared notification, Task 3 migration-normalization, and concurrent isolated-DB paths.

Task 4 adversarial fix round 3: the calendar exact-detail adapter now carries scalar and structured textbook scopes through read-form-save; calendar and annual exact detail use revision plus exact identity guards so stale success, failure, and loading completion cannot overwrite the latest selection; and deep-linked calendar requests share the same guard. RED was 0/4; new regressions are 4/4 and Task 4 focused is 54/54 GREEN. TypeScript, targeted ESLint, operations query guard, unchanged candidate hash, and diff checks pass. Candidate SHA-256 remains `47d6af4718be42c95d402fe183a229d9709bc50e3523466d34a7b19b4a2cf67c`. No DB runtime command ran; pgTAP/EXPLAIN/runtime remain unclaimed. The repository-wide glob remains non-GREEN in 17 unrelated notification and Task 3 migration-normalization checks.

Task 4 adversarial fix round 4: hidden EventForm scope inputs now retain existing scalar and structured metadata through the actual save output; calendar date/range/overflow/filter/new-event contexts and annual year/category/semester/school/search-param/reset contexts revoke pending exact-detail revision and identity before switching context. RED was 0/3; new regressions are 3/3, Task 4 focused is 57/57, and exact operations/query-budget is 76/76 GREEN. TypeScript, targeted ESLint, operations query guard, unchanged candidate hash, and diff checks pass. Candidate SHA-256 remains `47d6af4718be42c95d402fe183a229d9709bc50e3523466d34a7b19b4a2cf67c`. No DB runtime command ran; pgTAP/EXPLAIN/runtime remain unclaimed. The repository-wide glob did not terminate in unrelated provider-zero/notification adapter test handles and was stopped without external effects.

Task 4 adversarial fix round 5: calendar URL date/event changes and removals, URL-event effect cleanup, and successful event drops now revoke pending exact-detail revision and identity before applying the new context; removing the event parameter clears its applied identity for later re-entry. RED was 0/1; new regression is 1/1 and Task 4 focused is 58/58 GREEN. TypeScript, targeted ESLint, operations query guard, unchanged candidate hash, and diff checks pass. Candidate SHA-256 remains `47d6af4718be42c95d402fe183a229d9709bc50e3523466d34a7b19b4a2cf67c`. No DB runtime command ran; pgTAP/EXPLAIN/runtime remain unclaimed.

Task 5 adversarial fix round 2: null-period curriculum continuation is pinned to the first response's resolved default period and actor-bound cursor scope; no-session CTA/classification precedes textbook work while unlinked counts remain independent; next-unplanned identity accepts both `session_id` and `progress_key`; and curriculum only renders data whose successful and display fingerprints match the current request. RED was 23/6; targeted is 29/29 and five-suite focused is 95/95 GREEN. TypeScript, academic query guard, candidate hash, and diff checks pass. Candidate SHA-256 is `e62b0c3f49782536eef6ee7519faa27876f3f19546ac022c3b5cb4eea6fc3914`. No DB runtime command ran; pgTAP/EXPLAIN/runtime remain unclaimed.

Task 5 adversarial fix round 3: academic execution/data fingerprints now contain authenticated user ID plus the resolved AuthProvider dashboard role; actor changes synchronously hide last-good data and execution changes hide density/error/continuation while late prior-actor completions are rejected. Timetable support collections now return only explicit consumed scalar fields with no whole-row conversion. RED was 28/3, targeted is 31/31, and five-suite focused is 97/97 GREEN. TypeScript, targeted ESLint, academic query guard, candidate hash, and diff checks pass. Candidate SHA-256 is `1c4ebacc75debf9d156f75d241f73ddb2c1789379043cd4ce59d6fa16c02e7ae`. DB runtime, pgTAP runtime, and EXPLAIN remain unexecuted and unclaimed.

Task 4 final review remediation: annual list now keeps full embedded event metadata exact-detail-only while emitting bounded display/material sections and material-backed fallback subject rows; calendar/annual use the bounded authenticated school catalog for first empty-scope events; and lesson-detail mutations patch selected detail plus visible schedule range, conditionally refreshing the 30+1 list only when its displayed summary changes. RED was 0/4; new regression is 4/4, Task 4 focused is 63/63, and exact operations/query-budget is 80/80 GREEN. TypeScript, targeted ESLint, operations query guard, candidate hash, and diff checks pass. Candidate SHA-256 is `0fadb63a8733b8a44c1940c20af553a0f2874b854250d0a05c9203a51a85d623`. No DB runtime, pgTAP runtime, or EXPLAIN ran; all remain unclaimed.
