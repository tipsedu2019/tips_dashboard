# 학생관리 대표 화면과 공통 데이터테이블 검수

아래 3단계 기록은 당시의 650px 대표 화면 기준이다. 현재 구현과 판정은 문서 끝의 4단계 후속 검수에 기록했다.

## 대상과 범위

- Source visual truth: `.codex-artifacts/apple-design-phase1/option-3.png`, 1487 × 1058px. 사용자가 실제 표시 순서 3번을 선택했다.
- 구현: `http://127.0.0.1:3137/admin/students`. 격리 Next fixture에서 실제 ManagementPage, ManagementDataTable, AppSidebar, SiteHeader를 가져온다. 인증·데이터·서비스 경계는 합성 데이터로 대체했다.
- 비교: `http://127.0.0.1:3138/compare.html`. 전체 이미지를 동일 47% 배율로 나란히 표시하고, 편집 영역을 원본 픽셀 1:1로 비교했다. 마지막 로고 보완 후에는 원본과 최종 전체 캡처를 같은 검수 입력에 함께 열어 재확인했다.
- 데스크톱 실제 측정: 1487 × 1058 CSS px, DPR 1. 상세 시트 650 × 860, x546.5 / y99. 모바일 실제 측정: 390 × 844 및 320 × 720.
- 동일 업무 상태: 김학생 / S-001 / 제주중 / 중2 / 재원, 연락처 예시, 학생명 포커스. 목록은 양쪽 모두 8행이다.

## 보존에 따른 의도적 차이

- 실제 상세는 8필드이며 등록일을 편집하지 않는다. 신규 등록에는 등록일을 포함한 9필드를 유지했다.
- 상세의 수강·대기·수업 및 교재 이력을 연속 스크롤로 보존했다. 이 때문에 시안보다 상세 시트가 길며 저장·닫기는 고정 푸터로 접근한다.
- 실제 제목은 `김학생 학생정보`, 학교 구분은 기존 값 `중등`을 유지했다. 퇴원 처리는 기존 업무 경로를 사용한다.
- 현재 메뉴·경로·권한에 따른 노출, 전역 빠른 이동과 학생 검색의 구분은 유지했다. 시안의 제안 메뉴 묶음은 아직 적용하지 않았다.
- 체크박스의 일괄 선택과 상세 열기는 기존대로 별도 동작이다. 상세를 열었다는 이유로 체크박스를 선택하지 않는다.

## 발견과 수정

| 우선순위 | 발견 | 수정 및 재확인 |
| --- | --- | --- |
| P2 | 배경을 흰색으로 과하게 덮어 주변 맥락이 흐려짐 | 학생 overlay를 foreground 10%로 변경, 최종 전체 비교 |
| P2 | 학생 상세의 `삭제` 명칭이 실제 퇴원 처리와 불일치 | `퇴원 처리`로 교정, 브라우저에서 확인 |
| P2 | Escape/닫기 후 목록 포커스가 BODY로 빠짐 | 연결된 opener 또는 재렌더링된 동일 학생 버튼으로 복귀. 실제 Escape 후 student-001 버튼 포커스와 Enter 재진입 확인 |
| P2 | 모션 감소 규칙이 닫힘 상태를 누락 | open/closed 모두 animation none 적용 |
| P2 | 다크 모드에서 브랜드와 날짜 아이콘 대비 부족 | 브랜드의 기존 흰 표면 복원, 날짜 input color-scheme 적용, 다크 등록 화면 재검수 |
| P3 | 로고 원본의 내부 여백으로 시각적 크기가 작음 | 이미지 표시 크기와 컨테이너 여백 조정, 최종 전체 비교 |

가상 hook의 불안정 참조로 인한 최초 hydration 오류 및 empty fixture의 건수 불일치는 fixture에서 수정했다. 이를 제품 로직 결함으로 기록하거나 제품 서비스 수정으로 우회하지 않았다.

## 시각 검사

- 글꼴: 실제 computed font가 기존 Pretendard 및 한글 fallback, 필드 15px. 제목 22px, 라벨 13px, 표 셀 14px. 모바일 필드는 16px이다.
- 간격·배치: 650px 시트, 기본 필드 높이 42px와 모바일 44px, 그룹 간 구분선, 일관된 필드 간격. 기본·학교·연락처의 관계가 정렬로 드러난다.
- 색·표면: 기존 semantic theme 유지, 흰 편집 표면과 절제된 그림자, primary 저장 / outline 닫기 / ghost 퇴원 처리. 다크 테마도 별도 확인했다.
- 로고·아이콘: 실제 TIPS 로고와 기존 Lucide를 유지했다. 이미지 속 가짜 컨트롤이나 Apple 자산으로 대체하지 않았다.
- 내용: 필수 필드, 수강·대기와 이력, 상태·행동 명칭을 실제 UI와 대조했다.
- 390px 및 320px: 문서 가로 넘침 없음. 320px 상세 너비304px, x8px. 끝까지 이동해 교재 이력과 푸터에 접근했다. 연락처 포커스가 고정 푸터 아래에 가려지지 않았다.
- 남은 P0/P1/P2 시각 결함: 없음. 사용자 취향 검수와 전체 화면 확장은 별도 단계다.

## 기능·키보드·상태 증거

- 학생 검색 `김학생` → 1행 → 상세 → 이름 변경 → 합성 저장 실패. 오류 안내와 변경 입력을 유지했다.
- 동일 입력으로 재시도 → 저장 완료 및 목록 반영 → 닫기 → 재진입 확인.
- 1.8초 지연 저장에 더블클릭: 저장 시도는 2에서3으로 한 번만 증가, 동시 최대1. 저장 중 푸터와 관계 변경 버튼 비활성화.
- Tab으로 닫기 아이콘에서 학생명으로 이동, Escape로 상세 닫기, 동일 학생 버튼으로 포커스 복귀 및 Enter 재진입.
- 학교 구분 메뉴를 열고 Escape로 닫음. 실제 select 애니메이션 시간120ms 확인.
- 읽기 전용: 8개 상세 필드, 관계 변경, 저장·퇴원 처리 비활성; 닫기 가능.
- 빈 목록: 학생 없음과 0건. 로딩: skeleton 및 `학생 데이터를 불러오는 중입니다.`, 건수 확인 중. 오류: 기존 목록과 다시 시도 버튼.
- 신규 등록의 9필드·취소, 다크 모드 확인. 실제 신규 운영 데이터는 만들지 않았다.
- fixture 수정 후 최종 IAB 탭의 브라우저 error log: 0개.

## 모션

- 실제 computed 시트 진입: 180ms, cubic-bezier(0.2, 0, 0, 1). 키프레임 이동8px, 확대 없음.
- 메뉴120ms, 색·테두리 반응120ms. focus ring은 지연하지 않는다. 닫힌 Dialog는 기존 계약대로 포인터를 즉시 반환한다.
- 이 수치는 TIPS 제안값이며 Apple 앱을 프레임 단위로 측정한 수치가 아니다. 공식 HIG의 목적·접근성 원칙과 비교했다.
- reduced-motion: 별도 Chrome DevTools Rendering에서 실제 `prefers-reduced-motion: reduce` 선택을 AX로 확인했다. 학생 시트 Computed는 `animation-name: none`, `animation-duration: 0s`, `animation-fill-mode: none`. Escape 닫기와 김학생 행 포커스 복귀도 확인했다. 메뉴는 활성 reduced CSS selector 확인까지 수행했으며, reduce 상태 메뉴 자체의 Computed 확인은 도구 클릭 시간 초과로 수행하지 못했다. 검수 후 No emulation으로 원복하고 DevTools를 닫았다. OS 전역 설정은 변경하지 않았다. 상세 기록은 `.codex-artifacts/apple-design-phase3/reduced-motion-check.md`에 있다.

## 검사와 증거 파일

- 기존 대상 회귀 검사: 136 passed / 0 failed. `.codex-artifacts/apple-design-phase3/student-regression.log`
- 최종 TypeScript no-emit, 변경 TSX ESLint, git diff --check: 통과.
- 실제 Next production build 통과: `.codex-artifacts/apple-design-phase1/student-build.log`. 이후 접근성 포커스·CSS 보완은 TypeScript/ESLint/대상 회귀 검사와 브라우저로 검증했다.
- 전체: `final-desktop.png`, `comparison-full.png` (로고 마지막 보완 이전 비교 캡처; compare.html은 최종 이미지를 사용).
- 집중: `comparison-focused.png` (최종 필드 레이아웃과 동일).
- 모바일: `mobile-390-top.png`, `mobile-390-history.png`, `mobile-320-history.png`.
- 상태: `state-empty.png`, `state-loading.png`, `state-error.png`, `state-readonly.png`, `dark-create.png`.
- 위 이미지 파일은 `.codex-artifacts/apple-design-phase3/`에 있다. 운영 개인정보 원본은 포함하지 않았다.

## 검증 경계

합성 서비스의 UI 조작 증거이며 운영 DB 저장·인증/RLS·알림 전달 증거가 아니다. 서비스·API·권한·출력 코드는 수정하지 않았다. 학생관리 대표 화면에는 출력 기능이 없어 출력 비교는 수행하지 않았다. 다른 화면의 교재 PNG/PDF 등 출력 비교는 확장 단계에 남아 있다. 푸시·병합·배포를 하지 않았다. 1024px 및 우측 사이드바 배치는 추가 런타임 증거로 주장하지 않는다.

## 4단계 후속 검수 · 관계 너비와 공통 표

사용자가 학생정보의 수업 연결·수업교재 이력 너비 보완과 전체에서 재사용할 공통 데이터테이블 재설계를 요청했다. 선택한 3번의 구조를 유지하며 다음 변경을 의도적으로 적용했다.

| 영역 | 최종 구현 |
| --- | --- |
| 학생정보 | 최대 폭920px. 1487×1058에서 x411.5/y99, 920×860. 기본8필드와 연속 상세 유지 |
| 수업 연결 | 수강·대기 목록 각각 전체 폭. 긴 제목 줄바꿈, 데스크톱 행동 우측/모바일 다음 줄 |
| 수업·교재 이력 | 두 이력을 각각 전체 폭으로 배치. 제목·과목·교사·교재 부가 정보 줄바꿈. 기존 조회/표시 건수 계약 유지 |
| 수업 선택 | 긴 후보와 선택된 라벨 전체 표시, 모바일 폭 내에서 높이 확장 |
| 관리 표 | 공통 외곽 틀과 내부 구분선, 헤더44px/행 최소48px/좌우12px, 긴 제목 줄바꿈, 불투명 고정 열, 동일 선택·hover·정렬 표현 |
| 설정 표 | 공통 toolbar/viewport 및 header/cell 토큰. 설정별 입력·상태·action 계약 유지 |

### 브라우저 증거

- 1082px: 관계 행679px, scrollWidth679px. 이력 두 항목도 같은 폭이며 가로 넘침 없음. 넓이가 부족해 짧은 수업명까지 생략되던 상태를 해소했다.
- 390×844: 관계·이력 행315px, scrollWidth315px. 해당 제목 span의 ellipsis 없음. 긴 수업 선택 후 trigger 높이116px, label width/scrollWidth 모두241px, white-space normal, text-overflow clip.
- 학생 목록28행 fixture: 10행→2페이지11–20, 페이지 이동 후 선택0건. 15행 선택→1페이지/15행. 이름 너비 입력 변경과 Enter 초기화, 검색 `김학생`→1건 확인.
- fixture의 정렬 어댑터 보완 후 실제 이름 오름차순 첫3명 `강도윤/김학생/박서준`, 내림차순 `한소율/최민준/정유나` 및 aria-sort 변화 확인. 초기 fixture가 행 순서를 반영하지 않은 결과는 제품 검증 근거로 사용하지 않았다.
- 수업의 긴 현재학기 제목1건과 교재의 긴 제목1건을 검색하고 그룹을 펼쳐 전체 제목을 확인했다. 길이에 따라 행은144px/142.5px로 확장되며 제목이 생략되지 않는다.
- 수업 표의 가로 키보드 이동 scrollLeft40, 선택된 고정 열은 동일 불투명 accent 색. 설정 표는 모바일 frame341px/내용1180px, 키보드 가로 이동40px, 오른쪽 작업 열 sticky/불투명 및 입력값 유지 확인. 문서 전체가 가로로 넘치지 않는다.
- 학생 표 라이트/다크 테마 확인, 최종 탭 error log0. 검수 후 라이트 테마와 자연 화면 크기1082×987로 복원했다.
- 대표 화면 전후를 같은1487×1058 입력에 함께 열어 비교했다(`phase3/final-desktop.png`와 `phase4/final-student-desktop.png`). 동일 학생·8행·학생명 포커스 상태이며, 배경 행 순서는 fixture의 정렬 반영 보완으로 다르다. 넓어진 시트와 공통 목록 틀은 사용자 요청에 따른 차이다.
- 공통 표의 row/sort 색 전환 computed150ms. 공통 primitive에 motion-reduce:transition-none이 있고, 학생 시트의 기존180ms/8px 및 reduced-motion 규칙을 유지했다. 이번 후속에서는 OS/브라우저 reduced-motion 설정을 다시 변경하지 않았다. 앞선 3단계의 실제 reduce 검증과 구분한다.

### 검사·검토

- `table-regression.log`: 99 passed / 0 failed (표 primitive, pagination, page size, 학생 toolbar, filter transition, numbered pagination, progressive loading).
- `related-settings-tests.log`: 68 passed / 0 failed (관계·이력, 수업 명단, 검색 combobox, 학교 설정). 합계167개이며, 독립 검토의 추가63개는 중복이라 더하지 않았다.
- 제품 TypeScript no-emit, 변경 TSX/test ESLint, Next production build, git diff --check 통과. 로그는 `phase4/typecheck.log`, `eslint.log`, `build.log`.
- 독립 소스 검토: P1/P2 미발견. 상태·서비스·권한 소유권, 저장키/URL/선택 범위, overflow owner와 sticky, 키보드/포커스 복귀 계약을 확인했다.
- 별도 fixture 자체의 TypeScript 실행에는 Supabase mock의 unknown 반환형에 따른 기존 hook 타입 오류6개가 있다. fixture의 독립 clean type gate로 주장하지 않는다. 실제 제품 tsc/build 결과와 구분한다.

### 증거·범위

이미지는 `.codex-artifacts/apple-design-phase4/`에 있다: `student-relations-after.png`, `long-relations-desktop.png`, `long-relations-mobile.png`, `long-history-mobile.png`, `long-picker-mobile.png`, `long-picker-selected-mobile.png`, `table-classes-desktop.png`, `table-textbooks-desktop.png`, `table-textbooks-mobile.png`, `table-settings-desktop.png`, `table-settings-mobile.png`, `final-student-desktop.png`, `final-table-students.png`, `final-table-dark.png`.

관리3종 목록과 기존 SettingsTableFrame/Header 소비 화면에 적용됐다. 과목 설정은 header만 사용하며, 전용 달력·일정·교재 운영 행렬에는 일괄 강제 적용하지 않았다. 공통 API와 사용 지침으로 후속 화면에서 재사용할 수 있다. 운영 DB·인증·전송을 사용한 검증이나 전체 설정 업무의 종단간 검증은 수행하지 않았다. 출력·API·서비스·권한 코드는 변경하지 않았으며 배포하지 않았다.

최종 잔여 P0/P1/P2 시각·구조 결함: 발견되지 않음. 사용자 검수 가능한 로컬 상태다.

## 5단계 후속 검수 · 표 설정 패널

- 사용자 요청: 우측 상단 그룹화·정렬·컬럼 구성을 기존 Apple 계열 공통 표와 일관되게 정돈.
- 폭340→440px, 중복 카드 테두리 제거, 두 그룹 기준 및 각 정렬의 컬럼/방향을 행 단위로 배치. 컬럼 표시·이름·숫자 너비·순서를 공통 grid에 정렬했다. 기본값 복원은 전체 설정에 적용되는 의미에 맞춰 고정 footer로 이동했다.
- `DataTableSettings`/Section/Select/ColumnSetting 표현 컴포넌트를 공통으로 추출했다. 기존 grouping/sorting builder, 컬럼72–420 너비 정규화, visibility/order, resetPreferences와 저장 효과는 유지했다.
- 화면 밖으로 내려가던 팝오버를 Radix available-height 안으로 제한하고 본문만 스크롤한다. 첫 포커스는 상단 닫기 버튼, 완료·Escape 후 표 설정 trigger로 복귀한다. select와 체크박스·너비·순서 버튼에 명시적인 접근 가능한 이름을 부여했다.
- 학생·교재는 desktop header/mobile toolbar 중 한 위치, 수업은 toolbar에 공통 panel을 렌더한다. 수업 표에는 기존 action 열이 없어 접근할 수 없던 설정을 toolbar로 연결했다.
- 같은1357×987/학생8명/기존 컬럼 순서·너비·재원 상태 내림차순에서 전후 이미지를 함께 열어 비교했다. 이전 패널 bottom1022.6px, 이후974.9px로 화면 안에 들어온다. 이전 캡처는 사용자가 열어 스크롤한 상태이며 새 패널은 상단부터 접근한다.
- 실제390×844: 패널x12/y212,366×620, clientWidth=scrollWidth364. 마지막 학부모 연락처 너비를 클릭해 본문 스크롤과 footer 접근을 확인했다. 모바일에서 설정 trigger는1개다.
- 기능: 학교→학년2단 그룹(중복 학교 선택 제외), 이름 오름차순, 연락처 숨김, 이름 너비176, 컬럼 이동, 결과 없는 컬럼 검색과 지우기를 조작했다. 새로고침 후 전부 유지됨을 확인했다. 기본값 복원 뒤에는 검수 전 사용자 값(이름111, 재원 상태 위치/내림차순, 두번째 정렬없음, 모든 컬럼표시, 그룹없음)을 다시 적용했다.
- 다크 테마에서 발견한 흰 스크롤바 표면은 panel의 color-scheme dark로 수정하고 실제 computed 값과 캡처를 재확인했다. 검수 후 라이트 테마/자연 화면 크기로 원복했다.
- 대상 검사82/82, 제품 TypeScript/ESLint/Next production build 통과. JSDOM에 없는 matchMedia는 기존 실제 표 테스트의 데스크톱 환경 stub으로 추가했으며 제품 hook을 테스트 회피용으로 변경하지 않았다. 마지막 color-scheme 클래스 보완은 build 이후 브라우저로 확인했다.
- 독립 소스 검토에서 새 P1/P2 미발견. 최종 브라우저 error log0. 기존 API·권한·출력·DB·발송을 변경하지 않았다. 이번 UI 검수는 로컬 합성 데이터 기준이다.
- 증거: `.codex-artifacts/apple-design-phase5/settings-before.png`, `settings-after.png`, `settings-mobile.png`, `settings-mobile-columns.png`, `settings-classes.png`, `settings-dark.png`, `regression.log`, `typecheck.log`, `eslint.log`, `build.log`.


## 6단계 · 승인한 네 가지 후속 정돈

- 학생 행의 반복된 빨간 아이콘을 중립적인 더보기 버튼으로 바꾸고 명시적인 `퇴원 처리` 메뉴에 기존 콜백을 연결했다. 데스크톱과 모바일이 같은 StudentRowActions를 사용하며, 권한이 없어 콜백이 없으면 버튼도 비활성화된다.
- 학생 상세 8개/등록 9개 필드를 학생명·재원상태·UID, 학교구분·학교·학년, 학생·학부모 연락처(등록에서만 등록일) 순서로 정렬했다. DOM 순서도 동일하며 container 폭에 따라 3/2/1열로 바뀐다. 필드를 숨기거나 저장 payload를 변경하지 않았다.
- 학생·수업·교재 표 설정은 검색·등록 도구 줄의 오른쪽에 한 번만 렌더한다. 학교/수업 빠른 필터는 내용에 맞는 폭으로 줄였고, 교재 검색을 첫 번째에 맞췄다. 기존 URL·저장된 컬럼 설정·정렬·그룹 상태는 유지했다.
- DataTableSelectionCheckbox는 16px 표시와 40px label 클릭 영역을 공통 제공한다. 학생의 기본 재원·수강 표시는 중립색이며 대기·퇴원 구분은 유지한다. 수강·대기 요약 팝오버 기능도 유지한다.
- 추가 시각 보정: 좁은 그룹 열에서 제목이 건수 배지에 밀리지 않도록 배지를 제목의 인라인 흐름에 배치했다. 새 메뉴의 다크 테마 글자 대비를 밝게 보정하고 150ms 전환 및 reduced-motion 무애니메이션 클래스를 적용했다.

### 검사 결과

- 최종 관련 테스트 **141/141 통과**. 표 페이지/필터/IME/설정/선택 계약과 학생 상세·클래스 관계 회귀를 포함한다. 클래스 action을 표시하지 않는 기존 소스 검사 1건은 학생 더보기 분기를 허용하도록 같은 계약을 유지하며 갱신했다.
- 제품 TypeScript, 대상 ESLint, Next production build 통과. 빌드 후 마지막 변경은 새 메뉴의 다크 색상/reduced-motion CSS 클래스이며 실제 브라우저 색상과 대상 ESLint로 확인했다.
- 독립 읽기 전용 검토에서 근거 있는 P1/P2 회귀 미발견. API·서비스·DB·인증·권한·출력 코드는 이번 네 가지 정돈에서 변경하지 않았다.

### 실제 브라우저 증거와 범위

- 로컬 합성 서비스에 실제 제품 컴포넌트를 연결한 3137 fixture에서 확인했다. 운영 데이터/실제 전송/배포 검증이 아니다.
- 1357×987: 학생 목록 위계, 중립 상태 표시, 상세 3열/8필드, 등록 9필드 및 이름 자동 초점, 수업·교재 도구 막대와 표 설정을 검수했다.
- 790×900: 학생 필드 2열. 390×844: 1열과 입력 폭332px, 문서 가로폭390px, 모바일 더보기 및 세 목록의 설정 접근/화면 안 배치를 확인했다.
- 체크박스 바깥 label 모서리(x3,y3) 클릭으로 페이지8행이 선택됨을 확인했다. 키보드 Space로 1행을 해제하면 7행/상단 mixed 상태이며 전체 선택·해제가 정상 동작한다.
- 더보기 Enter 열기/Escape 닫기 후 해당 학생 더보기 버튼으로 초점 복귀를 확인했다. 읽기 전용에서 8개 더보기 버튼 모두 disabled였다.
- 퇴원 메뉴 선택 시 기존 `/admin/withdrawal?create=withdrawal&studentId=student-001` 경로로 이동했다. 이 격리 fixture에는 퇴원 업무 페이지가 없어 도착 화면은404이며, 이번 검수는 기존 경로 연결까지다. 실제 퇴원 신청·상태 변경은 수행하지 않았다.
- 교재 새로고침 후 기존 과목 그룹·과목 정렬·이름220px 설정이 유지됐다. 학생/수업/교재 표 설정 버튼은 각 화면에 한 개만 있다.
- 다크 메뉴의 최종 글자색 oklch(0.704 0.191 22.216) 및 표시를 확인했다. reduced-motion 클래스 존재는 확인했으며 새 메뉴의 OS reduced-motion 전환을 별도로 반복 측정하지 않았다.
- 증거 폴더: `.codex-artifacts/apple-design-phase6/`. 주요 화면은 `student-desktop.png`, `student-detail-desktop.png`, `student-create-desktop.png`, `student-create-tablet.png`, `student-create-mobile.png`, `student-menu-mobile.png`, `student-settings-mobile.png`, `student-dark-menu.png`, `classes-desktop.png`, `classes-settings-desktop.png`, `classes-settings-mobile.png`, `textbooks-desktop.png`, `textbooks-settings-mobile.png`이다. 검사 로그는 `final-tests.log`, `typecheck.log`, `eslint.log`, `build.log`에 있다.

## 7단계 · 수업정보 상세·등록으로 확장

사용자의 다음 단계 진행 요청에 따라, 완성된 학생관리와 공통 목록 기준을 실제 수업 상세·등록 컴포넌트에 확장했다.

- 기본 정보·시간표 입력의 높이와 간격을 공통 토큰으로 통일했다. 상세 시트는 최대1024px, 기본 필드는 데스크톱2열/모바일1열이다. 긴 수업명·연결 교재명·학생 정보는 줄바꿈한다.
- 일반 저장은 모든 화면 크기에서 같은 고정 하단 영역의 닫기·저장 버튼을 사용한다. 기존 모바일 전용 저장 막대는 제거했다. 기본 시간표의 별도 저장, revision/requestKey, 권한·과학 영역 선택 제한은 그대로 유지했다.
- 모바일 시간표는 요일, 시작/종료 시각, 선생님/강의실 순으로 재배치한다. 수강·대기 명단은 같은 열 너비를 사용하며 모바일에서 학교/학년과 두 연락처를 짝지어 표시한다.
- 명단 이동은 현재 고정 헤더 높이를 계산해 긴 제목에 가리지 않도록 했다. 상세를 닫으면 수업명 버튼으로 포커스가 돌아가며, 행이 사라진 경우 보이는 대체 행/검색 입력으로 복귀한다. 학생 화면에도 기존 복귀 계약을 유지했다.

### 검사 결과

- 최종 관련 테스트 **153/153 통과**. 수업 명단·기본 시간표 저장·교재 선택·과학 분류·검색 combobox·학생 입력/toolbar·페이지·공통 표를 포함한다. 앞선 중복 실행 결과는 더하지 않았다.
- 제품 TypeScript no-emit, 최종 대상 ESLint, 최종 Next production build 및 git diff --check 통과. 빌드에는 마지막 포커스 복귀·헤더 높이·명단 열 보완까지 포함됐다.
- 독립 읽기 전용 소스 검토에서 이번 변경의 P1/P2 회귀를 발견하지 못했다. 일반 저장과 시간표 저장 분리, 권한과 과학 선택 제한, 교재 연결·명단 변경의 기존 서비스 계약을 대조했다.

### 실제 브라우저 검수

- 동일 합성 수업/시간표2개/연결 교재2개, 1357×987 및390×844에서 변경 전후를 각각 캡처하고 함께 열어 비교했다. 이전 소스는 비교 캡처에만 잠시 사용한 뒤 빌드 검증된 최종 소스와 byte 단위로 같은 상태로 복원했다.
- 790×900: 시트758px, 기본 입력336px씩2열, 저장 버튼이 화면 안에 있다. 390×844: 시트366px, 문서 가로폭390px, 고정 하단 닫기·저장에 접근할 수 있다. 긴 수업명, 모바일 시간 선택 팝오버, 수강·대기 명단의 줄바꿈도 확인했다.
- 긴 제목 상태에서 명단 이동 후 헤더 하단270px/명단 시작286px로16px 간격을 확보했다. 수강/대기 명단은 데스크톱에서 같은 열에 정렬되고 모바일에서 불필요한 데스크톱 헤더가 숨겨진다.
- 시간 선택을 키보드 ArrowDown/Enter로18:00→18:10 변경하고 별도 기본 시간표 저장 완료를 확인했다. 일반 저장은 합성 실패 응답에서 입력을 보존하고 재시도 성공→목록 반영→상세 재진입까지 확인했다.
- 수업 등록 폼 입력→등록→새 목록 행→상세 재진입을 확인했다. 이름·정원·수업료·시간표가 반영됐다. fixture 메모리 저장의 검증이며 브라우저 새로고침 이후 영구 저장 검증은 아니다.
- 읽기 전용에서 일반 저장과 기본 정보·시간표 입력이 모두 비활성화됐다. 닫기 후 수업명 포커스 복귀, 학생 상세의 기존3열/42px 입력과 김학생 행 포커스 복귀도 재확인했다.
- 라이트/다크 화면의 표면·입력·고정 하단을 확인했다. 새 라이트 진입→다크 전환→상세 진입 구간의 추가 브라우저 오류는0개였다.
- 별도 관찰: 저장된 다크 테마로 fixture를 처음 열면 기존 ThemeProvider의 서버 기본 테마와 클라이언트 저장 테마가 달라 ModeToggle hydration 경고가 재현됐다. 이번 수업 변경 전부터 존재하는 초기화 코드이며 수정하지 않았다. 제품 관리자 화면에는 AuthGuard 로딩 경계가 있어 운영 재현 여부는 이 fixture 결과만으로 판단하지 않는다. 전후 소스 교체 중 발생한 일시적인 Hook 순서 HMR 경고는 최종 새 진입에서 재현되지 않았다.
- 시트는 기존180ms/8px 진입, 즉시 focus ring과 reduced-motion 무애니메이션 규칙을 공유한다. 명단 이동도 reduce 설정이면 즉시 이동하도록 코드에서 확인했다. 이번 단계에서는 OS/브라우저 reduced-motion 설정을 실제 전환해 다시 측정하지 않았다.

### 증거·범위

증거는 `.codex-artifacts/apple-design-phase7/`에 있다. 주요 파일: `class-detail-before-desktop.png`, `class-detail-after-desktop.png`, `class-detail-before-mobile.png`, `class-detail-after-mobile.png`, `class-detail-tablet.png`, `class-detail-dark.png`, `class-roster-desktop.png`, `class-roster-mobile-long.png`, `class-time-picker-mobile.png`, `class-save-error.png`, `class-create-desktop.png`, `class-create-mobile.png`, `student-detail-regression.png`, `final-tests.log`, `typecheck.log`, `eslint.log`, `build.log`.

실제 제품 컴포넌트에 로컬 합성 서비스를 연결한 검수다. 운영 DB·인증/RLS·실제 수강 변경·발송 검증은 수행하지 않았다. 서비스·API·권한·데이터 구조·출력 코드는 변경하지 않았으며 푸시·배포하지 않았다. 이번 수업 상세·등록 UI 범위는 완료했고, 등록 업무·전용 일정·교재 운영 화면의 전체 리디자인은 후속 범위로 남아 있다.

final result: passed

## 8단계 · 보류했던 수업 기간 제거

2026-09-10 사용자 요청으로, 7월28일 설계에서 합의하고 다음 릴리스로 미뤘던 기간 제거를 반영했다. 수업관리·수업계획의 기간 필터, 수업 상세·등록의 기간 입력, 환경 설정의 기본 기간 지정과 자동 기본 기간 조회를 제거했다. 독립 수업그룹과 과거 학기 데이터·시간표 기능은 유지한다.

이번 단계는 앞선 UI 전용 단계와 달리 기간 없는 수업 생성 및 수업계획 전체 조회를 위한 DB migration 2개를 포함한다. 운영 DB에는 적용하지 않았다. 로컬 DB lint와 관련 pgTAP231개, 관리 회귀184개, academic/operations pagination52개와 관련 회귀143개를 각각 검증했다. 중복 파일이 있어 합산하지 않는다. 최종 타입 검사·대상 ESLint·production build가 통과했다.

실제 제품 컴포넌트에 합성 서비스를 연결한 데스크톱/390px 브라우저에서 기간 없는 등록·상세 재진입, 기간 URL 제거·검색 보존, 수업계획 전체 조회, 수업그룹 설정을 확인했다. 검색 X 버튼의 눌림 위치가 움직여 클릭이 취소되는 문제도 수정했다. 전체 검사와 기존 admin-shell 기대 불일치2건, fixture의 읽기/저장 경계는 [기간 제거 검수 기록](docs/qa/2026-09-10-class-period-removal.md)에 분리해 기록했다. 푸시·배포는 하지 않았다.


## 9단계 · 목록 필터 직접 노출

학생관리·수업관리·수업계획에서 조건 하나만 남은 필터 팝오버를 제거했다. 공통 `DataTableFilters`로 상태를 첫 번째에 두고 모든 조건과 초기화를 바로 조작하게 했다. 데스크톱은 한 줄/자동 줄바꿈, 모바일은 두 열이며 반복하던 필터 배지를 줄였다.

관련136개 검사, 타입·ESLint·production build가 통과했다. 세 화면의 데스크톱/390px 모바일과 상태 선택·조건 초기화·키보드 과목 선택/초점 복귀를 실제 합성 브라우저에서 확인했다. API·DB·권한·필터의 조회 및 URL 계약은 유지했다. [정확한 검수 기록](docs/qa/2026-09-10-direct-list-filters.md). 푸시·배포는 하지 않았다.


## 10단계 · 수업계획 목록 정돈

수업계획을 공통 데이터테이블 프레임으로 정돈했다. 검색·분류·목록의 반복 틀을 합치고 모바일 보기 메뉴와 중복 요약을 없앴다. 긴 이름은 줄바꿈하며 수업명→보조 정보→상태→다음 작업 위계를 유지한다. 표는 짧은 내용에 맞춰 줄고 긴 목록만 내부 스크롤한다.

최종 관련102개 검사, 대상 ESLint·TypeScript·production build가 통과했다. 데스크톱1357px/모바일390px, 장문24개 수업, 분류 선택/초기화/키보드 초점, 10→20행 및 페이지 이동, 빈 상태·오류·로딩·다크 테마를 합성 브라우저로 검수했다. 새 페이지 스크롤 초기화, 실패 중 기존 위치/데이터 유지, 중첩 링크 키보드, 공통 Progress 접근성 값을 보완했다.

실제 상세 편집/저장 전체 흐름과 운영 적용은 검증하지 않았다. 자세한 보존 범위와 증거는 [수업계획 검수 기록](docs/qa/2026-09-10-curriculum-workspace.md)에 있다. 푸시·배포는 하지 않았다.


## 11단계 · 교재관리 마스터 목록 정돈

교재관리의 업무 탭과 마스터 목록을 공통 표 기준으로 정돈했다. 검색·직접 상태 필터·목록·페이지 탐색을 한 프레임으로 묶고, 반복 요약과 모바일 중첩 장식을 줄였다. 과목은 그룹에서, 학교·학년·세부과목은 분류 열에서 표시해 긴 교재명이 끝까지 읽히도록 했다.

데스크톱1357px/모바일390px에서 선택·필터/검색 초기화·10→20행·페이지/그룹 전환·상세 조회와 취소·키보드·지연/로딩/오류·다크 테마를 합성 브라우저로 확인했다. 초기 조회 실패의 잘못된 빈 목록 표현과 같은 과목이 다시 등장할 때의 그룹 key 충돌도 수정했다. 최종 관련 175개 회귀 검사와 타입·린트·production build가 통과했다.

요청/주문·입고/출고/실사/정산 전체 개편이나 실제 재고·삭제·저장 검증은 아니다. 서비스·권한·DB·출력 코드는 유지하며 푸시·배포하지 않았다. 자세한 범위와 증거는 [교재 마스터 검수 기록](docs/qa/2026-09-10-textbook-master.md)에 있다.


## 12단계 · 교재관리 기능 단순화

사용자의 복잡도 지적에 따라 ‘할 일’ 중복 메뉴, 재고 상태·정리 상태 필터, 교재/그룹 정리 경고, 전역 상태 배지 줄을 제거했다. 기본 목록은 검색과 네 가지 분류, 신규 등록, 미사용 교재 토글로 구성한다. 교재 목록/재고 실사로 탭 이름을 명확히 하고 요청·주문·출고 건수와 공통 새로고침을 유지한다.

이전 URL의 폐기 조건은 첫 조회 전에 해제한다. 미사용 목록 접근, 위치별 수량, 실사·입출고·정산 및 기존 쓰기 보호는 보존했다. 자동 검증 178개, 타입 검사, 변경 파일 린트, 빌드 통과. 실제 브라우저 1280px/390px 목록과 업무 탭, 빈 결과·로딩·오류·키보드 복원 확인. 실데이터 변경과 배포는 수행하지 않았다.

상세 결과: [교재관리 단순화 검수](docs/qa/2026-09-10-textbook-simplification.md). 11단계의 여섯 필터·정리 배지 기준은 이번 단계로 대체한다.

## 13단계 · 요청·주문·입고 처리표 정돈

교재명과 다음 처리에 집중하도록 공통 처리표를 정돈했다. 학생용·교사용의 여섯 수량을 요청/주문/입고 세 단계에 각각 묶고 교재명 바로 뒤에 배치했다. 반복 요약을 줄이고 기본 주문/입고 버튼, 행 더보기 메뉴, 주문서 메뉴로 작업을 정리했다. 모바일 긴 제목 줄바꿈과 키보드 가로 스크롤을 보완했으며 준비된 수업 인원을 사용해 잘못된 미선택 표시를 수정했다.

관련 179개 검사, 타입·ESLint·production build가 통과했다. 데스크톱1280px/모바일390px에서 수량·긴 제목·메뉴 초점 복귀·주문 편집 진입/취소·요청 탭 공통 적용을 검수했다. 삭제 전 최신 구성원 조회·재확인·권한과 전체 대상 주문서 조회 회귀 검사를 유지했다. 실제 저장·재고 이동·외부 전송·배포는 수행하지 않았다.

상세 결과: [요청·주문·입고 검수](docs/qa/2026-09-10-textbook-purchase.md).

## 14단계 · 출고 처리표와 공통 버튼 상태

출고 목록을 공통 표 구성으로 정돈하고, 학생·요청·주문·출고의 행 더보기와 제목 상세 버튼을 재사용하도록 했다. 일반 버튼의 hover·pressed·focus·disabled를 공통 Button으로 모으고 16개 호출 파일의 로컬 상태 스타일 및 학생/수업 CSS 덮어쓰기를 정리했다. 의미별 destructive 변형을 제공하며 공통 UI와 표 사용 기준을 문서화했다.

20행에서 선택을 5회 바꾸는 동일 하네스 비교에서 교재·수업·위치·일시 조회 함수 호출 총 800회를 0회로 줄였다. 추가 요청은 전후 모두 0회이며 새 데이터에서 캐시가 갱신됨을 검사했다. 관련 660개 검사, 타입·최종 빌드 통과. 린트 오류 0, 변경 전부터 존재한 시간표 경고 1개.

데스크톱1280px/모바일390px에서 긴 제목·선택·상태 메뉴·상세 조회/닫기·빈 검색/초기화와 메뉴 초점 복귀를 확인했다. 공통 버튼 64개 표본의 밝은/어두운 테마 문맥별 기본 상태 차이 0, hover와 키보드 focus 확인. pressed/reduced-motion은 공통 소스 기준을 검사했다. 실제 저장·재고 이동·외부 전송·배포는 하지 않았다.

상세 결과: [출고·공통 버튼 검수](docs/qa/2026-09-10-textbook-sales-and-controls.md).

## 15단계 · 재고 실사와 이력 정돈

재고 실사·이력을 공통 표와 페이지 탐색 프레임으로 정돈했다. 데스크톱·모바일의 수량 입력/초기화·반영·차이 표시를 재사용하고 모바일 행 선택을 추가해 기존 일괄 반영을 사용할 수 있게 했다. 긴 이름·메모 줄바꿈, 반복 배지·작은 카드 제거, 필터의 0건 유지, 고정 헤더 배경과 다크 상태 색상을 보완했다.

관련 187개 검사 통과. 모바일 0권·메모·선택 공유, 중복 Enter 차단, 조회 실패 후 입력 보존과 스키마 오류 차단을 추가로 검증했다. 기존 저장·잔고 재조회·권한·idempotency·revision 보호 함수는 유지했다. 데스크톱1280px/모바일390px의 밀집 목록과 긴 내용, 키보드 가로 스크롤, 빈 필터·로딩·오류 및 다크 테마를 확인했다. 실제 재고 변경·외부 전송·배포는 하지 않았다.

상세 결과: [재고 실사·이력 검수](docs/qa/2026-09-10-textbook-inventory.md).

## 16단계 · 정산 목록과 상세 비교 정돈

정산 목록과 이동 내역을 공통 표·도구 모음·페이지 탐색으로 통일했다. 중복 상세 버튼과 건수 배지를 줄이고, 초안/확정 상태를 함께 사용한다. 체크박스 Space가 상세 열기로 전파되지 않도록 역할을 분리했다. 저장값과 현재 계산값은 네 행의 비교표로 표시하며, 긴 교재명·위치·메모와 모바일 오류·빈 결과 안내가 생략되지 않는다. 상세 창의 헤더/닫기 영역과 본문 스크롤을 분리했다.

관련 195개 검사 통과. 실제 브라우저 1280px/390px, 선택·상세·검색·오류/로딩·다크 테마를 검수했다. 계산·저장·확정·전체 범위 복사 함수는 그대로이며 기존 월마감 입력과 전체 선택 4건 저장 범위도 유지했다. 이번 단계에서 속도 향상 수치를 주장하지 않는다. 실제 저장·확정·외부 전송·배포는 하지 않았다.

상세 결과: [정산 목록·상세 검수](docs/qa/2026-09-10-textbook-closing.md).

## 17단계 · 교재 재고·실사·요청 단순화와 정산 제거

사용자의 명시적 기능 축소 요청을 적용했다. 교재 목록은 위치별 수량을 보여주는 역할에 맞게 ‘교재 재고’로 바꿨다. 실사 권장/대기/완료/전체 분류, 추천 사유와 상태 열을 없애고 수량 입력·차이·최종 실사·메모를 남겼다. 요청·주문 공통 처리표의 판단 열과 모바일 부족/여유 표시, 입력 창의 중복 판단 요약을 제거했다.

정산 목록·생성·확정·상세·복사 UI와 관련 조회 훅을 제거했다. 다섯 업무 탭을 사용하며 기존 정산 주소는 첫 조회 전에 교재 재고 1페이지로 정리한다. 저장 기록과 재고 원장, 서비스·DB 계약, 기존 쓰기 보호는 보존했다. 앞선 12·15·16단계에서 유지한 실사 분류와 정산 UI 기준은 이 단계로 대체한다.

관련 194개 검사 통과. 최종 타입·ESLint·production build와 diff 공백 검사를 확인했다. 실제 브라우저 1280px/390px에서 긴 제목, 0권 입력과 선택, 수량·열 설정, 예전 정산 주소, 빈 결과와 오류를 검수했다. 이번 단계에서 성능 수치를 측정하거나 실제 재고를 변경하지 않았으며 푸시·배포하지 않았다.

상세 결과: [교재관리 업무 축소 검수](docs/qa/2026-09-10-textbook-workflow-reduction.md).

## 18단계 · 공통 필터와 화면 흔들림 개선

Apple HIG, web.dev, Radix와 Vercel 공식 자료를 조사하고 출고 빈 결과에서 검색창 제거로 발생한 48px 이동, 주문 필터의 가변 너비·높이, 선택 버튼의 테두리 차이를 재현했다. 주문·입고와 출고에 공통 `DataTableSelectFilter`를 적용하고 요청을 포함한 검색·도구 모음·추가 버튼을 결과 수와 무관하게 유지했다. 갱신 중 수락된 기존 행을 보존하고 다른 조건으로 자료를 작성하지 않게 했다.

관련 201개 검사와 타입·ESLint·production build·공백 검사 통과. 데스크톱과 390px 모바일, 4.5초 지연의 일반/진행 중/빈 결과에서 검색·필터 위치를 측정했다. 출고의 재현된 48px 이동은 0px가 됐으며 키보드 초점과 다크 테마를 검수했다. 전체 CLS 점수나 운영 성능 수치로 확대 해석하지 않는다. 기존 쓰기 함수·서비스·DB를 유지했고 푸시·배포하지 않았다.

상세 결과: [교재관리 필터·화면 안정성](docs/qa/2026-09-10-textbook-control-stability.md).

## 19단계 · 요청·주문·입고·출고 입력 창 통일

공통 `FormDialogContent`로 제목/행동 영역과 본문 스크롤을 분리했다. 교재명은 전체 너비로, 선생님·위치와 학생용·교사용 수량은 두 열로 정렬하고 모바일 한 열에 대응했다. 편집 창의 새 요청/새 주문은 기존 목록 추가 동선으로 통합했다. 저장 이름·크기를 유지하고 비활성 이유, 폼 안의 실패 안내·초점·재시도를 공통화했다. 취소한 오류가 새 폼으로 넘어가지 않으며 연속 Enter도 한 저장만 실행한다.

관련 203개 검사, 타입·최종 ESLint·production build·공백 검사 통과. 대형 renderer 파일에서 발생한 SIGKILL은 필터 시나리오를 별도 파일로 옮겨 DOM 수명을 분리한 뒤 전체 9개 파일 순차 실행으로 검증했다. 1280×720 요청 저장 실패 전후, 390×844 입고 스크롤 전후와 출고 대상 전환에서 창/저장 버튼의 실제 좌표가 동일했다. 일괄 주문, 키보드 초점과 다크 테마도 검수했다. 기존 저장 서비스 호출 25곳과 함수 193개가 동일하다. 실제 운영 저장·푸시·배포는 하지 않았다.

상세 결과: [교재관리 등록·편집 폼 통일](docs/qa/2026-09-10-textbook-form-consistency.md).


## 20단계 · 교재 재고 등록·수정 및 상세 창 통일

교재명 입력을 전체 너비로 넓히고 학교 구분·학년 선택의 세로 줄바꿈을 없앴다. 마스터 등록·수정에도 공통 FormDialogContent를 적용해 고정 제목·하단 행동, 입력 보존, 오류 초점, 연속 제출 차단을 재사용한다. 필수 검증은 유지하면서 초기 다중 경고를 한 가지 다음 입력 안내로 정리했다. 중복 교재 목록의 Button 상태와 긴 제목 표시도 공통화했다.

DetailDialogContent는 같은 프레임을 공유하며 출고 상세를 작은 카드에서 교재·대상 중심의 설명 목록으로 바꿨다. 기존 응답의 상태·수업·위치·월·일시·메모를 함께 보여주고 추가 조회를 만들지 않는다. 1280×720 등록 실패 전후와 390×844 본문 스크롤 전후에 창/저장 버튼 좌표가 동일했고, 상세 오류·재시도·완료의 창 크기도 유지됐다.

관련 454개 검사를 파일별 독립 프로세스로 통과했다. 합쳐 실행한 명령의 필터 SIGKILL은 원인을 확정하지 않았으며 성공으로 집계하지 않았다. 타입·ESLint·production build·공백 검사 통과. 기존 함수 199개와 서비스 호출 25곳이 동일하다. 실제 운영 저장·푸시·배포는 하지 않았다.

상세 결과: [교재 재고 등록·수정 및 상세 창 검수](docs/qa/2026-09-10-textbook-master-detail.md).


## 21단계 · 주문서·반품 요청서·청구 준비 창

DocumentDialogContent로 기존 폼·상세 프레임을 재사용하고 문서의 제목·저장 메뉴·피드백·닫기를 고정했다. 전체/개별 이미지·PDF를 공통 저장 메뉴로 묶고 복사 경로를 유지한다. 파일 생성은 별도 문서 복사본에서 수행해 화면 너비를 바꾸지 않으며 모바일에서 숨겨진 표 열을 포함한다. 동시 생성·닫은 뒤 늦은 파일 전달을 차단하고 조회 오류를 빈 결과와 구분했다.

관련 459개 검사, 타입·ESLint·production build·공백 검사 통과. 데스크톱 파일 생성 전후와 모바일 내부 스크롤 전후에 창·고정 도구 영역의 좌표가 유지된다. 주문/반품/청구 화면 및 실제 PNG/PDF 출력을 검수했다. 문서의 데이터·구조와 서비스 호출 25곳을 유지했고 공급처 미지정 이름만 한국어로 보완했다. 실제 전송·운영 저장·배포는 하지 않았다.

상세 결과: [문서 작업 창 검수](docs/qa/2026-09-10-textbook-document-dialogs.md).

## 22단계 · Apple 원칙 대표 구현

사용자 승인에 따라 교재 재고의 검색·필터·선택·표 설정·수정 폼을 실제 공용 컴포넌트로 정리했다. 선택, 빈 결과, 로딩, 목록 오류에서 검색창·표·페이지 이동 좌표를 유지한다. 공용 컬럼 설정의 화면 밖 잘림과 비동기 위치 컬럼의 저장값 보존 및 hydration 경계를 개선했다.

대표 1280×720 화면은 긴 제목을 자르지 않으면서 완전히 보이는 행이 3→4행으로 늘었다. 390×844에서 필터 적용에 따른 높이 증가는 0px이다. 실제 공용 상태 비교 화면, 전후 비교, 관련 고유 검사 200개, 제품 빌드 및 검증 범위는 아래 보고서에 기록했다. 렌더링·응답 시간의 모든 지표가 함께 개선된 것은 아니므로 전체 속도 개선을 주장하지 않는다. 사용자 대표 화면 검수 후 다른 화면으로 확장한다.

상세 결과: [Apple 원칙 대표 구현 검수](docs/qa/2026-09-10-apple-representative-implementation.md).


## Phase 23 · 교재관리 탭 통일과 고정 컬럼 (2026-09-10)

- 사용자 요청에 따라 교재관리의 컬럼 구성/숨김 커스터마이징 제거. 업무별 순서·너비 고정, 요청/주문/출고 관련 정보는 보조행에 보존.
- 다섯 탭의 검색/작업/필터/프레임/pager와 모바일 카드 스타일 공용화. tab별 scrollbar 차이 보정 및 수정 취소 포커스 복귀.
- 187개 관련 검사, 제품 build, 대상 lint, diff-check 통과. 누적 JSDOM의 SIGKILL과 독립 프로세스 검증을 구분.
- [검수 보고서](docs/qa/2026-09-10-textbook-tab-unification.md). 로컬 테스트, 운영 변경/발송/배포 없음.

## Phase 24 · 교재 일괄 속성 변경 창 (2026-09-10)

- 목록을 194px 밀어내던 속성 변경 인라인 폼을 공통 FormDialogContent로 옮겼다. 표 이동0px, 모바일 입력/저장44px, 선택·실패·취소·초점 복귀 검수.
- 저장 중복과 이전 창의 늦은 응답을 차단하고 기존 선택/초안 보호를 유지했다. 관련 고유 검사100개, 최종 build/타입/lint/diff-check 통과.
- [검수 보고서](docs/qa/2026-09-10-textbook-bulk-dialog.md). 운영 저장·발송·배포 없음.


## Phase 25 · 교재 정리·반품·취소 확인 창 (2026-09-10)

- 기존 DialogFrame과 Button을 재사용해 확인 창을 통일했다. 긴 대상명, 전체 건수, 취소 우선 초점, 고정 하단 버튼과 내부 스크롤을 적용했다.
- 실행 중 중복 처리·닫기를 막고 성공 후 닫으며 실패 시 대상과 오류를 유지한다. 교사용 출고 취소의 수령인 표시도 최신 상세 조회 결과로 수정했다. 기존 서비스 호출25곳과 문서 구조/내용은 유지했다.
- 관련 고유 검사103개, 최종 제품 build/타입/lint/diff-check 통과. 데스크톱·모바일에서 오류/스크롤 전후 창·실행 버튼 위치 유지와 키보드 복귀를 확인했다.
- [검수 보고서](docs/qa/2026-09-10-textbook-confirmation.md). 로컬 합성 데이터 검수이며 운영 저장·발송·배포 없음.


## Phase 26 · 일괄 주문 수량 편집과 묶음 선택 (2026-09-10)

- 공통 FormDialogContent/Input을 재사용해 모바일 고정 3열을 전체 너비 교재명과 수량 행으로 정리했다. 수량 입력80×36→112×44px, 요청/주문 숫자 정렬과 긴 제목·수업·위치 표시를 보완했다.
- 묶음 checkbox가 대표 학생용 ID만 선택하던 오류를 발견하고 실제 학생용/교사용 member를 함께 선택하도록 수정했다. 저장·재조회·계산 함수는 유지한다.
- 고유 검사95개, 제품 build/타입/lint/diff-check 통과. 실제 모바일/데스크톱 오류·스크롤 전후 버튼 위치 유지, 9/4 입력 보존과 취소 초점 복귀를 확인했다.
- [검수 보고서](docs/qa/2026-09-10-textbook-bulk-order.md). 로컬 합성 데이터 검수이며 운영 저장·발송·배포 없음.

## Phase 27 · 처리 결과 알림과 일괄 실행 안정성 (2026-09-11)

- 처리 오류가 탭·검색창·표를 58px 밀던 문제를 공통 ActionFeedback으로 해결했다. 동일 데스크톱 조건에서 이동0px, 모바일 문서 위치와 재시도 스크롤 유지, 닫기44px와 초점 복귀를 검수했다.
- 동일 actor/action의 연속 실행 방지를 목록 작업으로 확장했다. 일괄 입고 처리 중 상태와 비활성을 통일하고 실패 선택 보존·재시도를 확인했다. 폼 오류는 기존 창 안에 유지한다.
- 관련 고유 검사95개, 제품 build/타입/lint/diff-check 통과. 서비스 호출25곳과 문서 구조·내용을 유지했다. 전체 CLS/응답 속도나 실운영 저장 결과로 확대 해석하지 않는다.
- [검수 보고서](docs/qa/2026-09-11-textbook-action-feedback.md). 로컬 합성 데이터 검수이며 운영 저장·발송·배포 없음.

## 교재관리 UI 개선 최종 완료 (2026-09-11)

사용자의 완료까지 자율 진행 요청에 따라 기존 승인 범위를 통합 점검하고 마감했다. 조회 오류 때 70px 밀리던 탭·검색·표의 이동을 0px로 줄였고, 모바일 긴 제목, 출고 busy 상태, reduced-motion 메뉴, 안전한 오류 안내와 재시도 초점을 공용 컴포넌트에서 정리했다.

고유 257개 로컬 회귀, 최종 production build·타입·대상 ESLint(경고 0)·diff 검사를 통과했다. 기존 서비스 호출식 25곳을 보존했으며 영향 없는 실제 PNG/PDF와 읽기 서비스 검수는 재사용했다. 운영 저장·발송·푸시·배포는 하지 않았다. 추가 단계나 새 기능을 완료 조건에 덧붙이지 않고 현재 교재관리 UI 범위를 종료한다.

[최종 완료 보고서와 화면](docs/qa/2026-09-11-textbook-final-completion.md).
