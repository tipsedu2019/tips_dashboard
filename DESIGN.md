# TIPS 제품 품질 기준

## 목적과 우선순위

TIPS는 학원 구성원이 학생, 수업, 일정, 교재, 등록, 알림 업무를 빠르고 정확하게 처리하는 운영 도구다. 화면은 다음 행동을 바로 알 수 있게 만들고, 장식이나 별도 설명은 업무 판단에 기여할 때만 둔다.

판단 순서는 다음과 같다.

1. 인증·역할·RLS와 학생 데이터의 정확성
2. 저장, 상태 전이, 알림의 의도와 결과가 일치하는지
3. 가장 자주 하는 업무를 적은 이동과 입력으로 끝낼 수 있는지
4. 밀도 높은 목록에서도 상태, 선택, 다음 행동이 분명한지
5. 시각적 일관성과 세부 표현

외부 디자인 자료는 검토 도구다. 사용자 지시, `AGENTS.md`, 실제 업무 의미, 기존 컴포넌트와 토큰이 우선한다.

## 기존 시스템을 사용한다

- 글꼴은 `src/app/globals.css`의 `--font-sans-stack`을 따른다. 기본은 Pretendard이며 한글 시스템 폴백을 유지한다.
- 색은 `background`, `foreground`, `card`, `primary`, `secondary`, `muted`, `accent`, `destructive`, `border`, `input`, `ring`과 각 foreground 토큰을 의미에 맞게 쓴다. 화면 안에서 임의 브랜드 색을 새 기준으로 만들지 않는다.
- 테마와 간격의 기준점은 `src/app/globals.css`, shadcn 설정은 `components.json`이다. 현재 설정은 `new-york`, CSS 변수, neutral base, Lucide 아이콘이다.
- 공통 원시는 `src/components/ui/`, 앱 셸은 `src/components/app-sidebar.tsx`와 `src/components/site-header.tsx`, 공통 목록 탐색은 `src/components/data-table/data-table-pagination.tsx`에 있다. 먼저 실제 구현과 호출부를 확인하고 재사용하거나 고친다.
- 반복 규칙은 토큰과 공통 컴포넌트에 둔다. 특정 화면의 업무 의미는 해당 feature에 둔다.

## 화면과 상호작용

### 밀도와 계층

- 관리 화면은 정보를 숨겨 단순해 보이게 만들기보다, 현재 상태와 주 행동을 빠르게 비교할 수 있는 밀도를 택한다.
- 한 영역의 대표 행동은 하나로 보이게 하고 보조 행동은 위계가 낮은 변형을 쓴다. 같은 의미의 행동 이름은 버튼, 진행 상태, 성공 메시지에서 유지한다.
- 카드, 배지, 구분선은 정보의 관계나 상태를 표현할 때만 쓴다. 요약 카드와 안내 문구로 본문을 반복하지 않는다.
- 긴 이름, 메모, 학교·수업명과 실제 빈 상태, 일부 데이터, 최대 데이터에서 레이아웃을 확인한다.

### 모바일

- 390px 너비에서도 페이지 전체의 가로 넘침 없이 핵심 내용과 주 행동에 도달할 수 있어야 한다. 넓은 표는 열을 무작정 축약하지 말고 업무 단위의 카드, 우선 열, 또는 명시적인 내부 스크롤을 선택한다.
- 터치 대상과 간격은 실제 모바일에서 잘못 누르지 않을 크기로 확인한다. 주요 모바일 행동은 아래 44px 역할 토큰을 사용하며 작은 보조 제어를 일괄 확대하지 않는다.
- 데스크톱과 모바일에서 같은 저장·취소·오류 복구 의미를 유지한다.
- 여러 조건을 가진 목록은 `DataTableFilterPanel`을 사용한다. 데스크톱에는 조건을 펼쳐 두고, 모바일에는 높이 44px의 필터 버튼과 적용 조건을 표시한다. 패널 안에서 즉시 적용하며 닫기·Escape는 버튼으로 포커스를 돌린다. 화면 폭이 바뀌어도 조건과 점수 초안을 유지한다.
- 모바일 관리 목록은 `DATA_TABLE_MOBILE_ITEM_CLASS_NAME`으로 기본·선택 배경을 통일한다. 이름을 먼저 읽게 하고 빈 연락처 두 줄은 `연락처 없음` 한 줄로 표시한다. 전화 가능한 값만 `tel:` 링크로 제공한다.
- 재시험 카드는 학생·응시예정·현재 할 수 있는 행동 순서다. 진행 중일 때 점수 입력을 노출하며, 시작 전에는 시험 시작을 우선한다. 교재·범위·본시험일·메모는 시험 정보에서 확인할 수 있다.

### 키보드와 접근성

- 링크는 이동, 버튼은 동작에 사용한다. 기본 HTML 의미를 먼저 쓰고 아이콘 전용 버튼에는 구체적인 접근 가능한 이름을 둔다.
- 모든 업무 흐름은 키보드로 실행할 수 있고 포커스가 보이며, dialog·popover가 포커스를 가리거나 잃게 하지 않는다.
- 탭은 `tablist`·`tab`·`tabpanel`과 `aria-controls` 관계를 완성한다. 패널 전환이 RPC를 일으켜 지연될 수 있는 업무 화면은 방향키로 포커스만 옮기고 Enter·Space 또는 클릭으로 활성화하는 manual activation을 사용한다.
- 색만으로 상태를 구분하지 않는다. 비동기 결과와 목록 건수 변화는 필요한 곳에 `aria-live` 또는 상태 역할로 알린다.
- 필터나 탭을 적용하는 동안 선택한 제어를 없애거나 포커스를 `body`로 보내지 않는다. 마지막으로 수락된 필터와 활성 상태는 로딩·오류 중에도 보이고 다시 조작할 수 있어야 한다.
- 요약의 바로가기는 라벨이 약속한 실제 업무 화면과 상태로 이동해야 한다. 비슷한 목록이 있다는 이유로 다른 경로를 대신 연결하지 않는다.
- 동작을 추가하면 마우스, 키보드, 포커스 복귀를 함께 확인한다.

### 폼과 저장

- 실제 `src/components/ui/form.tsx`의 label, control, description, message 연결을 우선 사용한다. 필드 오류는 해당 필드 가까이에 원인과 고치는 방법을 표시한다.
- 제출이 시작되기 전에는 입력 오류를 볼 수 있게 하고, 진행 중에는 중복 제출만 막는다. 진행 표시에서도 행동 이름을 유지한다.
- 입력값, 수정 여부, 저장된 결과를 구분한다. 실패하면 사용자가 입력한 내용을 보존하고 실패한 작업만 다시 시도할 수 있게 한다.
- 권한 확인, idempotency, 잠금, 데이터 불변식은 시각적 비활성화만으로 대신하지 않는다.

### 로딩, 빈 상태, 오류

- 로딩 표시는 최종 배치와 크기를 최대한 유지한다. 이미 유효한 데이터가 있으면 전체 화면을 지우지 말고 갱신 중인 범위를 드러낸다.
- 빈 상태는 데이터가 정말 없는 경우와 필터 결과가 없는 경우를 구분하고, 가능한 다음 행동을 한 가지 제시한다.
- 오류는 무엇을 불러오거나 저장하지 못했는지와 재시도·수정 경로를 함께 말한다. 원시 예외, 내부 식별자, 비밀값은 화면에 노출하지 않는다.
- 성공 toast만으로 저장을 증명하지 않는다. 반환값과 다시 읽은 상태가 화면의 완료 표현과 일치해야 한다.

## 관리 목록 계약

아래 숫자는 외부 권고가 아니라 현재 TIPS 코드와 테스트가 정한 프로젝트 계약이다.

- 공통 선택지는 페이지당 `10`, `15`, `20`행이며 기본값은 `10`행이다. `src/lib/numbered-pagination.ts`와 `src/hooks/use-data-table-page-size.ts`가 값을 제한하고 저장한다.
- 번호 버튼은 현재 페이지가 속한 10페이지 묶음을 표시한다. 예를 들어 11페이지에서는 11–20을 보이고 마지막 묶음은 실제 총 페이지에서 끝난다.
- 첫·이전·다음·마지막 이동, 현재 페이지의 `aria-current`, 전체 건수와 현재 행 범위를 유지한다. 로딩 중이거나 총 건수를 모르면 잘못된 이동을 허용하지 않는다.
- 필터나 페이지 크기가 바뀌면 해당 목록의 페이지를 1로 되돌린다. 서로 다른 목록의 상태와 저장 키를 섞지 않는다.
- 계약 근거는 `tests/numbered-pagination.test.mjs`, 렌더링 근거는 `tests/data-table-pagination.test.mjs`이며 각 업무 서비스 테스트가 `10/15/20` 요청을 별도로 검증한다. 달력·시간표처럼 연속 탐색이 핵심인 화면에는 이 목록 계약을 억지로 적용하지 않는다.
- 수업 표의 추천 기본 열은 수업명·선생님·요일/시간·강의실·수강 현황·정원·수업료다. 과목·학년 열이 숨겨졌을 때만 수업명 아래에 해당 정보를 표시한다. 기존에 저장한 열 표시·폭·순서는 보존하고, 표 설정의 `기본값으로 복원`에서 추천 구성을 적용한다. 등록·대기 인원은 중립적인 표시를 사용한다.

학생관리의 재원 상태는 실제 수업 관계로 계산한다. 등록 수업이 있으면 `재원`, 등록 없이 대기 수업만 있으면 `대기`, 둘 다 없으면 `퇴원`이다. 기본 필터와 초기화는 `재원`이며 전체 상태 선택과 수동 상태 편집은 제공하지 않는다. 학생과 수업의 `수강 현황`은 같은 `EnrollmentStatusTrigger`로 등록·대기 수를 각각 표시한다. 학생 수업 목록은 요청할 때 30개씩 읽고 더 불러오기를 제공하며, 기존 열 설정은 유지한다.

### 행 선택과 일괄 작업

- 행을 체크해도 검색, 필터, 표 헤더와 첫 행의 위치는 변하지 않는다. 선택 건수 배지나 일괄 수정 폼을 새 줄로 삽입하지 않는다.
- `DataTableCommandRow`의 기존 작업 영역에서 일반 작업과 `DataTableSelectionActions`를 전환한다. 선택 건수에 따라 작업 영역의 크기나 검색창 너비를 바꾸지 않는다. 명령이 없는 읽기 전용 목록에는 빈 작업줄을 만들지 않는다.
- 일괄 수정 입력은 명시적인 수정 버튼으로 연다. 취소하면 입력을 유지하고 수정 버튼으로, 성공하면 검색창으로 포커스를 돌린다. 실패 시 입력과 선택 대상을 유지하고 재시도를 제공한다.

## 검증 계약

- 코드 검사는 구현 구조를, 테스트는 명시된 불변식을, 브라우저 검사는 실제 상호작용과 배치를 증명한다. 각각을 별도로 기록한다.
- UI 변경은 영향받은 실제 경로를 데스크톱과 390px 모바일에서 확인하고 키보드·로딩·빈 상태·오류·긴 콘텐츠 중 관련 상태를 검증한다.
- React 성능 변경은 먼저 재현 가능한 측정으로 병목을 확인하고, 같은 조건의 전후 결과를 비교한다.
- DB 변경은 최종 정렬된 migration 체인의 실제 함수 정의와 pgTAP을 기준으로 인증, RLS/ACL, 잠금, idempotency, SQLSTATE, no-send 경계를 확인한다.
- fixture 결과, 운영 데이터, 배포 상태, 외부 provider 결과를 서로 대신하는 증거로 쓰지 않는다.

## 공통 시각 수치

`src/app/globals.css`가 수치의 유일한 기준이다. 기존 업무 계약은 유지한다.

| 역할 | 토큰 / 값 | 사용 |
| --- | --- | --- |
| 일반 제어 | `--control-height`: 36px | desktop Button, 검색·필터 |
| 편집 필드 | `--field-height`: 42px | 학생·수업의 기존 편집 밀도 |
| 모바일 주 행동 | `--touch-target-height`: 44px | 검색 아이콘, 주 action; 모든 보조 아이콘에 강제하지 않음 |
| 표 | `--table-header-height`: 44px / `--table-row-height`: 48px / `--table-cell-padding-inline`: 12px | 행 높이는 최솟값이며 긴 식별명은 전체 줄바꿈 |
| 반경 | `--radius-control`: 8px / `--radius-surface`: 12px | `rounded-md`는 control; 표 surface는 명시 토큰. dialog·sheet는 `--radius-overlay`: 20px |
| 셸 | sidebar 240px, header desktop72/mobile56px, logo36px, menu40px | `--shell-*`; 긴 제목·확대 시 header 자연 확장 |
| 모션 | control150ms / dialog200ms | 기존 easing, reduced motion 유지 |

본문은14/20px·400, 식별명600, 셸 h1은 desktop22/28px·600, mobile18/26px·600을 기준으로 한다. 한글 기본 자간은0이다. focus는 기존3px ring을 사용한다.

색은 기존 semantic token을 소비한다. 밝은 테마 primary/destructive는 기존 hue·chroma를 유지한 채 명도를 낮추고, 어두운 테마는 밝은 action색과 어두운 foreground를 짝지었다. hover의90% 배경과 선택면 위 보조 본문까지4.5:1을 검사한다. 상태는 텍스트·선택 제어로도 표현한다. 수치 대비 검증은 `tests/premium-semantic-contrast.test.mjs`; 실제 브라우저 상태·색역 검수는 별도 증거다.

### 전체 화면과 편집 창의 구성

- 내부 앱의 바탕은 `--workspace-background`, 작업 영역은 기존 `background`·`card` 토큰으로 구분한다. 상단바는 같은 바탕을 쓰고 작은 섹션명 아래에 화면명을 표시한다. 메뉴 순서·권한·라우트는 시각 개편과 함께 바꾸지 않는다.
- 사이드바 선택은 중립적인 면과 아이콘의 primary 색, `aria-current`로 표시한다. 세로 강조선·파란 면·굵은 글자를 중복해 사용하지 않는다. 빠른 이동과 테마는 상단의 보조 행동으로 유지한다.
- 탐색 섹션은 운영 → 영어 → 관리 → 설정 순서다. 영어 단어 재시험은 영어 섹션, 통계는 관리 섹션의 마지막에 둔다. 각 메뉴의 기존 접근 권한은 유지하고, 화면 제목의 섹션명과 빠른 이동도 같은 분류를 사용한다.
- 일반 입력·보조 버튼은 테두리와 상태 변화로 구분하며 항상 그림자를 두지 않는다. 주요 제출 행동과 실제 선택 상태에 primary를 사용하고, 로딩 skeleton은 중립적인 secondary를 사용한다.
- dialog·sheet는 `--radius-overlay`, `--overlay-scrim`, `--shadow-overlay`를 공유한다. 표와 카드의 surface 반경과 구분한다. 입력 창의 고정 머리·바닥, 본문 스크롤, 취소·오류·초안 보존은 유지한다.
- 고정 도구막대는 상단바 높이 아래에 머물러야 한다. 본문 바로가기는 기본 키보드 접근을 제공하며 새 단축키를 도입하지 않는다.

## 메뉴별 개선의 연속성

2026-09-16 공통 UI 개편을 이후 메뉴 개선의 기준으로 삼는다. 메뉴마다 새 테마를 만드는 대신, 같은 구성 요소로 업무 정보의 순서·묶음·밀도·동선을 개선한다. 이 규칙은 TIPS의 프로젝트 결정이며 외부 브랜드의 공식 디자인 인증을 뜻하지 않는다.

### 기준과 변경 위치

| 개선 대상 | 먼저 사용할 구현 | 유지할 기준 |
| --- | --- | --- |
| 앱 바탕·메뉴·화면 제목 | `src/app/globals.css`, `src/components/app-sidebar.tsx`, `src/components/site-header.tsx` | 중립적인 바탕, 작은 섹션명과 명확한 제목, 동일한 탐색 구조 |
| 검색·필터·목록·페이지 이동 | `src/components/data-table/data-table-surface.tsx`, `data-table-filter-panel.tsx`, `data-table-pagination.tsx` | 검색/행동 → 조건 → 데이터 → 페이지 이동; 하나의 외곽면과 내부 구분선 |
| 선택·일괄 작업·행 메뉴 | `src/components/data-table/data-table-selection.tsx`, `data-table-row-actions.tsx` | 선택해도 표 위치 유지, 고정된 작업 영역, 기존 선택/권한 계약 |
| 일반 입력과 행동 | `src/components/ui/button.tsx`, `input.tsx`, `select.tsx`, `native-select.tsx`, `textarea.tsx` | 공통 variant·size·focus 상태; 기능마다 다른 테두리·그림자를 덧씌우지 않음 |
| 편집·확인·문서 창 | `src/components/ui/form-dialog.tsx`, `dialog.tsx`, `sheet.tsx` | 공통 반경·배경·그림자, 고정 머리/바닥, 본문 스크롤, 초안과 초점 복귀 |
| 업무 탭 | `src/components/ui/workspace-tabs.tsx` | 지연 조회는 수동 활성화, 선택과 포커스 구분, 기존 패널 상태 유지 |
| 로딩·처리 결과 | `src/components/ui/skeleton.tsx`, `action-feedback.tsx` | 중립적인 로딩, 레이아웃 유지, 기존 alert/status와 재시도 의미 |
| 설정 목록 | `src/features/management/settings-master-layout.tsx` | 공통 표 토큰, 상단바 아래의 도구막대, 동일한 저장/오류 동작 |

표 구성의 상세 사용법은 `src/components/data-table/README.md`를 따른다. 관리 목록의 기준 화면은 `/admin/students`, 복잡한 편집은 `/admin/classes`, 설정 목록은 `/admin/settings/schools`, 시간순 요약은 `/admin/dashboard`다. 기준 화면은 시각·상호작용의 비교 대상이며 다른 메뉴의 업무 데이터나 필드를 복사하는 템플릿은 아니다.

등록 목록은 단계 탭 아래 한 외곽면 안에 공통 검색/등록 행동, 상담 담당 범위, 데이터, 페이지 이동을 배치한다. `RegistrationListToolbar`는 공통 `DataTableToolbar`, `DataTableCommandRow`, `Button`의 합성이며 별도 시각 체계를 만들지 않는다. 과목별 행 정렬과 상담 기본 범위 `내 담당`을 유지하고, 건수가 0이어도 담당 범위 버튼은 남긴다. 등록 상세에서는 과목 탭과 진행상태를 같은 행에 두고 진행상태 메뉴는 다른 입력과 동일한 `RegistrationSelect`를 사용한다. 관리팀 알림은 해당 진행 단계의 `sectionActions`, 방문 취소 전달은 상담 영역 아래에 배치하며 고정 헤더에 넣지 않는다. 기존 단계별 폼은 접거나 다시 마운트하지 않아 미저장 입력을 유지한다.

행에서 연 편집창은 공통 `DialogContent`의 `restoreFocusToOpener`를 사용한다. 저장 후 행이 사라지거나 직접 링크로 열 수 있는 화면은 `restoreFocusFallbackRef`에 검색창 등 기존 목록 제어를 연결한다. 같은 경로에서 열기 요소를 우선하고, 보이지 않거나 비활성화된 요소·다른 경로에는 초점을 되돌리지 않는다.

프로필 창과 입력 상태는 반응형 사이드바 바깥의 안정된 소유자에 둔다. 계정 메뉴만 현재 사이드바 footer에 연결해 화면 크기가 바뀌어도 열린 창·선택·입력을 유지한다. 닫을 때 현재 계정 메뉴로 초점을 돌리고 메뉴가 보이지 않으면 본문으로 복귀한다.

수업 상세는 모든 편집 영역을 한 화면에 유지하고 작은 고정 영역 이동 버튼으로 수업 정보·시간표·교재·학생 명단에 접근한다. 긴 제목과 시간표 요약은 본문과 함께 스크롤하여 명단을 가리지 않는다. 명단 검색은 불러온 행을 대상으로 하며 추가 페이지가 있으면 검색 범위와 더 불러오기를 표시한다. 수강 해제 등의 보조 행동은 공통 `DataTableRowActions`에 둔다. 높은 관리 dialog 안에서는 `menuLayer="dialog"`로 메뉴만 z90에 올리고 기본 목록의 z50은 유지한다. 저장 오류는 상세 하단 행동 영역에 한 번 표시한다.

등록 폼의 같은 행동 행에 있는 저장·초기화·알림 버튼은 공통 `Button size="form"`을 사용한다. 모바일은 `--touch-target-height`(44px), sm 이상은 `--control-height`(36px)이며, `RegistrationSaveButton`의 기본 크기도 같다. 기능마다 `min-h-11` 또는 `size="sm"`으로 섞지 않는다. 선택 입력에는 별도 “선택” 표시를 붙이지 않고 필수 입력만 `*`로 표시한다. 별표는 시각 표식이며 스크린리더용 “필수 입력”, required 및 기존 검증 규칙은 유지한다. 섹션 전체의 잠김/미지원 안내는 shell에서 한 번만 표시하고 자식 폼에서 반복하지 않는다.

### 메뉴 안에서 바꿀 것과 공통으로 바꿀 것

- 메뉴 안에서는 업무에 맞는 열, 정보 순서, 묶음, 표시 밀도와 반응형 배치를 조정한다. 반복되는 버튼·필터·표·편집창의 모양과 동작은 공통 구현을 사용한다.
- `globals.css`의 기존 역할 토큰을 쓴다. 메뉴별 새 팔레트·글꼴·반경·그림자·모션 시간을 만들거나 공통 토큰을 route CSS에서 재정의하지 않는다. 표/카드는 `--radius-surface`, dialog/하단 sheet는 `--radius-overlay`, 떠 있는 메뉴는 `--shadow-popover`를 따른다. 레이아웃상의 폭·정렬·여백 조정은 가능하지만 제어의 역할별 크기를 덮어쓰지 않는다.
- 페이지 바탕·툴바·표에 중복 카드와 그림자를 쌓지 않는다. 기존 구획에 정보를 배치하고, 상단 제목이나 숫자를 반복하는 설명·요약 카드를 추가하지 않는다. 주요 행동, 실제 선택, 위험/실패 상태에만 필요한 강조를 사용한다.
- 공통 기능이 부족하면 현재 호출부를 조사하고 가장 작은 공통 variant 또는 합성 컴포넌트로 보완한다. 별도의 `AppleButton`, 메뉴 전용 필터 프레임 같은 사본을 만들지 않는다. 한 메뉴에만 필요한 데이터와 업무 상태는 공유 UI로 옮기지 않는다.
- 전역 색·반경·제어 크기·overlay·선택 표시를 바꿔야 하면 공통 구현과 이 문서를 함께 갱신한다. 메뉴 하나의 보기 좋은 결과만으로 전체에 적용하지 말고, 영향받는 목록·편집창·모바일·다크 모드의 기존 소비자를 확인한다.
- 한 메뉴의 개선 요청만으로 DB/RPC, 권한, 저장·취소 의미, 페이지 계약, 사용자가 저장한 열/필터 설정, 메뉴 순서를 바꾸지 않는다. 기능 변경이 작업 범위에 있으면 디자인 변경과 구분해 설명하고 해당 기능을 검증한다.

### 업무 목록과 학사 캘린더

- 전반·퇴원·휴보강의 검색은 기본으로 노출하며 검색 범위와 정렬은 독립된 제어다. 정렬 때문에 검색 대상을 바꾸지 않는다. 학생/수업 식별과 현재 단계의 주 행동을 기본 열에 두고, 부가 열은 공용 열 설정에서 표시한다.
- `useDataTableColumns`의 `defaultVisible: false`는 저장값이 없는 열의 추천 기본값이다. 기존 사용자의 명시적 표시값을 우선하며 필수 열은 숨기지 않는다. 기존 소비자는 생략 시 모두 표시하는 동작을 유지한다.
- 재시험 기본 열은 학생·상태·응시예정·맞은 개수·결과·다음 행동이다. 본시험·담당선생님·교재·범위·메모·출제/커트라인은 열 설정으로 볼 수 있다. 모바일은 학생과 현재 시험 행동을 먼저, 시험 정보는 펼쳐 보게 한다. 재재시험 계보와 계정 연결 경고는 업무 의미가 있어 보존한다.
- 휴보강 모바일 목록은 수업과 상태, 휴강/보강 일정과 장소, 처리에 필요한 사유를 우선한다. 신청/승인 이력은 상세에서 확인하고, 항목마다 별도 카드를 중첩하지 않는다.
- 학사 캘린더는 중립적인 일정 면과 얇은 종류 색 표식, 줄바꿈 제목을 사용한다. 다일 일정의 한 줄 높이는 44px, 내부 면은 40px로 두 줄을 확보한다. 색은 일정 종류를 구분하는 도메인 예외이며 다른 메뉴의 강조색으로 확장하지 않는다. 전체 제목은 상세에서 확인한다.
- 캘린더의 날짜 이동·기준 월과 새 일정은 상단, 검색·보기 전환은 공용 툴바에 둔다. 최초 조회 실패는 빈 일정으로 표시하지 않으며, 한 주 복구도 같은 상세·권한·닫기 초점 계약을 쓴다.
- 알림 설정은 직원/고객 채널을 공용 수동 활성화 탭으로 구분한다. 직원 채널 준비 실패가 고객 안내 진입을 가리지 않는다. 홈페이지 관리도 수동 활성화 탭과 하나의 연결된 목록 패널을 사용한다.

### 시간표

- 수업 상태는 `수강 · 개강 준비 · 종강`만 표시한다. 수업그룹 설정과 학기·기간 필터는 종료했으며 저장된 과거 기간 값으로 목록을 제한하지 않는다. 업무 이력의 날짜 검색과 수업 회차·정산 기간은 이 필터와 별개다.
- 보기 전환은 공용 `WorkspaceTabs`, 조회 조건은 `DataTableFilterPanel`과 공용 Select를 사용한다. 선생님·강의실·요일은 검색 가능한 다중 선택으로 비교 대상을 고른다.
- 두 열 비교는 충분히 넓은 화면에서만 사용한다. 모바일에서는 한 열과 시간표 내부 가로 스크롤을 유지하고 페이지 자체는 넘치지 않는다.
- 비교하는 표는 같은 시간축을 쓰며 실제 수업 전후 30분까지만 표시한다. 오전 및 23:30 이후 수업을 누락하지 않는다. 수업 색은 기존 시간표의 구분 의미를 유지한다.
- 프리셋 편집은 운영 읽기와 달리 기본 09:00–24:00에 더 이른 배치를 포함하며, 00:00–24:00을 명시적으로 선택할 수 있다. 드래그 중 축을 고정하고 분 단위 위치·높이를 보존한다. 동일 위치의 겹친 수업은 나란한 선택 가능한 면으로 모두 표시한다. 기존 수업 그림자는 읽기 전용이며 새 배치의 이동·길이 조정은 전용 핸들과 동일한 공용 편집 폼을 사용한다. 이 규칙은 시간표 프리셋 편집에만 적용한다.
- 프리셋의 화면 밖 패널은 실제 측정한 표 높이를 보존한 채 브라우저 렌더링만 미룬다. 전체 수업·충돌 자료와 시간축은 유지하고, 이미지 저장 동안 모든 표 내용을 렌더링한다. 드래그 미리보기와 충돌 안내는 독립적으로 갱신하며 취소·보기/조건 변경·화면 종료 때 제거한다.
- 읽기 전용 수업 블록의 전체 정보는 마우스, 터치, 표준 키보드 포커스로 확인할 수 있고 Escape로 닫는다. 편집용 블록의 드래그·클릭 동작은 유지한다.

- 시간표 내부 블록은 수업명 아래에 과목·선생님·강의실을 모아 읽는다. 과목/수업 구분색은 옅은 배경과 안쪽의 얇은 왼쪽 표식으로 유지하고, 작은 배지를 중첩하지 않는다. 일정 면은 공용 surface 반경과 구분색의 옅은 테두리를 사용한다. 짧은 블록은 이름을 우선 표시하며 전체 정보는 상세에서 제공한다.

### 학교 연간 일정표

- 학교·학년·시험 시기를 비교하는 행렬을 유지한다. 학교명과 시기는 줄이지 않고, 미입력 칸은 중립적으로 표현한다. 일정 구분색은 기존 의미를 유지하고 일반 면·테두리·제어는 공통 토큰을 쓴다.
- 모바일은 학교·학년별로 묶고 각 날짜에 과목명을 표시한다. 시험기간은 한 줄 전체를 사용하며 과목별 시험일은 세 열로 비교한다.
- 셀의 상세는 공용 Popover로 클릭·터치·Enter/Space에 열리고 Escape로 닫힌다. 시험범위 확인에서 기존 일정 편집창으로 이어지며 읽기 전용 권한과 내보내기 의미를 유지한다.

### 수업계획과 수업 교재

- 수업계획은 일정 편성에 집중한다. 목록은 수업·정규 시간표·편성 회차와 일정 편성 행동만 보여주고, 교재 연결과 진도 상태로 일정을 막지 않는다.
- 일정 편성은 별도 작업 화면에서 월별 날짜와 회차를 조정한다. 주요 저장 행동은 상단에 유지하고, 모바일에서는 회차 목록을 사용한다. 월 구분색은 작은 표식과 옅은 회차 배경에만 적용하며 일반 제어는 공용 컴포넌트를 사용한다.
- 교재 등록의 기준은 수업관리의 사용 교재다. 교재마다 선택적인 시작일·종료일을 입력하고, 잘못된 기간은 해당 입력 옆에 표시한다. 모바일 날짜 입력은 한 열로 배치한다.
- 기존 교재 연결·사용기간과 수업 기록은 보존한다. 수업계획의 일정 저장은 기존 교재·진도 기록을 수정하지 않는다.

### 필요한 예외

- 대시보드의 처리 대기 업무는 팀·담당자·업무 종류를 동시에 비교하는 행렬이므로 요약 행에 일반 업무 목록의 페이지 계약을 적용하지 않는다. 공통 표 안에서 최대 480px 높이로 스크롤하고, 건수로 여는 실제 업무 목록은 공통 상세창과 기본 10행, 10/15/20행 페이지 계약을 유지한다. 모바일의 가로 스크롤은 행렬 내부에 한정하며 데스크톱·390px·다크 모드와 초점 복귀를 검증한다.
- 달력·시간표·수업계획 행렬과 인쇄/내보내기 문서는 각각 시간축·교차 비교·출력 규격을 우선한다. 일반 목록 프레임을 강제하지 않되 공통 입력·버튼·색 의미는 유지한다.
- 학교/과목 색상, 일정 구분색, 차트의 실제 범례는 업무 데이터다. 임의 브랜드 색 금지 규칙을 이유로 기존 의미를 지우지 않으며 텍스트/범례를 함께 유지한다.
- 학생·수업 편집의 `student-workspace.css`와 `class-workspace.css`는 기존 필드 구성·본문 스크롤·시트 배치를 담당한다. 공통 overlay 토큰을 계속 소비하며 새로운 메뉴용 CSS 사본의 출발점으로 삼지 않는다.
- 새 예외가 필요하면 이 절에 적용 화면, 공통 구성으로 해결되지 않는 업무 이유, 유지할 공통 규칙과 검증 범위를 기록한다. 설명 없는 임시 override를 다음 메뉴의 기준으로 삼지 않는다.

### 작업 순서와 완료 조건

1. 활성 브랜치와 최신 `origin/main`, 운영 배포의 차이를 확인한다. 오래된 worktree나 이전 목업을 현재 기준으로 착각하지 않는다. 영향받는 메뉴와 위 표의 공통 컴포넌트를 먼저 정한다.
2. 수정 전 상태를 같은 합성 데이터, 화면 폭, 테마, 필터·선택 상태로 남긴다. 운영 개인정보를 fixture나 PR 이미지에 복사하지 않는다. 1440px 데스크톱과 390px 모바일을 기본 비교 폭으로 사용한다.
3. 기준 컴포넌트를 재사용해 한 메뉴를 개선한다. 코드 리뷰에서 새 style override, 유사 컴포넌트 복제, 조건부 줄 삽입, 업무 상태 변경이 없는지 확인한다.
4. 관련 자동 테스트와 실제 브라우저를 확인한다. 목록 선택 전/후 검색·필터·헤더·첫 행 위치, 필터 적용/초기화, 편집 저장 실패·취소·초안 보존·초점 복귀를 변경 범위에 맞게 확인한다. 긴 이름, 빈 상태, 로딩, 오류, 다크 모드와 동작 줄이기도 영향받는 상태를 포함한다.
5. 공유 UI를 바꿨으면 해당 소비자 중 구조가 다른 화면도 확인한다. 예: 목록 toolbar는 학생·교재 또는 등록, dialog는 학생·수업 또는 교재 문서, 셸은 목록·설정 화면. 작은 메뉴 수정에 관련 없는 전체 업무 검증을 반복하지 않는다.
6. PR에는 재사용한 기준, 변경 범위, 예외 유무와 테스트/스크린샷 근거를 남긴다. 공통 토큰·컴포넌트 변경 시 이 문서와 호출부가 함께 맞아야 완료다. 로컬 검증, 운영 배포, 실제 데이터 저장·외부 발송은 별도로 보고한다.

CI의 `Verify shared dashboard design contracts` 단계는 공통 표, 선택 상태, 페이지 계약, 제어, 탭, 색 대비와 셸의 기존 회귀 테스트를 실행한다. 이것은 일부 계약의 자동 보호이며 모든 시각적 일관성을 보장하지 않는다. 문구 존재만 검사하는 테스트로 화면 검토를 대신하지 않는다.
